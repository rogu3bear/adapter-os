#[cfg(test)]
mod kernel_buffer_layout {
    #[test]
    fn placeholder() {
        // This placeholder exists so `cargo fmt` can process the kernel_buffer_layout test target.
        assert!(true);
    }
}
