---
phase: 47-production-cut-contract-closure
plan: 01
type: execute
wave: 1
depends_on: [46]
requirements: [REL-47-01, API-47-01, SEC-47-01, OPS-47-01]
files_modified:
  - scripts/service-manager.sh
  - start
  - aosctl
  - scripts/ci/local_release_gate.sh
  - scripts/ci/local_release_gate_prod.sh
  - docs/governance/README.md
  - QUICKSTART.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/phases/47-production-cut-contract-closure/*
autonomous: true
must_haves:
  truths:
    - Local release governance defaults to fail-closed enforcement.
    - Startup preflight fails early when model path readiness is invalid.
    - `aosctl` local builds remain deterministic under SQLx env drift.
    - Planning artifacts match roadmap phase inventory.
  artifacts:
    - path: scripts/ci/local_release_gate.sh
      provides: Default governance lane behavior for local release.
    - path: scripts/service-manager.sh
      provides: Model readiness preflight guardrails.
    - path: aosctl
      provides: SQLx build env normalization for CLI rebuild path.
  key_links:
    - from: scripts/ci/local_release_gate_prod.sh
      to: scripts/ci/local_release_gate.sh
      via: Prod wrapper enforces governance mode.
      pattern: "LOCAL_RELEASE_GOVERNANCE_MODE=enforce"
---

# Phase 47 Plan 01: Production Cut Contract Closure

## Objective

Close deterministic release blockers by enforcing governance lane defaults, adding startup preflight model readiness checks, hardening `aosctl` SQLx build behavior, and restoring planning artifact integrity.

## Natural-Language Execution Plan

1. Add explicit model readiness checks to preflight and ensure `./start preflight` runs them.
2. Normalize `aosctl` build environment for SQLx online/offline mode selection.
3. Default local release governance mode to `enforce` and document override semantics.
4. Create phase-47 planning artifacts so roadmap and filesystem stay consistent.

## Success Criteria

- [ ] `./start preflight` fails on missing model path with actionable error.
- [ ] `./aosctl --rebuild --help` succeeds when `DATABASE_URL` points to a missing sqlite file by forcing `SQLX_OFFLINE=1`.
- [ ] `scripts/ci/local_release_gate.sh` defaults governance mode to `enforce`.
- [ ] GSD health check no longer emits `W006`.

