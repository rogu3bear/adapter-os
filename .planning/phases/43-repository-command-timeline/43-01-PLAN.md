---
phase: 43-repository-command-timeline
plan: 01
type: execute
wave: 1
depends_on: [42]
requirements: [UX-41-01]
files_modified:
  - crates/adapteros-ui/src/components/adapter_detail_panel.rs
  - .planning/phases/43-repository-command-timeline/43-01-SUMMARY.md
  - .planning/phases/43-repository-command-timeline/43-VERIFICATION.md
  - .planning/phases/43-repository-command-timeline/43-UAT.md
autonomous: true
must_haves:
  truths:
    - Adapter detail shows repository command history timeline with latest-first ordering.
    - Timeline refreshes after promote/checkout so operator history is immediately current.
    - Timeline copy remains command-first and assistive-friendly.
  artifacts:
    - path: crates/adapteros-ui/src/components/adapter_detail_panel.rs
      provides: Timeline fetch, render, and refresh-on-command behavior.
  key_links:
    - from: crates/adapteros-ui/src/components/adapter_detail_panel.rs
      to: crates/adapteros-ui/src/api/client.rs
      via: Update Center timeline uses `get_repo_timeline` as canonical source.
      pattern: "get_repo_timeline"
---

# Phase 43 Plan 01: Land Repository Command Timeline

## Objective

Expose repository command timeline directly in adapter detail Update Center so operators can validate promote/checkout history before feeding new data.

## Natural-Language Execution Plan

1. Add timeline state, load, and error handling in `AdapterVersionPromotionSection`.
2. Render timeline card with latest-first entries and explicit command-history messaging.
3. Refresh timeline after successful promote/checkout actions.
4. Run targeted compile and artifact verification checks.

## Success Criteria

- [x] Timeline surface exists in adapter detail Update Center.
- [x] Promote/checkout success paths refresh timeline.
- [x] Timeline language remains plain and command-oriented.
