---
phase: 54-performance-and-security-hardening
plan: 03
type: execute
wave: 3
depends_on: ["54-01", "54-02"]
files_modified:
  - crates/adapteros-memory/src/pressure_manager.rs
  - crates/adapteros-server-api/src/sse/lifecycle_events.rs
  - crates/adapteros-server-api/src/handlers/sse_streams.rs
  - crates/adapteros-ui/src/sse.rs
  - crates/adapteros-ui/src/signals/notifications.rs
  - scripts/contracts/check_security_audit.sh
autonomous: true
requirements: [PERF-54-02, SEC-54-02]

must_haves:
  truths:
    - "When an adapter is evicted from memory, a toast notification appears in the UI"
    - "Eviction events flow from MemoryPressureManager through SSE Alerts stream to UI toast"
    - "Model weight files in var/models/ have restricted file permissions (0600 or 0700)"
    - "Failed auth attempts, rate limit hits, and suspicious input are logged with structured fields"
  artifacts:
    - path: "crates/adapteros-server-api/src/sse/lifecycle_events.rs"
      provides: "MemoryEviction variant on SystemHealthEvent or new MemoryEvent enum"
      contains: "evict"
    - path: "crates/adapteros-ui/src/sse.rs"
      provides: "AlertEvent or SystemEvent deserialization for eviction notifications"
      contains: "evict"
    - path: "crates/adapteros-ui/src/signals/notifications.rs"
      provides: "Eviction toast handler that shows brief non-blocking notification"
      contains: "evict"
  key_links:
    - from: "crates/adapteros-memory/src/pressure_manager.rs"
      to: "crates/adapteros-server-api/src/sse/lifecycle_events.rs"
      via: "Eviction produces a broadcast event that SSE infrastructure sends to Alerts stream"
      pattern: "broadcast|evict"
    - from: "crates/adapteros-server-api/src/sse/lifecycle_events.rs"
      to: "crates/adapteros-ui/src/sse.rs"
      via: "UI deserializes Alerts SSE events into typed enum for toast display"
      pattern: "AlertEvent|MemoryEvent"
    - from: "crates/adapteros-ui/src/sse.rs"
      to: "crates/adapteros-ui/src/signals/notifications.rs"
      via: "Deserialized eviction event triggers toast via notification context"
      pattern: "toast|notification.*evict"
---

<objective>
Wire the eviction notification pipeline from memory manager to UI toast, harden model weight file permissions, and add structured security audit trail logging.

Purpose: Complete PERF-54-02 by making adapter evictions visible to operators (per user decision: "Toast notification when adapter is evicted"). Complete SEC-54-02 by protecting model weights at the OS level and logging security-relevant events with structured fields for the audit trail.

Output: End-to-end eviction notification (memory -> SSE -> UI toast), model weight file permission enforcement, structured security event logging.
</objective>

<execution_context>
@/Users/star/.claude/get-shit-done/workflows/execute-plan.md
@/Users/star/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-performance-and-security-hardening/54-CONTEXT.md
@.planning/phases/54-performance-and-security-hardening/54-RESEARCH.md
@.planning/phases/54-performance-and-security-hardening/54-01-SUMMARY.md
@.planning/phases/54-performance-and-security-hardening/54-02-SUMMARY.md
@crates/adapteros-memory/src/pressure_manager.rs
@crates/adapteros-server-api/src/sse/lifecycle_events.rs
@crates/adapteros-server-api/src/sse/types.rs
@crates/adapteros-server-api/src/handlers/sse_streams.rs
@crates/adapteros-server-api/src/auth.rs
@crates/adapteros-server-api/src/middleware_security.rs
@crates/adapteros-ui/src/sse.rs
@crates/adapteros-ui/src/signals/notifications.rs
@crates/adapteros-ui/src/components/toast.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eviction notification pipeline (SSE + UI toast)</name>
  <files>
    crates/adapteros-memory/src/pressure_manager.rs
    crates/adapteros-server-api/src/sse/lifecycle_events.rs
    crates/adapteros-server-api/src/handlers/sse_streams.rs
    crates/adapteros-ui/src/sse.rs
    crates/adapteros-ui/src/signals/notifications.rs
  </files>
  <action>
    Wire end-to-end: memory eviction -> broadcast channel -> SSE Alerts stream -> UI toast.

    1. **Backend: Eviction event type** -- Add to `crates/adapteros-server-api/src/sse/lifecycle_events.rs`:
       - Add `AdapterEvicted` variant to `SystemHealthEvent` enum (which maps to `SseStreamType::Alerts`):
         ```rust
         AdapterEvicted {
             adapter_id: String,
             adapter_name: String,
             reason: String, // e.g., "memory_pressure_lru"
             freed_mb: u32,
         },
         ```
       - Follow existing `#[serde(tag = "event", rename_all = "snake_case")]` pattern

    2. **Backend: Emit eviction event** -- In `crates/adapteros-memory/src/pressure_manager.rs`:
       - After an adapter is evicted in the LRU eviction path, emit the event
       - The pressure manager needs access to a `tokio::sync::broadcast::Sender<SystemHealthEvent>` (or a more general event sender)
       - Check how other SSE events are emitted in the codebase (look for `SseStreamType::Alerts` send patterns in handlers or other managers)
       - If the pressure manager doesn't have access to the SSE broadcast channel, add it as a constructor parameter or use a global/app-state channel
       - Only emit if `UmaMemoryConfig::eviction_notifications` is true (from Plan 01 config)

    3. **Backend: SSE stream delivery** -- Verify `SseStreamType::Alerts` stream handler delivers events to connected clients:
       - Check `crates/adapteros-server-api/src/handlers/sse_streams.rs` for the Alerts stream handler
       - Ensure `SystemHealthEvent::AdapterEvicted` variants are serialized and sent
       - If the Alerts stream handler only forwards specific event types, add AdapterEvicted to its filter

    4. **UI: Deserialize alert events** -- In `crates/adapteros-ui/src/sse.rs`:
       - Add an `AlertEvent` enum (or extend existing) that can deserialize `SystemHealthEvent` variants:
         ```rust
         #[derive(Debug, Clone, Deserialize)]
         #[serde(tag = "event", rename_all = "snake_case")]
         pub enum AlertEvent {
             AdapterEvicted {
                 adapter_id: String,
                 adapter_name: String,
                 reason: String,
                 freed_mb: u32,
             },
             #[serde(other)]
             Other,
         }
         ```
       - Use `#[serde(other)]` for forward compatibility (same pattern as InferenceEvent)
       - Use `#[serde(default)]` on all fields for backward compatibility

    5. **UI: Toast notification** -- In `crates/adapteros-ui/src/signals/notifications.rs`:
       - Add handler for eviction events that creates a toast:
         - Severity: `Warning` (not Error -- eviction is expected behavior under memory pressure)
         - Message: "Adapter {name} unloaded to free memory" (per user decision: brief, non-blocking)
         - Auto-dismiss: Use existing auto-dismiss timer (toast infrastructure already supports this)
         - Use `try_get()` / `try_set()` for all signal access (Leptos 0.7 disposal safety)
       - Wire the alerts SSE subscription:
         - If the UI doesn't already subscribe to `SseStreamType::Alerts`, add the subscription
         - Parse incoming events as `AlertEvent`, dispatch `AdapterEvicted` to toast
         - Research pitfall 5: verify UI subscribes to Alerts stream (may need new `EventSource` connection)

    CRITICAL: Use `leptos::logging::log!` not `println!` in UI code. Use `try_get()`/`try_set()` not `.get()`/`.set()` on signals.
  </action>
  <verify>
    <automated>cargo check -p adapteros-server-api && cargo check -p adapteros-memory && ./scripts/ui-check.sh</automated>
    <manual>Verify SystemHealthEvent has AdapterEvicted variant, UI sse.rs has AlertEvent enum, notifications.rs handles eviction toast</manual>
  </verify>
  <done>
    Eviction event flows end-to-end: pressure_manager emits AdapterEvicted -> SSE Alerts stream delivers to UI -> UI shows warning toast "Adapter X unloaded to free memory". Toast is brief, non-blocking, auto-dismissing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Model weight protection and security audit trail</name>
  <files>
    crates/adapteros-server-api/src/auth.rs
    crates/adapteros-server-api/src/middleware_security.rs
    scripts/contracts/check_security_audit.sh
  </files>
  <action>
    1. **Model weight file permissions** -- Add a boot-time check/enforcement for model file permissions:
       - In the server boot sequence (find where `var/models/` is created or accessed), add a function that sets directory permissions to 0700 and file permissions to 0600 on `var/models/` and its contents
       - Use `std::fs::set_permissions` with `std::os::unix::fs::PermissionsExt`
       - Log a warning if permissions cannot be set (e.g., running as non-owner)
       - This ensures model weights are only readable by the server process owner
       - Add this check to `scripts/contracts/check_security_audit.sh` (from Plan 02): verify var/models/ has correct permissions if it exists

    2. **Structured security audit trail logging** -- Add structured `tracing` events for security-relevant actions:
       a. In `crates/adapteros-server-api/src/auth.rs`:
          - On failed authentication (invalid JWT, expired token, bad API key): emit `tracing::warn!` with structured fields:
            ```rust
            tracing::warn!(
                event = "auth_failure",
                reason = %reason,
                source_ip = %client_ip,
                endpoint = %path,
                auth_mode = %mode,
                "authentication failed"
            );
            ```
          - Check existing auth failure paths and ensure they all log structured events (not just return error responses)

       b. In `crates/adapteros-server-api/src/middleware_security.rs`:
          - On rate limit exceeded (429 response): emit `tracing::warn!` with structured fields:
            ```rust
            tracing::warn!(
                event = "rate_limit_exceeded",
                tenant_id = %tenant_id,
                source_ip = %client_ip,
                endpoint = %path,
                tier = %route_tier,
                limit_rpm = %applied_limit,
                "rate limit exceeded"
            );
            ```
          - On suspicious input detection (if any input validation rejects a request): log with `event = "input_validation_failure"`

       c. Ensure all security events use consistent field names: `event`, `reason`, `source_ip`, `endpoint`, `tenant_id` where applicable

    3. **Fail-closed auth in production** (verify/harden):
       - Verify `dev_bypass_status()` blocks in release builds (already implemented per research)
       - Ensure `AOS_DEV_NO_AUTH=1` only works in debug builds
       - If not already enforced, add `#[cfg(debug_assertions)]` guard around the dev bypass path
       - This is a verification task -- the mechanism exists, ensure it's airtight

    4. **Update check_security_audit.sh** (from Plan 02) to add:
       - Check that `var/models/` permissions are 0700 (if directory exists)
       - Check that auth failure logging includes structured `event` field (grep for `event = "auth_failure"`)
       - Check that rate limit exceeded logging includes structured `event` field
  </action>
  <verify>
    <automated>cargo check -p adapteros-server-api && bash -n scripts/contracts/check_security_audit.sh</automated>
    <manual>Verify auth.rs logs structured auth_failure events, middleware_security.rs logs rate_limit_exceeded events, model weight permission enforcement exists</manual>
  </verify>
  <done>
    Model weights in var/models/ get 0700/0600 permissions at boot. Auth failures, rate limit hits, and input validation failures are logged with structured fields (event, source_ip, endpoint, reason). Dev bypass is confirmed fail-closed in production. Security audit script checks all of the above.
  </done>
</task>

</tasks>

<verification>
- `cargo check -p adapteros-server-api` passes (all backend changes compile)
- `cargo check -p adapteros-memory` passes (eviction event emission compiles)
- `./scripts/ui-check.sh` passes (UI compiles with AlertEvent and toast handler)
- `grep -q 'AdapterEvicted' crates/adapteros-server-api/src/sse/lifecycle_events.rs` confirms event type exists
- `grep -q 'auth_failure' crates/adapteros-server-api/src/auth.rs` confirms structured logging
- `grep -q 'rate_limit_exceeded' crates/adapteros-server-api/src/middleware_security.rs` confirms structured logging
</verification>

<success_criteria>
1. Adapter eviction triggers a visible toast notification in the UI ("Adapter X unloaded to free memory")
2. Model weight files have restricted OS permissions (0700 dir, 0600 files)
3. Failed auth, rate limit hits, and validation failures are logged with structured event fields
4. Dev bypass is confirmed fail-closed in release/production builds
5. Security audit script validates all security hardening measures
</success_criteria>

<output>
After completion, create `.planning/phases/54-performance-and-security-hardening/54-03-SUMMARY.md`
</output>
