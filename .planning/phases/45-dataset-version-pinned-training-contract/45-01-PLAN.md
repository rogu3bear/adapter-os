---
phase: 45-dataset-version-pinned-training-contract
plan: 01
type: execute
wave: 1
depends_on: [44]
requirements: [VC-41-01, DOC-41-01]
files_modified:
  - crates/adapteros-ui/src/pages/training/wizard.rs
  - .planning/phases/45-dataset-version-pinned-training-contract/45-01-SUMMARY.md
  - .planning/phases/45-dataset-version-pinned-training-contract/45-VERIFICATION.md
  - .planning/phases/45-dataset-version-pinned-training-contract/45-UAT.md
autonomous: true
must_haves:
  truths:
    - Training wizard uses typed `create_training_job` request path.
    - Dataset version ID is carried into submit payload when available.
    - Review step and dataset status surfaces show dataset version context.
  artifacts:
    - path: crates/adapteros-ui/src/pages/training/wizard.rs
      provides: Typed request construction and dataset-version continuity.
  key_links:
    - from: crates/adapteros-ui/src/pages/training/wizard.rs
      to: crates/adapteros-ui/src/api/client.rs
      via: Wizard submit now calls `create_training_job`.
      pattern: "create_training_job"
---

# Phase 45 Plan 01: Typed Training Contract with Dataset Version Pinning

## Objective

Ensure adapter training starts through typed API contracts and preserves explicit dataset version provenance.

## Natural-Language Execution Plan

1. Add backend mapping helpers for typed training enums.
2. Track and surface `dataset_version_id` across dataset selection/upload.
3. Replace legacy submit route with `CreateTrainingJobRequest` + `TrainingConfigRequest`.
4. Re-validate targeted compile and phase artifact checks.

## Success Criteria

- [x] Wizard submit uses typed training request path.
- [x] `dataset_version_id` is populated when available.
- [x] Review UI shows dataset version for operator confirmation.
