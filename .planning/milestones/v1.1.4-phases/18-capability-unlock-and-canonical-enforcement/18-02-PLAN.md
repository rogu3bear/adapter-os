---
phase: 18-capability-unlock-and-canonical-enforcement
plan: 02
type: execute
wave: 1
depends_on: [18-01]
requirements: [GOV-14]
files_modified:
  - scripts/ci/check_governance_preflight.sh
  - var/evidence/governance-enforcement-<UTCSTAMP>/blocked-preflight.log
  - var/evidence/governance-enforcement-<UTCSTAMP>/blocked-branch.txt
  - var/evidence/governance-enforcement-<UTCSTAMP>/blocked-write-attempts.txt
autonomous: true
user_setup:
  - Canonical target capability may remain externally blocked and must be treated as legal blocked branch.
must_haves:
  truths:
    - Blocked branch execution never attempts required_status_checks PATCH/write operations.
    - Blocked branch artifacts preserve explicit blocker reason with deterministic no-write receipt.
  artifacts:
    - path: var/evidence/governance-enforcement-<UTCSTAMP>/blocked-branch.txt
      provides: Branch classification and operator-ready blocker statement.
      contains: blocked_external
    - path: var/evidence/governance-enforcement-<UTCSTAMP>/blocked-write-attempts.txt
      provides: Explicit no-write assertion for blocked execution branch.
      contains: write_attempts=0
  key_links:
    - from: var/evidence/governance-enforcement-<UTCSTAMP>/blocked-preflight.log
      to: var/evidence/governance-enforcement-<UTCSTAMP>/blocked-write-attempts.txt
      via: gate-state branch guard
---

# Phase 18 Plan 02: Blocked Branch No-Write Enforcement

## Objective
- **What:** Execute blocked branch path and prove strict no-write behavior under external capability block.
- **Why:** Deterministic safety requires preventing unsafe policy mutations while capability is unavailable.
- **Output:** Immutable blocked-branch evidence set with explicit no-write receipts.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-capability-unlock-and-canonical-enforcement/18-CONTEXT.md
@.planning/phases/18-capability-unlock-and-canonical-enforcement/18-RESEARCH.md
@.planning/phases/18-capability-unlock-and-canonical-enforcement/18-01-PLAN.md

## Tasks

<task type="code">
  <name>Execute blocked branch probe and capture blocker receipt</name>
  <files>scripts/ci/check_governance_preflight.sh, var/evidence/governance-enforcement-<UTCSTAMP>/blocked-preflight.log, var/evidence/governance-enforcement-<UTCSTAMP>/blocked-branch.txt</files>
  <action>Run canonical preflight and, when blocked, persist explicit blocked branch receipt with parsed status, reason, endpoint, and deterministic exit-code mapping.</action>
  <verify>Blocked receipt includes status token and consistent blocker reason fields from preflight output.</verify>
  <done>Blocked branch truth is captured in immutable artifacts.</done>
</task>

<task type="code">
  <name>Prove zero-write behavior under blocked branch</name>
  <files>var/evidence/governance-enforcement-<UTCSTAMP>/blocked-write-attempts.txt</files>
  <action>Produce deterministic no-write receipt asserting zero PATCH/write attempts and listing guard conditions that prevented mutation commands.</action>
  <verify>Receipt contains `write_attempts=0` and references blocked gate-state source artifact.</verify>
  <done>No-write guarantee is auditable and explicit.</done>
</task>

<task type="code">
  <name>Prepare capable-branch handoff package</name>
  <files>var/evidence/governance-enforcement-<UTCSTAMP>/capable-handoff.txt</files>
  <action>Record exact command contract, required artifacts, and prerequisites that must be satisfied before Plan 03 capable execution proceeds.</action>
  <verify>Handoff package includes pre-read/write/post-read/rollback artifact checklist with deterministic filenames.</verify>
  <done>Plan 03 can execute quickly and safely once capability transitions to capable.</done>
</task>

## Verification
- Confirm blocked branch artifacts exist and contain explicit status and no-write statements.
- Confirm no files resembling write receipts are generated under blocked branch.
- Confirm capable-branch handoff is complete and references immutable blocked artifacts.

## Success Criteria
- [ ] Blocked branch classification is captured deterministically.
- [ ] No-write receipt proves zero mutation attempts under blocked conditions.
- [ ] Capable-branch handoff artifact is ready for immediate Plan 03 execution.
