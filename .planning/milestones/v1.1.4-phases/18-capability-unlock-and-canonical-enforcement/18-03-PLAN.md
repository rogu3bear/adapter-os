---
phase: 18-capability-unlock-and-canonical-enforcement
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
requirements: [GOV-15]
files_modified:
  - scripts/ci/execute_governance_required_checks.sh
  - docs/governance/README.md
  - var/evidence/governance-enforcement-<UTCSTAMP>/pre-read.json
  - var/evidence/governance-enforcement-<UTCSTAMP>/write.json
  - var/evidence/governance-enforcement-<UTCSTAMP>/post-read.json
  - var/evidence/governance-enforcement-<UTCSTAMP>/rollback-write.json
  - var/evidence/governance-enforcement-<UTCSTAMP>/rollback-post-read.json
autonomous: true
user_setup:
  - Capable branch execution requires preflight terminal status `capable`.
must_haves:
  truths:
    - Canonical required-check enforcement write/readback is proven only on capable branch.
    - Rollback path is captured to preserve safe reversibility and auditability.
  artifacts:
    - path: var/evidence/governance-enforcement-<UTCSTAMP>/post-read.json
      provides: Post-write required_status_checks proof for strict + required context presence.
      contains: FFI AddressSanitizer (push)
    - path: var/evidence/governance-enforcement-<UTCSTAMP>/rollback-post-read.json
      provides: Post-rollback required_status_checks snapshot.
      contains: contexts
  key_links:
    - from: var/evidence/governance-enforcement-<UTCSTAMP>/pre-read.json
      to: var/evidence/governance-enforcement-<UTCSTAMP>/post-read.json
      via: required_status_checks PATCH enforcement write
    - from: var/evidence/governance-enforcement-<UTCSTAMP>/post-read.json
      to: var/evidence/governance-enforcement-<UTCSTAMP>/rollback-post-read.json
      via: rollback write/readback safety proof
---

# Phase 18 Plan 03: Canonical Enforcement Write/Readback and Rollback Proof

## Objective
- **What:** Execute capable-branch canonical required-check enforcement with read/write/readback and rollback-safe evidence.
- **Why:** Milestone closure requires strict enforcement proof, not parity-only approximation.
- **Output:** Complete capable-branch enforcement evidence package plus reconciliation-ready receipts.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-capability-unlock-and-canonical-enforcement/18-CONTEXT.md
@.planning/phases/18-capability-unlock-and-canonical-enforcement/18-RESEARCH.md
@.planning/phases/18-capability-unlock-and-canonical-enforcement/18-02-PLAN.md

## Tasks

<task type="code">
  <name>Implement executable capable-path enforcement flow with rollback guard</name>
  <files>scripts/ci/execute_governance_required_checks.sh</files>
  <action>Implement a deterministic executor that performs preflight gating, baseline capture, preserve+add write/readback verification, and rollback-to-baseline on verification failure.</action>
  <verify>Script exits with explicit status classes and produces deterministic artifact set for blocked/capable branches.</verify>
  <done>Operators can run a single command to execute canonical enforcement safely.</done>
</task>

<task type="code">
  <name>Execute strict required-check write and post-read verification</name>
  <files>var/evidence/governance-enforcement-<UTCSTAMP>/write.json, var/evidence/governance-enforcement-<UTCSTAMP>/post-read.json, var/evidence/governance-enforcement-<UTCSTAMP>/post-contexts.txt</files>
  <action>Apply required_status_checks PATCH with strict=true and preserve+add context union, then read back canonical policy state and normalize contexts for verification.</action>
  <verify>Post-read proves strict=true and includes required context set, including `FFI AddressSanitizer (push)`.</verify>
  <done>Canonical enforcement behavior is proven on capable branch.</done>
</task>

<task type="code">
  <name>Capture rollback write/readback safety artifacts</name>
  <files>var/evidence/governance-enforcement-<UTCSTAMP>/rollback-write.json, var/evidence/governance-enforcement-<UTCSTAMP>/rollback-post-read.json</files>
  <action>Apply rollback to baseline policy shape and capture post-rollback readback receipt to prove reversibility and safe operator recovery path.</action>
  <verify>Rollback readback payload is present and matches baseline policy expectations for approved rollback scope.</verify>
  <done>Enforcement closure includes reversible safety evidence, not one-way mutation proof only.</done>
</task>

## Verification
- Confirm capable branch gate-state before any write attempt.
- Confirm pre-read/write/post-read/rollback artifacts are present and JSON-parseable.
- Confirm post-read includes strict=true and required contexts, and rollback readback is captured.

## Success Criteria
- [ ] Capable-branch canonical write/readback is evidenced with strict policy proof.
- [ ] Rollback write/readback receipts are captured for safety.
- [ ] Evidence package is ready for phase-level verification and milestone reconciliation.
