---
phase: 18-capability-unlock-and-canonical-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [GOV-14, AUTO-02]
files_modified:
  - scripts/ci/run_governance_capability_loop.sh
  - docs/governance/README.md
  - var/evidence/governance-enforcement-<UTCSTAMP>/capability-loop.log
autonomous: true
user_setup:
  - GitHub CLI auth is configured for canonical governance target probes.
must_haves:
  truths:
    - Capability probe loop emits deterministic status transitions with explicit exit codes.
    - Unlock detection artifacts distinguish blocked and capable outcomes without ambiguity.
  artifacts:
    - path: var/evidence/governance-enforcement-<UTCSTAMP>/capability-loop.log
      provides: Timestamped capability probe sequence and terminal status.
      contains: status=
    - path: var/evidence/governance-enforcement-<UTCSTAMP>/gate-state.txt
      provides: Canonical terminal gate classification for downstream branching.
      contains: blocked_external
  key_links:
    - from: scripts/ci/run_governance_capability_loop.sh
      to: var/evidence/governance-enforcement-<UTCSTAMP>/capability-loop.log
      via: deterministic preflight polling transcript
---

# Phase 18 Plan 01: Deterministic Capability Unlock Detection

## Objective
- **What:** Harden capability probe behavior and produce deterministic unlock detection artifacts.
- **Why:** Enforcement execution must branch on explicit capability truth, not ad hoc retries.
- **Output:** Stable capability-loop evidence package with terminal gate-state receipt.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-capability-unlock-and-canonical-enforcement/18-CONTEXT.md
@.planning/phases/18-capability-unlock-and-canonical-enforcement/18-RESEARCH.md

## Tasks

<task type="code">
  <name>Implement deterministic capability polling wrapper</name>
  <files>scripts/ci/run_governance_capability_loop.sh</files>
  <action>Add a reusable polling command that wraps governance preflight, captures per-attempt logs/exits, and emits canonical gate-state receipts for branch-safe enforcement execution.</action>
  <verify>Loop output preserves stable field ordering (`ts`, `attempt`, `exit`, `status`, `reason`) and deterministic terminal exit/status mapping.</verify>
  <done>Capability branch conditions are machine-consumable and reproducible across runs.</done>
</task>

<task type="code">
  <name>Capture capability-loop evidence transcript</name>
  <files>var/evidence/governance-enforcement-<UTCSTAMP>/capability-loop.log, var/evidence/governance-enforcement-<UTCSTAMP>/gate-state.txt</files>
  <action>Run deterministic preflight probes for canonical target, persist timestamped transcript, and derive terminal gate-state receipt consumed by Plans 02 and 03.</action>
  <verify>Transcript includes explicit status per probe and gate-state receipt contains one legal terminal outcome token.</verify>
  <done>Capability branch condition is immutable and auditable.</done>
</task>

<task type="code">
  <name>Align governance runbook wording to unlock-aware gating semantics</name>
  <files>docs/governance/README.md</files>
  <action>Update runbook language so no-write blocked branch and capable write/readback branch are both explicit and non-contradictory.</action>
  <verify>Runbook contains deterministic gate model and does not imply writes under blocked conditions.</verify>
  <done>Operator guidance matches enforced runtime branch behavior.</done>
</task>

## Verification
- Run repeated canonical preflight probes and diff status field ordering.
- Confirm capability-loop transcript and gate-state receipt are generated once per UTC-stamped evidence directory.
- Confirm runbook text mirrors blocked vs capable branch semantics.

## Success Criteria
- [ ] Capability polling output is deterministic and parse-stable.
- [ ] Unlock detection artifacts exist with explicit terminal gate-state.
- [ ] Governance runbook correctly documents unlock-aware execution behavior.
