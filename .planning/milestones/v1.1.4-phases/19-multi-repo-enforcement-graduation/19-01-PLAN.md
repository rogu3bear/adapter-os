---
phase: 19-multi-repo-enforcement-graduation
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [OPS-10]
files_modified:
  - docs/governance/target-manifest.json
  - docs/governance/README.md
  - var/evidence/governance-graduation-<UTCSTAMP>/target-matrix-schema.txt
autonomous: true
user_setup:
  - Approved target manifest ownership remains available for controlled metadata updates.
must_haves:
  truths:
    - Enforcement-ready target policy is explicit and machine-checkable.
    - Matrix schema captures both raw and final classified outcomes per target.
  artifacts:
    - path: var/evidence/governance-graduation-<UTCSTAMP>/target-matrix-schema.txt
      provides: Deterministic matrix contract for multi-target graduation outputs.
      contains: outcome
    - path: docs/governance/target-manifest.json
      provides: Approved target inventory with exception metadata used by graduation audit.
      contains: approved_exceptions
  key_links:
    - from: docs/governance/target-manifest.json
      to: var/evidence/governance-graduation-<UTCSTAMP>/target-matrix-schema.txt
      via: per-target outcome-classification contract
---

# Phase 19 Plan 01: Enforcement-Ready Target Policy and Matrix Contract

## Objective
- **What:** Lock approved target policy and matrix schema for multi-repo enforcement graduation.
- **Why:** Graduation verification depends on deterministic per-target classification and operator action mapping.
- **Output:** Finalized target/exception metadata and matrix schema artifact.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-multi-repo-enforcement-graduation/19-CONTEXT.md
@.planning/phases/19-multi-repo-enforcement-graduation/19-RESEARCH.md

## Tasks

<task type="code">
  <name>Reconcile approved target manifest for graduation run</name>
  <files>docs/governance/target-manifest.json</files>
  <action>Review and reconcile target-level metadata (probe context, required contexts, approved exceptions) so graduation classification remains explicit and reproducible.</action>
  <verify>Manifest validates cleanly and target ids/repo+branch pairs remain unique.</verify>
  <done>Graduation run scope is locked and deterministic.</done>
</task>

<task type="code">
  <name>Define target outcome matrix schema artifact</name>
  <files>var/evidence/governance-graduation-<UTCSTAMP>/target-matrix-schema.txt</files>
  <action>Create a deterministic schema receipt defining required matrix fields: target id, repo, branch, raw_outcome, final_outcome, strict_state, missing_context_count, routing_action.</action>
  <verify>Schema includes all required fields in stable order and references legal outcome classes.</verify>
  <done>Plan 02 can emit predictable matrix outputs.</done>
</task>

<task type="code">
  <name>Update operator routing guidance for graduation outcomes</name>
  <files>docs/governance/README.md</files>
  <action>Document target-level routing actions for each outcome class, including when to escalate blockers vs remediate drift vs accept approved exceptions.</action>
  <verify>README routing table aligns with matrix schema and manifest exception policy language.</verify>
  <done>Operators can act on graduation outputs without ambiguity.</done>
</task>

## Verification
- Validate manifest after policy reconciliation.
- Confirm matrix schema artifact exists with deterministic field contract.
- Confirm governance README contains explicit outcome-to-action routing.

## Success Criteria
- [ ] Approved target policy metadata is validated and stable.
- [ ] Matrix schema artifact defines deterministic per-target outputs.
- [ ] Operator routing guidance matches classification contract.
