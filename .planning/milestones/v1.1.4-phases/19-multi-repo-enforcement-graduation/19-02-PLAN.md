---
phase: 19-multi-repo-enforcement-graduation
plan: 02
type: execute
wave: 1
depends_on: [19-01]
requirements: [OPS-10]
files_modified:
  - scripts/ci/audit_governance_drift.sh
  - scripts/ci/render_governance_graduation_receipts.sh
  - var/evidence/governance-graduation-<UTCSTAMP>/report.json
  - var/evidence/governance-graduation-<UTCSTAMP>/report.txt
  - var/evidence/governance-graduation-<UTCSTAMP>/graduation-matrix.txt
  - var/evidence/governance-graduation-<UTCSTAMP>/routing-actions.txt
autonomous: true
user_setup:
  - GitHub CLI auth is available for all approved manifest targets.
must_haves:
  truths:
    - Multi-target graduation run produces deterministic target-level outcomes.
    - Routing actions are generated directly from classified outcomes and exception policy.
  artifacts:
    - path: var/evidence/governance-graduation-<UTCSTAMP>/graduation-matrix.txt
      provides: Per-target outcome matrix including raw and final outcomes.
      contains: final_outcome
    - path: var/evidence/governance-graduation-<UTCSTAMP>/routing-actions.txt
      provides: Deterministic operator action list per target outcome.
      contains: action=
  key_links:
    - from: var/evidence/governance-graduation-<UTCSTAMP>/report.json
      to: var/evidence/governance-graduation-<UTCSTAMP>/graduation-matrix.txt
      via: result normalization and deterministic sorting
---

# Phase 19 Plan 02: Multi-Target Capability-Aware Graduation Execution

## Objective
- **What:** Execute multi-target capability-aware governance run and emit graduation matrix plus operator routing artifacts.
- **Why:** OPS-10 requires explicit, actionable per-target outcomes rather than aggregate parity statements.
- **Output:** Evidence package with report, matrix, and routing receipts for approved target set.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-multi-repo-enforcement-graduation/19-CONTEXT.md
@.planning/phases/19-multi-repo-enforcement-graduation/19-RESEARCH.md
@.planning/phases/19-multi-repo-enforcement-graduation/19-01-PLAN.md

## Tasks

<task type="code">
  <name>Execute deterministic multi-target governance graduation audit</name>
  <files>scripts/ci/audit_governance_drift.sh, var/evidence/governance-graduation-<UTCSTAMP>/report.json, var/evidence/governance-graduation-<UTCSTAMP>/report.txt</files>
  <action>Run governance drift audit for approved target set with deterministic output capture and fail-on drifted policy.</action>
  <verify>Report artifacts exist, parse correctly, and include all approved targets.</verify>
  <done>Graduation outcome source artifacts are captured.</done>
</task>

<task type="code">
  <name>Generate normalized graduation matrix receipt</name>
  <files>var/evidence/governance-graduation-<UTCSTAMP>/graduation-matrix.txt</files>
  <action>Transform report outputs into a stable, sorted matrix showing target id, raw outcome, final outcome, strict state, missing-context count, and exception usage.</action>
  <verify>Matrix row count equals approved target count and ordering is deterministic by target id.</verify>
  <done>Target-level outcomes are operator-readable and machine-checkable.</done>
</task>

<task type="code">
  <name>Emit deterministic routing action receipt</name>
  <files>var/evidence/governance-graduation-<UTCSTAMP>/routing-actions.txt</files>
  <action>Map each target outcome to explicit operator action (`retain`, `remediate`, `escalate_blocker`, `review_exception`) and capture in deterministic action log.</action>
  <verify>Every matrix row has exactly one routing action and no unknown action tokens.</verify>
  <done>Graduation output is operationally actionable.</done>
</task>

## Verification
- Confirm report/matrix/routing artifact counts match approved target inventory.
- Confirm deterministic sort order and field consistency across reruns.
- Confirm fail-on policy behavior remains aligned with expected outcome classes.

## Success Criteria
- [ ] Multi-target graduation report artifacts are complete and deterministic.
- [ ] Graduation matrix captures explicit raw/final outcomes per target.
- [ ] Routing action receipt provides one deterministic action per target.
