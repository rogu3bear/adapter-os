---
phase: 08-observability-and-runtime-evidence-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/adapteros-server-api/src/middleware_security.rs
  - crates/adapteros-server-api/tests/streaming_infer.rs
  - crates/adapteros-server-api/tests/drain_timeout_test.rs
  - .planning/phases/08-observability-and-runtime-evidence-closure/08-01-SUMMARY.md
autonomous: true
requirements:
  - OBS-06

must_haves:
    truths:
      - "New requests are rejected with `503` once drain is active"
      - "In-flight streaming requests that started before drain still complete successfully"
      - "Drain semantics are proven by the exact previously failing integration test"
    artifacts:
      - path: "crates/adapteros-server-api/tests/streaming_infer.rs"
        provides: "Drain integration contract for reject-new/allow-in-flight behavior"
        contains: "draining_rejects_new_requests_while_allowing_in_flight_stream_to_complete"
      - path: "crates/adapteros-server-api/src/middleware_security.rs"
        provides: "Drain middleware behavior returning 503 under drain state"
        contains: "StatusCode::SERVICE_UNAVAILABLE"
      - path: ".planning/phases/08-observability-and-runtime-evidence-closure/08-01-SUMMARY.md"
        provides: "OBS-06 execution evidence and residual risk notes"
        contains: "drain semantics"
    key_links:
      - from: "crates/adapteros-server-api/src/routes/mod.rs"
        to: "crates/adapteros-server-api/src/middleware_security.rs"
        via: "Route stack applies drain middleware for request rejection during shutdown"
        pattern: "drain_middleware"
      - from: "crates/adapteros-server-api/tests/streaming_infer.rs"
        to: "crates/adapteros-server-api/src/middleware_security.rs"
        via: "Integration test validates middleware semantics against boot drain state"
        pattern: "draining_rejects_new_requests_while_allowing_in_flight_stream_to_complete"
      - from: "crates/adapteros-server-api/tests/streaming_infer.rs"
        to: "crates/adapteros-server-api/src/middleware_security.rs"
        via: "In-flight request tracking and drain gate must preserve completion ordering"
        pattern: "request_tracking_middleware"
---

<objective>
Restore the failing drain semantic contract in streaming integration coverage: reject new requests with `503` once drain is active while letting already in-flight streams finish cleanly.

Purpose: closes `OBS-06` as a precise behavioral fix, not a broad shutdown refactor.
Output: green drain integration evidence and a minimal patch in existing middleware/test paths.
</objective>

<execution_context>
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/workflows/execute-plan.md
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-observability-and-runtime-hardening/05-01-SUMMARY.md
@.planning/phases/08-observability-and-runtime-evidence-closure/08-CONTEXT.md
@.planning/phases/08-observability-and-runtime-evidence-closure/08-RESEARCH.md
@crates/adapteros-server-api/src/middleware_security.rs
@crates/adapteros-server-api/tests/streaming_infer.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the known OBS-06 regression exactly</name>
  <files>
    crates/adapteros-server-api/tests/streaming_infer.rs
  </files>
  <action>
    Run only the previously failing drain test to establish a clean baseline before edits:

    ```bash
    CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer draining_rejects_new_requests_while_allowing_in_flight_stream_to_complete -- --exact --test-threads=1 --nocapture
    ```

    Capture pre-fix behavior in notes so the final summary can prove closure against the exact failure, not a proxy test.
  </action>
  <verify>
    <automated>CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer draining_rejects_new_requests_while_allowing_in_flight_stream_to_complete -- --exact --test-threads=1 --nocapture</automated>
    <manual>Confirm failure mode (or unexpected pass) is explicitly recorded before patching.</manual>
  </verify>
  <done>
    - Baseline result for the exact OBS-06 failing test is captured
    - No code changes made before baseline is known
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch drain gating with minimal behavior change</name>
  <files>
    crates/adapteros-server-api/src/middleware_security.rs
    crates/adapteros-server-api/tests/streaming_infer.rs
  </files>
  <action>
    Implement the smallest fix in the existing drain middleware path so active drain state rejects new requests with `503` while preserving the in-flight completion path already tracked by request middleware.

    Scope guardrails:
    - Keep existing middleware stack and error envelope shape.
    - Do not introduce alternate shutdown/drain paths.
    - Change only logic needed to satisfy the drain contract.
  </action>
  <verify>
    <automated>CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer draining_rejects_new_requests_while_allowing_in_flight_stream_to_complete -- --exact --test-threads=1 --nocapture</automated>
    <manual>Confirm second request is `503` while first in-flight request still returns `200`.</manual>
  </verify>
  <done>
    - Exact drain test passes
    - Regression is fixed without adding new middleware/routes
  </done>
</task>

<task type="auto">
  <name>Task 3: Guard adjacent drain invariants with targeted checks</name>
  <files>
    crates/adapteros-server-api/tests/drain_timeout_test.rs
    crates/adapteros-server-api/tests/streaming_infer.rs
  </files>
  <action>
    Run a small adjacent drain guard after the fix to reduce regression risk in timeout/drain lifecycle behavior:

    ```bash
    CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test drain_timeout_test -- --test-threads=1
    ```

    If this exposes collateral drift, apply only minimal corrections directly related to drain semantics.
  </action>
  <verify>
    <automated>CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test drain_timeout_test -- --test-threads=1</automated>
    <manual>Check that OBS-06 fix does not break timeout-based drain lifecycle expectations.</manual>
  </verify>
  <done>
    - Drain timeout integration tests remain green (or failures are explicitly documented and scoped)
    - OBS-06 fix is validated beyond a single assertion
  </done>
</task>

</tasks>

<verification>
1. `CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer draining_rejects_new_requests_while_allowing_in_flight_stream_to_complete -- --exact --test-threads=1 --nocapture`
2. `CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test drain_timeout_test -- --test-threads=1`
3. Summary records exact before/after result for the known Phase 05 regression test.
</verification>

<success_criteria>
- OBS-06 drain semantics are restored with exact integration proof.
- Patch stays inside existing middleware and test abstractions.
- No new shutdown/drain path is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-observability-and-runtime-evidence-closure/08-01-SUMMARY.md`
</output>
