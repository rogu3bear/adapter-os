---
phase: 08-observability-and-runtime-evidence-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - crates/adapteros-server-api/src/handlers/streaming_infer.rs
  - crates/adapteros-server-api/src/types/error.rs
  - crates/adapteros-server-api/src/session_tokens.rs
  - crates/adapteros-server-api/tests/streaming_infer.rs
  - .planning/phases/08-observability-and-runtime-evidence-closure/08-02-SUMMARY.md
autonomous: true
requirements:
  - OBS-07

must_haves:
    truths:
      - "`streaming_infer_emits_structured_error_on_unavailable_resource` passes with stable structured error envelope"
      - "`streaming_infer_resolves_effective_adapters_from_session_stack` passes and no longer fails with session/adapters validation drift"
      - "Unavailable-worker streaming paths consistently surface canonical `NO_COMPATIBLE_WORKER` behavior through stream error events"
    artifacts:
      - path: "crates/adapteros-server-api/src/types/error.rs"
        provides: "Canonical inference error code mapping for no-compatible-worker cases"
        contains: "NO_COMPATIBLE_WORKER"
      - path: "crates/adapteros-server-api/src/handlers/streaming_infer.rs"
        provides: "Structured stream error payload (`code`, `message`, `retryable`, `correlation_id`)"
        contains: "correlation_id"
      - path: "crates/adapteros-server-api/tests/streaming_infer.rs"
        provides: "Integration contract covering unavailable-resource and session-stack worker-unavailable flows"
        contains: "streaming_infer_emits_structured_error_on_unavailable_resource"
      - path: ".planning/phases/08-observability-and-runtime-evidence-closure/08-02-SUMMARY.md"
        provides: "OBS-07 closure evidence and residual risk notes"
        contains: "NO_COMPATIBLE_WORKER"
    key_links:
      - from: "crates/adapteros-server-api/src/types/error.rs"
        to: "crates/adapteros-server-api/src/handlers/streaming_infer.rs"
        via: "Stream handler maps `InferenceError` to serialized stream error event code"
        pattern: "error_code()"
      - from: "crates/adapteros-server-api/src/session_tokens.rs"
        to: "crates/adapteros-server-api/src/handlers/streaming_infer.rs"
        via: "Session token lock resolution feeds effective adapters into streaming request path"
        pattern: "resolve_session_token_lock"
      - from: "crates/adapteros-server-api/tests/streaming_infer.rs"
        to: "crates/adapteros-server-api/src/handlers/streaming_infer.rs"
        via: "Integration tests parse stream error event payload for code/message/retryable/correlation_id"
        pattern: "event: error"
---

<objective>
Close the unavailable-worker streaming regressions by making stream error envelopes stable and canonical, including session-stack adapter resolution flows.

Purpose: satisfies `OBS-07` by proving worker-unavailable paths are predictable and contract-safe.
Output: passing unavailable-worker streaming integration tests with explicit `NO_COMPATIBLE_WORKER` evidence.
</objective>

<execution_context>
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/workflows/execute-plan.md
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-observability-and-runtime-hardening/05-01-SUMMARY.md
@.planning/phases/08-observability-and-runtime-evidence-closure/08-CONTEXT.md
@.planning/phases/08-observability-and-runtime-evidence-closure/08-RESEARCH.md
@.planning/phases/08-observability-and-runtime-evidence-closure/08-01-SUMMARY.md
@crates/adapteros-server-api/src/handlers/streaming_infer.rs
@crates/adapteros-server-api/src/types/error.rs
@crates/adapteros-server-api/tests/streaming_infer.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce both unavailable-worker regressions exactly</name>
  <files>
    crates/adapteros-server-api/tests/streaming_infer.rs
  </files>
  <action>
    Run the two known failing tests independently to lock baseline behavior and error details:

    ```bash
    CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer streaming_infer_emits_structured_error_on_unavailable_resource -- --exact --test-threads=1 --nocapture
    CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer streaming_infer_resolves_effective_adapters_from_session_stack -- --exact --test-threads=1 --nocapture
    ```

    Record exact payload differences (code/message/retryable/correlation_id) before patching.
  </action>
  <verify>
    <automated>CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer streaming_infer_emits_structured_error_on_unavailable_resource -- --exact --test-threads=1 --nocapture && CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer streaming_infer_resolves_effective_adapters_from_session_stack -- --exact --test-threads=1 --nocapture</automated>
    <manual>Confirm baseline failure signatures are captured from the exact tests before code changes.</manual>
  </verify>
  <done>
    - Both OBS-07 baseline failures are reproduced or explicitly noted if already green
    - Current envelope mismatch is documented for summary traceability
  </done>
</task>

<task type="auto">
  <name>Task 2: Stabilize canonical unavailable-worker envelope in streaming path</name>
  <files>
    crates/adapteros-server-api/src/types/error.rs
    crates/adapteros-server-api/src/handlers/streaming_infer.rs
  </files>
  <action>
    Apply the smallest patch needed so streaming worker-unavailable paths consistently serialize canonical error semantics:
    - canonical error code behavior (`NO_COMPATIBLE_WORKER` where applicable)
    - structured stream payload fields (`code`, `message`, `retryable`, `correlation_id`)

    Keep this inside existing `InferenceError` mapping and streaming event serialization logic. Do not introduce alternate error transport paths.
  </action>
  <verify>
    <automated>CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer streaming_infer_emits_structured_error_on_unavailable_resource -- --exact --test-threads=1 --nocapture</automated>
    <manual>Inspect SSE error block and verify canonical code + stable structured fields.</manual>
  </verify>
  <done>
    - Structured unavailable-worker stream error payload is stable
    - Canonical code behavior is preserved through stream serialization
  </done>
</task>

<task type="auto">
  <name>Task 3: Preserve session-stack effective-adapter behavior under worker unavailability</name>
  <files>
    crates/adapteros-server-api/src/session_tokens.rs
    crates/adapteros-server-api/src/handlers/streaming_infer.rs
    crates/adapteros-server-api/tests/streaming_infer.rs
  </files>
  <action>
    Ensure session-stack resolution still drives effective adapter selection while unavailable-worker paths emit the expected stream error envelope instead of session/adapters validation failures.

    After targeted fix, run a small broader guard over the integration test file:

    ```bash
    CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer -- --test-threads=1
    ```
  </action>
  <verify>
    <automated>CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer streaming_infer_resolves_effective_adapters_from_session_stack -- --exact --test-threads=1 --nocapture && CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer -- --test-threads=1</automated>
    <manual>Confirm session-stack scenario now fails only with expected unavailable-worker envelope (when worker is absent) and not validation drift.</manual>
  </verify>
  <done>
    - Session-stack unavailable-worker test passes
    - Integration suite for `streaming_infer` stays stable after fix
  </done>
</task>

</tasks>

<verification>
1. `CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer streaming_infer_emits_structured_error_on_unavailable_resource -- --exact --test-threads=1 --nocapture`
2. `CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer streaming_infer_resolves_effective_adapters_from_session_stack -- --exact --test-threads=1 --nocapture`
3. `CARGO_TARGET_DIR=target-phase08 cargo test -p adapteros-server-api --test streaming_infer -- --test-threads=1`
4. Summary records exact pre-fix vs post-fix envelope behavior for both known failures.
</verification>

<success_criteria>
- OBS-07 unavailable-worker regressions are closed with canonical, stable stream error envelopes.
- Session-stack effective-adapter path remains intact under worker-unavailable conditions.
- Changes remain in existing error + streaming abstractions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-observability-and-runtime-evidence-closure/08-02-SUMMARY.md`
</output>
