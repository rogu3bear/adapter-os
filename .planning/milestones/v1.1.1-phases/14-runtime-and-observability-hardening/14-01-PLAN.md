---
phase: 14-runtime-and-observability-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/adapteros-config/src/effective.rs
  - crates/adapteros-lora-worker/src/lib.rs
  - crates/adapteros-lora-worker/src/deadlock.rs
  - etc/adapteros/cp.toml
  - .planning/phases/14-runtime-and-observability-hardening/14-01-SUMMARY.md
autonomous: true
requirements:
  - OBS-08
must_haves:
    truths:
      - "Deadlock detector settings are sourced from effective worker-safety config, not default-only wiring"
      - "Deadlock recovery remains fail-safe: artifact persisted and worker exits with explicit reason code"
      - "Supervisor restart is best-effort and cannot suppress fail-safe termination"
    artifacts:
      - path: "crates/adapteros-lora-worker/src/lib.rs"
        provides: "DeadlockConfig construction from effective config"
        contains: "DeadlockDetector::new"
      - path: "crates/adapteros-lora-worker/src/deadlock.rs"
        provides: "Deadlock recovery artifact + exit contract"
        contains: "deadlock_recovery_"
    key_links:
      - from: "etc/adapteros/cp.toml"
        to: "crates/adapteros-config/src/effective.rs"
        via: "worker.safety deadlock fields mapped into effective config"
        pattern: "deadlock_check_interval_secs"
      - from: "crates/adapteros-config/src/effective.rs"
        to: "crates/adapteros-lora-worker/src/lib.rs"
        via: "worker boot wiring consumes effective deadlock settings"
        pattern: "worker_safety"
      - from: "crates/adapteros-lora-worker/src/lib.rs"
        to: "crates/adapteros-lora-worker/src/deadlock.rs"
        via: "DeadlockConfig passed into detector rather than default-only path"
        pattern: "DeadlockDetector::new"
---

<objective>
Harden worker deadlock recovery into a config-driven fail-safe path with explicit evidence.

Purpose: close `OBS-08` by removing configuration drift and guaranteeing fail-safe behavior on deadlock detection.
Output: worker deadlock settings wired from config, fail-safe semantics preserved, and targeted verification evidence.
</objective>

<execution_context>
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/workflows/execute-plan.md
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-runtime-and-observability-hardening/14-CONTEXT.md
@.planning/phases/14-runtime-and-observability-hardening/14-RESEARCH.md
@etc/adapteros/cp.toml
@crates/adapteros-config/src/effective.rs
@crates/adapteros-lora-worker/src/lib.rs
@crates/adapteros-lora-worker/src/deadlock.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline deadlock config and fail-safe contract</name>
  <files>
    etc/adapteros/cp.toml
    crates/adapteros-config/src/effective.rs
    crates/adapteros-lora-worker/src/lib.rs
    crates/adapteros-lora-worker/src/deadlock.rs
  </files>
  <action>
    Establish current wiring and identify where `[worker.safety]` deadlock knobs are not propagated into `DeadlockConfig` at worker bootstrap.
  </action>
  <verify>
    <automated>rg -n "deadlock_check_interval_secs|max_wait_time_secs|max_lock_depth|recovery_timeout_secs|DeadlockConfig::default|DeadlockDetector::new" etc/adapteros/cp.toml crates/adapteros-config/src/effective.rs crates/adapteros-lora-worker/src/lib.rs crates/adapteros-lora-worker/src/deadlock.rs</automated>
    <manual>Record explicit config-to-runtime gaps before patching.</manual>
  </verify>
  <done>
    - Deadlock knob propagation gap is explicit
    - Fail-safe exit/artifact baseline behavior is captured
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire deadlock detector settings from effective config</name>
  <files>
    crates/adapteros-config/src/effective.rs
    crates/adapteros-lora-worker/src/lib.rs
    crates/adapteros-lora-worker/src/deadlock.rs
  </files>
  <action>
    Add/consume effective worker-safety deadlock fields so worker startup builds `DeadlockConfig` from config values. Keep existing fail-safe recovery semantics and exit reasons intact.
  </action>
  <verify>
    <automated>cargo test -p adapteros-lora-worker deadlock::tests::test_deadlock_detector_creation -- --exact --test-threads=1</automated>
    <manual>Confirm startup no longer relies exclusively on `DeadlockConfig::default()` when effective config is available.</manual>
  </verify>
  <done>
    - Deadlock detector initialization is config-driven
    - Existing fail-safe recovery path still terminates with explicit reason codes
  </done>
</task>

<task type="auto">
  <name>Task 3: Add targeted fail-safe regression tests</name>
  <files>
    crates/adapteros-lora-worker/src/deadlock.rs
    crates/adapteros-lora-worker/src/lib.rs
  </files>
  <action>
    Add narrow tests for deadlock fail-safe behavior and config wiring (no broad worker refactor), including assertions around artifact/exit decision branches that can be unit-tested without process-wide side effects.
  </action>
  <verify>
    <automated>cargo test -p adapteros-lora-worker deadlock::tests:: -- --test-threads=1</automated>
    <manual>Confirm tests cover both configured and fallback fail-safe branches.</manual>
  </verify>
  <done>
    - Deadlock fail-safe invariants are regression-protected
    - OBS-08 evidence is backed by targeted automated tests
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "deadlock_check_interval_secs|max_wait_time_secs|max_lock_depth|recovery_timeout_secs|DeadlockConfig::default|DeadlockDetector::new" ...`
- [ ] `cargo test -p adapteros-lora-worker deadlock::tests::test_deadlock_detector_creation -- --exact --test-threads=1`
- [ ] `cargo test -p adapteros-lora-worker deadlock::tests:: -- --test-threads=1`
</verification>

<success_criteria>
- Worker deadlock behavior is config-driven and fail-safe.
- Recovery artifacts + exit reasons are explicit and test-backed.
- No alternate deadlock handling path is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/14-runtime-and-observability-hardening/14-01-SUMMARY.md`
</output>
