---
phase: 14-runtime-and-observability-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/adapteros-server-api/src/state.rs
  - crates/adapteros-server-api/src/handlers/streaming.rs
  - crates/adapteros-server-api/src/handlers/streaming_infer.rs
  - crates/adapteros-server-api/src/handlers/admin.rs
  - .planning/phases/14-runtime-and-observability-hardening/14-02-SUMMARY.md
autonomous: true
requirements:
  - OBS-09
must_haves:
    truths:
      - "SSE breaker thresholds and recovery timeout are configurable from streaming config"
      - "Breaker transition telemetry (`open`, `half_open`, `recover`) is emitted with stable metadata"
      - "Target SSE endpoints use consistent breaker policy without parallel stream stacks"
    artifacts:
      - path: "crates/adapteros-server-api/src/handlers/streaming.rs"
        provides: "SSE breaker implementation with transition telemetry/metrics"
        contains: "streaming.circuit_breaker.transition"
      - path: "crates/adapteros-server-api/src/state.rs"
        provides: "Streaming breaker configuration fields"
        contains: "sse_circuit_failure_threshold"
    key_links:
      - from: "crates/adapteros-server-api/src/state.rs"
        to: "crates/adapteros-server-api/src/handlers/streaming.rs"
        via: "stream breaker settings resolved from runtime config"
        pattern: "stream_breaker_settings"
      - from: "crates/adapteros-server-api/src/handlers/streaming.rs"
        to: "crates/adapteros-server-api/src/handlers/admin.rs"
        via: "operator-visible config/telemetry contract remains aligned"
        pattern: "streaming_circuit_failure_threshold"
      - from: "crates/adapteros-server-api/src/handlers/streaming.rs"
        to: "crates/adapteros-server-api/src/handlers/streaming_infer.rs"
        via: "inference SSE hardening follows shared breaker semantics"
        pattern: "SSE stream circuit breaker"
---

<objective>
Harden configurable SSE breaker behavior and transition telemetry consistency across streaming surfaces.

Purpose: close `OBS-09` with config-driven breaker behavior and observable state transitions.
Output: consistent breaker settings usage, transition telemetry evidence, and targeted regression tests.
</objective>

<execution_context>
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/workflows/execute-plan.md
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-runtime-and-observability-hardening/14-CONTEXT.md
@.planning/phases/14-runtime-and-observability-hardening/14-RESEARCH.md
@crates/adapteros-server-api/src/state.rs
@crates/adapteros-server-api/src/handlers/streaming.rs
@crates/adapteros-server-api/src/handlers/streaming_infer.rs
@crates/adapteros-server-api/src/handlers/admin.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline breaker config + transition telemetry wiring</name>
  <files>
    crates/adapteros-server-api/src/state.rs
    crates/adapteros-server-api/src/handlers/streaming.rs
    crates/adapteros-server-api/src/handlers/admin.rs
  </files>
  <action>
    Map exactly how breaker threshold/recovery config is read and where transition metrics/telemetry are emitted so hardening changes remain contract-safe.
  </action>
  <verify>
    <automated>rg -n "sse_circuit_failure_threshold|sse_circuit_recovery_timeout_secs|stream_breaker_settings|streaming.circuit_breaker.transition" crates/adapteros-server-api/src/state.rs crates/adapteros-server-api/src/handlers/streaming.rs crates/adapteros-server-api/src/handlers/admin.rs</automated>
    <manual>Confirm operator-facing config exposure and transition signal names before edits.</manual>
  </verify>
  <done>
    - Breaker config source and transition sinks are explicit
    - No hidden/duplicate breaker settings path remains
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply shared breaker hardening to intended SSE paths</name>
  <files>
    crates/adapteros-server-api/src/handlers/streaming.rs
    crates/adapteros-server-api/src/handlers/streaming_infer.rs
    crates/adapteros-server-api/src/state.rs
  </files>
  <action>
    Ensure intended SSE paths use the same configurable breaker semantics and transition telemetry behavior, without introducing a parallel streaming subsystem.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api stream_circuit_breaker_opens_after_threshold -- --exact --test-threads=1</automated>
    <manual>Confirm `open`, `half_open`, and `recover` transitions are observable and threshold/timeouts come from config.</manual>
  </verify>
  <done>
    - Intended SSE paths honor shared breaker policy
    - Transition telemetry remains deterministic and explicit
  </done>
</task>

<task type="auto">
  <name>Task 3: Add targeted regression tests for breaker transitions</name>
  <files>
    crates/adapteros-server-api/src/handlers/streaming.rs
    crates/adapteros-server-api/src/handlers/streaming_infer.rs
  </files>
  <action>
    Add narrow tests that validate breaker transition behavior and telemetry metadata under configurable thresholds/timeouts, focusing on failure and recovery boundaries.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api stream_circuit_breaker_ -- --test-threads=1</automated>
    <manual>Check tests assert transition-specific metadata (`transition`, `threshold`, `error_count`, `recovery_timeout_secs`).</manual>
  </verify>
  <done>
    - Breaker transition behavior is regression-protected
    - OBS-09 evidence is test-backed with targeted scope
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "sse_circuit_failure_threshold|sse_circuit_recovery_timeout_secs|stream_breaker_settings|streaming.circuit_breaker.transition" ...`
- [ ] `cargo test -p adapteros-server-api stream_circuit_breaker_opens_after_threshold -- --exact --test-threads=1`
- [ ] `cargo test -p adapteros-server-api stream_circuit_breaker_ -- --test-threads=1`
</verification>

<success_criteria>
- SSE breaker behavior is consistently configurable.
- Breaker transitions are operationally observable and test-covered.
- No duplicate or parallel stream breaker path is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/14-runtime-and-observability-hardening/14-02-SUMMARY.md`
</output>
