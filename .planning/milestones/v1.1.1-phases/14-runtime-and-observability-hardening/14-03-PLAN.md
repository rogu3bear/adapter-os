---
phase: 14-runtime-and-observability-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - crates/adapteros-config/src/effective.rs
  - crates/adapteros-config/src/schema.rs
  - crates/adapteros-lora-worker/src/model_server_client.rs
  - crates/adapteros-lora-worker/src/backend_factory.rs
  - crates/adapteros-server/src/boot/model_server.rs
  - crates/adapteros-server-api/src/runtime_mode.rs
  - docs/CONFIGURATION.md
  - .planning/phases/14-runtime-and-observability-hardening/14-03-SUMMARY.md
autonomous: true
requirements:
  - SEC-06
must_haves:
    truths:
      - "Model-server transport contract is aligned to UDS-first semantics for hardened runtime mode"
      - "Production zero-egress constraints reject incompatible model-server endpoint configuration"
      - "Model-server client no longer silently falls back to localhost TCP for socket-path configuration"
    artifacts:
      - path: "crates/adapteros-lora-worker/src/model_server_client.rs"
        provides: "Model-server client transport configuration logic"
        contains: "from_socket_path"
      - path: "crates/adapteros-server-api/src/runtime_mode.rs"
        provides: "Runtime-mode validation for production security constraints"
        contains: "Production mode requires"
    key_links:
      - from: "crates/adapteros-config/src/effective.rs"
        to: "crates/adapteros-lora-worker/src/backend_factory.rs"
        via: "model-server endpoint config consumed by worker backend bootstrap"
        pattern: "model_server"
      - from: "crates/adapteros-lora-worker/src/backend_factory.rs"
        to: "crates/adapteros-lora-worker/src/model_server_client.rs"
        via: "configured endpoint flows into model-server client transport"
        pattern: "ModelServerClientConfig::with_addr"
      - from: "crates/adapteros-server-api/src/runtime_mode.rs"
        to: "docs/CONFIGURATION.md"
        via: "production UDS/egress policy reflected in operator docs"
        pattern: "UDS"
---

<objective>
Align model-server connectivity with UDS-first transport and production zero-egress policy expectations.

Purpose: close `SEC-06` by removing transport-policy drift across config, runtime validation, and client behavior.
Output: coherent model-server endpoint contract, policy-consistent runtime validation, and targeted verification evidence.
</objective>

<execution_context>
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/workflows/execute-plan.md
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-runtime-and-observability-hardening/14-CONTEXT.md
@.planning/phases/14-runtime-and-observability-hardening/14-RESEARCH.md
@crates/adapteros-config/src/effective.rs
@crates/adapteros-config/src/schema.rs
@crates/adapteros-lora-worker/src/model_server_client.rs
@crates/adapteros-lora-worker/src/backend_factory.rs
@crates/adapteros-server/src/boot/model_server.rs
@crates/adapteros-server-api/src/runtime_mode.rs
@docs/CONFIGURATION.md
@crates/adapteros-model-server/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize model-server endpoint contract</name>
  <files>
    crates/adapteros-config/src/effective.rs
    crates/adapteros-config/src/schema.rs
    docs/CONFIGURATION.md
  </files>
  <action>
    Align config contract so model-server endpoint semantics are explicit for UDS-first hardened deployments while preserving backward-compatible migration behavior for existing `server_addr` usage.
  </action>
  <verify>
    <automated>rg -n "model_server.server_addr|AOS_MODEL_SERVER_ADDR|socket_path|model_server" crates/adapteros-config/src/effective.rs crates/adapteros-config/src/schema.rs docs/CONFIGURATION.md</automated>
    <manual>Confirm docs and schema describe one coherent endpoint contract with explicit production guidance.</manual>
  </verify>
  <done>
    - Endpoint contract is explicit and internally consistent
    - Migration behavior is documented and bounded
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement policy-consistent model-server client transport behavior</name>
  <files>
    crates/adapteros-lora-worker/src/model_server_client.rs
    crates/adapteros-lora-worker/src/backend_factory.rs
    crates/adapteros-server/src/boot/model_server.rs
  </files>
  <action>
    Remove silent localhost-TCP fallback for socket-path configuration and make client transport behavior align with UDS-first expectations in hardened mode.
  </action>
  <verify>
    <automated>cargo test -p adapteros-lora-worker model_server_client::tests:: -- --test-threads=1</automated>
    <manual>Confirm socket-path config no longer degrades silently to TCP localhost.</manual>
  </verify>
  <done>
    - Client transport behavior matches configured endpoint semantics
    - Hardened mode avoids implicit network transport fallback
  </done>
</task>

<task type="auto">
  <name>Task 3: Enforce production zero-egress transport validation</name>
  <files>
    crates/adapteros-server-api/src/runtime_mode.rs
    crates/adapteros-server/src/boot/model_server.rs
    docs/CONFIGURATION.md
  </files>
  <action>
    Tighten production-mode validation so model-server endpoint configuration cannot violate UDS/zero-egress constraints, and ensure errors are explicit for operators.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api runtime_mode::tests::test_mode_properties -- --exact --test-threads=1</automated>
    <manual>Verify production-mode validation errors clearly identify required transport/policy settings.</manual>
  </verify>
  <done>
    - Production mode rejects policy-incompatible model-server endpoints
    - SEC-06 closure evidence is explicit and reproducible
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "model_server.server_addr|AOS_MODEL_SERVER_ADDR|socket_path|model_server" crates/adapteros-config/src/effective.rs crates/adapteros-config/src/schema.rs docs/CONFIGURATION.md`
- [ ] `cargo test -p adapteros-lora-worker model_server_client::tests:: -- --test-threads=1`
- [ ] `cargo test -p adapteros-server-api runtime_mode::tests::test_mode_properties -- --exact --test-threads=1`
</verification>

<success_criteria>
- Model-server endpoint contract is aligned across config, runtime, and client layers.
- Production zero-egress constraints are technically enforced, not only documented.
- No silent TCP fallback remains in hardened UDS path selection.
</success_criteria>

<output>
After completion, create `.planning/phases/14-runtime-and-observability-hardening/14-03-SUMMARY.md`
</output>
