---
phase: 02-ffi-safety-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/adapteros-lora-mlx-ffi/src/
autonomous: true
requirements:
  - FFI-01
  - FFI-02

must_haves:
  truths:
    - "Every unsafe block in non-test FFI runtime source has a concrete call-site `SAFETY:` rationale"
    - "Zero `unwrap`/`expect` calls remain in non-test FFI runtime source"
    - "FFI runtime error paths propagate typed `Result` errors instead of panicking"
    - "`cargo check -p adapteros-lora-mlx-ffi` passes after hardening"
  artifacts:
    - path: "crates/adapteros-lora-mlx-ffi/src/"
      provides: "Unsafe call sites with complete safety invariants"
      contains: "SAFETY:"
    - path: "crates/adapteros-lora-mlx-ffi/src/"
      provides: "No non-test panic edges at FFI runtime boundary"
      contains: "Result"
  key_links:
    - from: "crates/adapteros-lora-mlx-ffi/src/"
      to: ".planning/REQUIREMENTS.md"
      via: "Source-level hardening fulfills FFI-01 and FFI-02"
      pattern: "FFI-01|FFI-02"
---

<objective>
Complete source-level FFI runtime hardening by auditing every unsafe call site for concrete `SAFETY` invariants and removing non-test `unwrap`/`expect` panic edges in favor of typed `Result` propagation.

Purpose: This is the correctness core of Phase 2. ASAN/stress checks in the next plan are meaningful only after runtime boundary invariants and error handling are explicit.
Output: Hardened `adapteros-lora-mlx-ffi` runtime source with SAFETY coverage and panic-edge elimination.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ffi-safety-hardening/02-CONTEXT.md
@.planning/phases/02-ffi-safety-hardening/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and complete unsafe SAFETY rationale coverage</name>
  <files>
    crates/adapteros-lora-mlx-ffi/src/
  </files>
  <action>
    Build an unsafe inventory of non-test runtime FFI source files and add/fix call-site `SAFETY:` comments where missing or generic.

    **Step 1 — Inventory unsafe usage:**
    ```bash
    rg -n "unsafe" crates/adapteros-lora-mlx-ffi/src
    rg -n "SAFETY:" crates/adapteros-lora-mlx-ffi/src
    ```

    **Step 2 — Close coverage gaps:**
    For each unsafe block/call lacking a concrete rationale, document:
    - pointer validity + nullability assumptions
    - ownership/lifetime assumptions
    - aliasing/mutability assumptions
    - thread-safety/serialization assumptions

    **Step 3 — Keep comments local and specific:**
    Do not add boilerplate global notes. Each comment must prove why this specific call is safe.
  </action>
  <verify>
    <automated>rg -n "unsafe" crates/adapteros-lora-mlx-ffi/src && rg -n "SAFETY:" crates/adapteros-lora-mlx-ffi/src</automated>
    <manual>Spot-check representative high-risk files (`lora_ffi.rs`, `tensor.rs`, `session_cache.rs`, `training.rs`) for concrete invariants</manual>
  </verify>
  <done>
    - Every non-test runtime unsafe site has a concrete `SAFETY:` rationale
    - No generic copy-paste safety comments remain in runtime FFI source
    - Safety rationale is tied to call-site invariants
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove non-test unwrap/expect panic edges in runtime FFI code</name>
  <files>
    crates/adapteros-lora-mlx-ffi/src/
  </files>
  <action>
    Replace remaining non-test `unwrap`/`expect` in runtime FFI source with typed `Result` propagation.

    **Step 1 — Inventory non-test panic edges:**
    ```bash
    rg -n "\\b(unwrap|expect)\\(" crates/adapteros-lora-mlx-ffi/src -S
    ```

    **Step 2 — Convert call paths:**
    - Replace panic-based conversion/parsing/allocation paths with fallible handling.
    - Map failures to existing typed error boundaries (`ffi_error.rs` and related error types).
    - Preserve existing behavior for test-only modules and test code.

    **Step 3 — Verify compile integrity:**
    ```bash
    cargo check -p adapteros-lora-mlx-ffi
    ```

    **Duplication guard:**
    Reuse existing error enums/helpers; do not introduce a parallel error stack unless blocked.
  </action>
  <verify>
    <automated>rg -n "\\b(unwrap|expect)\\(" crates/adapteros-lora-mlx-ffi/src -S && cargo check -p adapteros-lora-mlx-ffi</automated>
    <manual>Confirm remaining `unwrap`/`expect` matches are test-only or build-script-only, not runtime FFI source</manual>
  </verify>
  <done>
    - Runtime FFI source has zero non-test `unwrap`/`expect`
    - All touched call paths propagate typed `Result` errors
    - `cargo check -p adapteros-lora-mlx-ffi` passes
  </done>
</task>

</tasks>

<verification>
1. `rg -n "unsafe" crates/adapteros-lora-mlx-ffi/src` — unsafe sites inventoried
2. `rg -n "SAFETY:" crates/adapteros-lora-mlx-ffi/src` — safety comments present at call sites
3. `rg -n "\b(unwrap|expect)\(" crates/adapteros-lora-mlx-ffi/src -S` — zero non-test runtime panic edges
4. `cargo check -p adapteros-lora-mlx-ffi` — crate compiles
</verification>

<success_criteria>
- FFI-01 and FFI-02 are satisfied in runtime source
- Unsafe sites are explicitly justified with concrete invariants
- Panic-based runtime FFI failure paths are eliminated
- FFI crate compiles cleanly after hardening
</success_criteria>

<output>
After completion, create `.planning/phases/02-ffi-safety-hardening/02-01-SUMMARY.md`
</output>
