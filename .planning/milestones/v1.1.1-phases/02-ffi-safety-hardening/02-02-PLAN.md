---
phase: 02-ffi-safety-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .github/workflows/ci.yml
  - crates/adapteros-lora-mlx-ffi/tests/
autonomous: false
requirements:
  - FFI-03
  - FFI-04

must_haves:
  truths:
    - "AddressSanitizer (ASAN) runs on push in CI for an FFI test lane"
    - "ASAN lane fails on sanitizer findings"
    - "Concurrent adapter hot-swap stress test overlaps inference load with adapter load/unload churn"
    - "Targeted FFI stress path passes without memory safety violations"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Push-triggered ASAN coverage for FFI crate test path"
      contains: "-Zsanitizer=address"
    - path: "crates/adapteros-lora-mlx-ffi/tests/"
      provides: "Concurrent hot-swap under inference load validation"
      contains: "load_adapter_runtime|unload_adapter_runtime"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".planning/REQUIREMENTS.md"
      via: "CI sanitizer enforcement fulfills FFI-03"
      pattern: "FFI-03"
    - from: "crates/adapteros-lora-mlx-ffi/tests/"
      to: ".planning/REQUIREMENTS.md"
      via: "Concurrent stress validation fulfills FFI-04"
      pattern: "FFI-04"
---

<objective>
Add enforceable CI sanitizer coverage for the FFI boundary and prove concurrent hot-swap resilience under inference load.

Purpose: After source hardening (`02-01`), this plan delivers runtime evidence: sanitizer detection in CI and stress-path validation for adapter churn under load.
Output: ASAN-on-push CI lane plus verified concurrent hot-swap stress coverage tied to FFI-03/FFI-04.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ffi-safety-hardening/02-CONTEXT.md
@.planning/phases/02-ffi-safety-hardening/02-RESEARCH.md
@.planning/phases/02-ffi-safety-hardening/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ASAN into existing CI workflow for FFI test path</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <action>
    Add a CI lane that runs on push and executes an FFI crate test target under AddressSanitizer.

    **Step 1 — Extend existing workflow (no parallel workflow creation):**
    Add ASAN execution in `ci.yml` using nightly toolchain + sanitizer flags for a focused FFI target.

    **Step 2 — Keep lane runtime bounded:**
    Prefer a focused FFI test target (for example dedicated memory-safe or integration subset) over full workspace tests.

    **Step 3 — Ensure failures are blocking:**
    Sanitizer findings must fail the lane and surface logs in CI output.

    **Target command pattern:**
    ```bash
    RUSTFLAGS="-Zsanitizer=address" cargo +nightly test -p adapteros-lora-mlx-ffi --tests -- --nocapture
    ```

    Reuse existing CI conventions already used for nightly/Miri jobs.
  </action>
  <verify>
    <automated>rg -n "sanitizer=address|adapteros-lora-mlx-ffi" .github/workflows/ci.yml</automated>
    <manual>Confirm ASAN lane is push-triggered and marked required in CI policy/gates</manual>
  </verify>
  <done>
    - `ci.yml` contains ASAN lane targeting FFI tests
    - ASAN lane runs on push
    - Sanitizer errors fail the lane
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate concurrent adapter hot-swap stress under inference load</name>
  <files>
    crates/adapteros-lora-mlx-ffi/tests/
  </files>
  <action>
    Ensure at least one stress/integration test exercises concurrent adapter load/unload while inference work is active.

    **Step 1 — Search existing near-match tests first:**
    Identify existing tests in `e2e_workflow_tests.rs`, `resilience_tests.rs`, and related integration files that already perform adapter churn.

    **Step 2 — Extend instead of duplicate:**
    If a near-match exists, extend it to enforce true overlap between inference execution and adapter hot-swap operations. Only add a new focused test if no suitable owner exists.

    **Step 3 — Verify under sanitizer-aware path:**
    Run targeted local command(s) that validate the stress case deterministically enough for CI adoption.

    **Suggested targeted check:**
    ```bash
    cargo test -p adapteros-lora-mlx-ffi e2e_workflow_tests -- --nocapture
    ```

    If a dedicated test name is created/extended, run just that test selector.
  </action>
  <verify>
    <automated>cargo test -p adapteros-lora-mlx-ffi -- --nocapture</automated>
    <manual>Confirm test behavior includes overlapping inference + load/unload and completes without corruption/UB symptoms</manual>
  </verify>
  <done>
    - Concurrent hot-swap-under-load coverage exists in FFI tests
    - The targeted stress scenario passes locally
    - The scenario is wired (or documented for wiring) into CI evidence for phase completion
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm CI lane cost and merge gate policy</name>
  <files></files>
  <action>
    Present ASAN runtime impact and stress-test stability results for human confirmation before final gate tightening.

    **What was built:**
    - Push-triggered ASAN lane for FFI tests
    - Concurrent hot-swap-under-load stress validation path

    **How to verify:**
    1. Review CI runtime delta for the added ASAN lane.
    2. Confirm lane should be required for merge (or staged as non-blocking for one iteration).
    3. Confirm stress-test flake rate is acceptable for CI.

    **Resume signal:** Type "approved" to finalize strict gating, or provide the requested gating adjustment.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification for ASAN gate strictness"</automated>
    <manual>User approves final ASAN gate strictness and stress-test policy</manual>
  </verify>
  <done>
    - User-approved CI gating policy for ASAN lane
    - User-approved stress-test integration policy
  </done>
</task>

</tasks>

<verification>
1. `rg -n "sanitizer=address" .github/workflows/ci.yml` — ASAN config present
2. CI run on push executes ASAN lane for FFI target
3. `cargo test -p adapteros-lora-mlx-ffi ...` targeted stress check passes locally
4. FFI-03 and FFI-04 are traceable to CI/test artifacts
</verification>

<success_criteria>
- FFI-03 satisfied with enforceable ASAN CI coverage
- FFI-04 satisfied with concurrent hot-swap-under-load stress evidence
- CI/test changes reuse existing structures and avoid duplicate harnesses
</success_criteria>

<output>
After completion, create `.planning/phases/02-ffi-safety-hardening/02-02-SUMMARY.md`
</output>
