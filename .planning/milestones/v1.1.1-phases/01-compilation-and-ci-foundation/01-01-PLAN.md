---
phase: 01-compilation-and-ci-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/benchmark/benches/kernel_performance.rs
  - crates/adapteros-telemetry/src/diagnostics/writer.rs
autonomous: true
requirements:
  - COMP-01
  - COMP-02

must_haves:
  truths:
    - "`cargo check --workspace` completes with zero errors"
    - "`cargo clippy --workspace` completes with zero errors (existing warnings acceptable)"
    - "`cargo test --workspace --no-run` compiles all test and bench targets without errors"
    - "All deleted `query/` and `util/` module consumers compile against replacement APIs"
  artifacts:
    - path: "tests/benchmark/benches/kernel_performance.rs"
      provides: "Fixed IoBuffers struct initializers with session_id field"
      contains: "session_id"
    - path: "crates/adapteros-telemetry/src/diagnostics/writer.rs"
      provides: "Fixed io_other_error clippy lint"
      contains: "Error::other"
  key_links:
    - from: "tests/benchmark/benches/kernel_performance.rs"
      to: "crates/adapteros-lora-kernel-api/src/lib.rs"
      via: "IoBuffers struct definition"
      pattern: "session_id.*None"
---

<objective>
Fix the two remaining compilation errors blocking a fully green workspace: the missing `session_id` field in benchmark IoBuffers initializers and the clippy `io_other_error` lint in the telemetry writer. Then commit all ~95 working tree changes (the completed DB refactor) as a clean baseline.

Purpose: Establishes the clean compilation baseline that all subsequent plans depend on. The working tree already compiles (`cargo check --workspace` passes), but bench targets and clippy have errors that must be fixed before CI can pass.
Output: A committed, cleanly-compiling workspace with all DB refactor changes landed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compilation-and-ci-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix compilation errors in benchmark and telemetry crates</name>
  <files>
    tests/benchmark/benches/kernel_performance.rs
    crates/adapteros-telemetry/src/diagnostics/writer.rs
  </files>
  <action>
    **Fix 1 — Benchmark IoBuffers missing `session_id` field:**

    In `tests/benchmark/benches/kernel_performance.rs`, there are two `IoBuffers { ... }` struct literals (around lines 33 and 49) that are missing the `session_id` field. Add `session_id: None,` to both struct initializers, after the `activations: None,` line.

    The IoBuffers struct is defined in `crates/adapteros-lora-kernel-api/src/lib.rs` and now includes a `session_id: Option<String>` field. Every other IoBuffers construction site in the codebase already includes this field.

    **Fix 2 — Clippy `io_other_error` lint:**

    In `crates/adapteros-telemetry/src/diagnostics/writer.rs` around line 373-377, replace:
    ```rust
    std::io::Error::new(
        std::io::ErrorKind::Other,
        format!("failed to serialize stale diagnostics batch: {}", e),
    )
    ```
    with:
    ```rust
    std::io::Error::other(
        format!("failed to serialize stale diagnostics batch: {}", e),
    )
    ```

    This uses the stabilized `Error::other()` constructor which is the idiomatic replacement for `Error::new(ErrorKind::Other, ...)`.
  </action>
  <verify>
    <automated>cargo check --workspace && cargo clippy --workspace -- -D warnings 2>&1 | head -50 && cargo test --workspace --no-run 2>&1 | tail -20</automated>
    <manual>Verify zero compilation errors and zero clippy errors across the full workspace including bench targets</manual>
  </verify>
  <done>
    - Both IoBuffers initializers in kernel_performance.rs include `session_id: None`
    - The telemetry writer uses `Error::other()` instead of `Error::new(ErrorKind::Other, ...)`
    - `cargo check --workspace` passes with zero errors
    - `cargo clippy --workspace` passes with zero errors
    - `cargo test --workspace --no-run` compiles all test/bench targets without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit the clean workspace baseline</name>
  <files>
    Cargo.lock
    Cargo.toml
    crates/adapteros-cli/src/commands/aos.rs
    crates/adapteros-cli/src/commands/serve.rs
    crates/adapteros-cli/src/commands/train_cli.rs
    crates/adapteros-core/src/recovery/orchestrator.rs
    crates/adapteros-crypto/src/providers/gcp.rs
    crates/adapteros-crypto/src/providers/keychain.rs
    crates/adapteros-db/src/adapters/mod.rs
    crates/adapteros-db/src/audit.rs
    crates/adapteros-db/src/git_repositories.rs
    crates/adapteros-db/src/inference_trace.rs
    crates/adapteros-db/src/kv_isolation_scan.rs
    crates/adapteros-db/src/lifecycle.rs
    crates/adapteros-db/src/plugin_configs.rs
    crates/adapteros-db/src/policy_audit.rs
    crates/adapteros-db/src/prefix_templates.rs
    crates/adapteros-db/src/query/builders.rs
    crates/adapteros-db/src/query/filters.rs
    crates/adapteros-db/src/query/mod.rs
    crates/adapteros-db/src/query/pagination.rs
    crates/adapteros-db/src/replay_kv.rs
    crates/adapteros-db/src/replay_metadata.rs
    crates/adapteros-db/src/routing_decision_chain.rs
    crates/adapteros-db/src/telemetry_kv.rs
    crates/adapteros-db/src/tenant_weight_encryption.rs
    crates/adapteros-db/src/training_datasets/types.rs
    crates/adapteros-db/src/training_jobs.rs
    crates/adapteros-db/src/user_tenant_access.rs
    crates/adapteros-db/src/util/mod.rs
    crates/adapteros-db/src/util/result_mappers.rs
    crates/adapteros-db/src/util/tenant_scoping.rs
    crates/adapteros-db/src/util/transaction.rs
    crates/adapteros-db/src/workers.rs
    crates/adapteros-db/tests/diagnostics_flow_tests.rs
    crates/adapteros-lora-lifecycle/src/loader.rs
    crates/adapteros-lora-mlx-ffi/benches/mlx_integration_benchmark.rs
    crates/adapteros-lora-mlx-ffi/src/backend.rs
    crates/adapteros-lora-mlx-ffi/src/safetensors_loader.rs
    crates/adapteros-lora-mlx-ffi/tests/real_mlx_integration.rs
    crates/adapteros-lora-mlx-ffi/tests/resilience_tests.rs
    crates/adapteros-lora-worker/src/adapter_operations.rs
    crates/adapteros-lora-worker/src/backpressure.rs
    crates/adapteros-lora-worker/src/bin/worker_modules/cli.rs
    crates/adapteros-lora-worker/src/bin/worker_modules/init.rs
    crates/adapteros-lora-worker/src/health.rs
    crates/adapteros-lora-worker/src/lib.rs
    crates/adapteros-lora-worker/src/limiter.rs
    crates/adapteros-lora-worker/src/training/builder.rs
    crates/adapteros-lora-worker/src/training/loader.rs
    crates/adapteros-lora-worker/src/training/packager/aos.rs
    crates/adapteros-lora-worker/src/training/packager/manifest.rs
    crates/adapteros-lora-worker/src/uds_server.rs
    crates/adapteros-orchestrator/Cargo.toml
    crates/adapteros-orchestrator/src/training/execution.rs
    crates/adapteros-orchestrator/src/training/packaging.rs
    crates/adapteros-orchestrator/src/training/service.rs
    crates/adapteros-orchestrator/src/training_dataset_integration.rs
    crates/adapteros-orchestrator/tests/codebase_ingestion_test.rs
    crates/adapteros-orchestrator/tests/training_lineage_conformance.rs
    crates/adapteros-orchestrator/tests/training_pipeline.rs
    crates/adapteros-secd/tests/security_hardening.rs
    crates/adapteros-server-api/src/handlers/adapters/duplicate.rs
    crates/adapteros-server-api/src/handlers/admin.rs
    crates/adapteros-server-api/src/inference_core/tests/adapter_tests.rs
    crates/adapteros-server-api/src/middleware/api_prefix_compat.rs
    crates/adapteros-server-api/src/services/training_dataset.rs
    crates/adapteros-server-api/src/state.rs
    crates/adapteros-server-api/tests/process_monitoring_anomalies_tests.rs
    crates/adapteros-tui/src/app.rs
    crates/adapteros-tui/src/app/api.rs
    crates/adapteros-tui/src/app/config_io.rs
    crates/adapteros-tui/src/app/service_control.rs
    crates/adapteros-tui/src/app/sse.rs
    crates/adapteros-tui/src/app/types.rs
    crates/adapteros-ui/Trunk.toml
    crates/adapteros-ui/crates/adapteros-server/src/main.rs
    scripts/build-ui.sh
    scripts/fresh-build.sh
    scripts/golden_path_adapter_chat.sh
    scripts/lib/http.sh
    scripts/reference.sh
    scripts/service-manager.sh
    scripts/smoke-reference.sh
    scripts/smoke_reference.sh
    scripts/start_minimal_training.sh
    tests/benchmark/examples/router_perf_test.rs
    tests/client_error_reporting_tests.rs
    tests/benchmark/benches/kernel_performance.rs
    crates/adapteros-telemetry/src/diagnostics/writer.rs
    .github/workflows/foundation-smoke.yml
    crates/adapteros-db/tests/foundation_receipt_invariants_tests.rs
    crates/adapteros-server-api/tests/adapteros_receipts_tenant_isolation_tests.rs
    crates/adapteros-server-api/tests/foundation_dataset_determinism_tests.rs
    docs/foundation-run.md
    scripts/foundation-run.sh
    scripts/foundation-smoke.sh
    scripts/functional-path-smoke.sh
  </files>
  <action>
    Commit all working tree changes (modified + deleted + untracked files) as a single atomic commit. This includes the completed DB refactor (~70 modified files), the two compilation fixes from Task 1, new foundation scripts and tests, and deleted files.

    Use git to stage all changes. The commit message should be:
    ```
    fix(workspace): land DB refactor, fix compilation errors, add foundation scripts
    ```

    This commit lands:
    - Completed DB refactor: direct sqlx queries replacing query builders, pool_result() migration
    - Deleted empty modules: query/{builders,filters,mod,pagination}.rs, util/{mod,result_mappers,tenant_scoping,transaction}.rs
    - Deleted obsolete scripts: smoke-reference.sh, smoke_reference.sh
    - Deleted obsolete UI server: crates/adapteros-ui/crates/adapteros-server/src/main.rs
    - Fixed benchmark IoBuffers missing session_id field
    - Fixed clippy io_other_error lint in telemetry writer
    - New foundation scripts: foundation-run.sh, foundation-smoke.sh, functional-path-smoke.sh
    - New foundation CI workflow: foundation-smoke.yml
    - New foundation tests: receipt invariants, tenant isolation, dataset determinism

    **IMPORTANT:** Do NOT commit `.planning/` directory contents. Only commit source code, scripts, tests, and CI files.
  </action>
  <verify>
    <automated>git log --oneline -1 && cargo check --workspace && cargo clippy --workspace -- -D warnings 2>&1 | head -20</automated>
    <manual>Verify the commit landed cleanly, the working tree is clean (except .planning/), and compilation still passes</manual>
  </verify>
  <done>
    - All ~95 working tree changes are committed in a single atomic commit
    - `git status` shows a clean working tree (except .planning/ untracked files)
    - `cargo check --workspace` still passes after commit
    - The commit message accurately describes the changes
  </done>
</task>

</tasks>

<verification>
1. `cargo check --workspace` — zero errors
2. `cargo clippy --workspace -- -D warnings` — zero errors
3. `cargo test --workspace --no-run` — all targets compile
4. `git status` — clean working tree (except .planning/)
5. `git log --oneline -1` — commit message present and accurate
</verification>

<success_criteria>
- The entire 85-crate workspace compiles cleanly
- All bench and test targets compile without errors
- Clippy passes without errors
- All working tree changes are committed as a clean baseline
- The deleted `query/` and `util/` modules and their consumers are fully reconciled
</success_criteria>

<output>
After completion, create `.planning/phases/01-compilation-and-ci-foundation/01-01-SUMMARY.md`
</output>
