---
phase: 05-observability-and-runtime-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/adapteros-server-api/src/handlers.rs
  - crates/adapteros-server-api/src/handlers/inference_metrics.rs
  - crates/adapteros-server-api/src/middleware/trace_context.rs
  - crates/adapteros-server-api/src/inference_core/core.rs
  - crates/adapteros-server/src/boot/metrics.rs
  - crates/adapteros-server/src/logging.rs
  - crates/adapteros-server/src/boot/server.rs
  - crates/adapteros-server/src/shutdown.rs
  - crates/adapteros-memory/src/allocator_stats.rs
  - crates/adapteros-memory/src/unified_tracker.rs
  - crates/adapteros-telemetry/src/metrics/critical_components.rs
  - crates/adapteros-server-api/tests/streaming_infer.rs
  - crates/adapteros-server-api/tests/observability_trace_propagation.rs
  - crates/adapteros-server-api/tests/metrics_contract.rs
autonomous: false
requirements:
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-04
  - OBS-05

must_haves:
  truths:
    - "`/metrics` exports TTFT and TPS histograms, adapter load summaries, and memory pressure gauges"
    - "One inference request produces a connected trace across control plane, worker, and kernel spans"
    - "SIGTERM during active streaming allows in-flight responses to complete within configured drain timeout"
    - "Process runs with mimalloc as global allocator and startup evidence confirms allocator selection"
    - "Memory budget enforcement tracks MLX unified memory and heap usage with observable threshold/backpressure signals"
  artifacts:
    - path: "crates/adapteros-server-api/src/handlers.rs"
      provides: "Prometheus metrics endpoint contract including required observability metrics"
      contains: "metrics_handler"
    - path: "crates/adapteros-telemetry/src/metrics/critical_components.rs"
      provides: "TTFT/TPS and memory-pressure metric definitions"
      contains: "Histogram"
    - path: "crates/adapteros-server-api/src/middleware/trace_context.rs"
      provides: "W3C trace propagation continuity across request lifecycle"
      contains: "traceparent"
    - path: "crates/adapteros-server/src/shutdown.rs"
      provides: "Graduated drain behavior under SIGTERM"
      contains: "Drain phase"
    - path: "crates/adapteros-server/src/logging.rs"
      provides: "Allocator and observability startup evidence"
      contains: "OpenTelemetry"
  key_links:
    - from: "crates/adapteros-server-api/src/routes/mod.rs"
      to: "crates/adapteros-server-api/src/handlers.rs"
      via: "Prometheus route surface"
      pattern: "route\(\"/metrics\""
    - from: "crates/adapteros-server/src/boot/server.rs"
      to: "crates/adapteros-server/src/shutdown.rs"
      via: "graceful shutdown + drain orchestration"
      pattern: "with_graceful_shutdown"
    - from: "crates/adapteros-server/src/otel.rs"
      to: "crates/adapteros-server-api/src/middleware/trace_context.rs"
      via: "trace continuity from collector integration to request propagation"
      pattern: "traceparent"
---

<objective>
Close all Phase 5 observability/runtime hardening requirements by extending existing metrics, tracing, shutdown, allocator, and memory-pressure systems without introducing parallel infrastructure.

Purpose: Make runtime behavior observable and operationally reliable under production stress paths.
Output: Verified observability contract (`OBS-01..OBS-05`) with targeted tests and deterministic shutdown evidence.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-observability-and-runtime-hardening/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete Prometheus metrics contract for runtime observability (OBS-01)</name>
  <files>
    crates/adapteros-server-api/src/handlers.rs
    crates/adapteros-server-api/src/handlers/inference_metrics.rs
    crates/adapteros-telemetry/src/metrics/critical_components.rs
    crates/adapteros-server/src/boot/metrics.rs
  </files>
  <action>
    Ensure `/metrics` and `/v1/metrics` export the required Phase 5 metrics contract:

    1. Add/confirm TTFT histogram metric (seconds-based canonical unit).
    2. Add/confirm TPS histogram metric (or explicit throughput histogram with stable naming).
    3. Ensure adapter/model load duration summaries are present and populated.
    4. Ensure memory pressure gauges are exported and updated continuously.

    Reuse existing metrics exporter and telemetry registration flow. Avoid creating new route families or sidecar exporters.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api --test metrics_contract -- --nocapture</automated>
    <manual>Scrape `/metrics` and confirm TTFT/TPS/adapter-load/memory-pressure signals are present with valid samples</manual>
  </verify>
  <done>
    - `/metrics` contains required observability metrics for OBS-01
    - TTFT and TPS are exported as histogram-compatible metrics
    - Adapter load and memory pressure metrics are visible and non-empty under activity
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove end-to-end trace continuity across control plane, worker, kernel (OBS-02)</name>
  <files>
    crates/adapteros-server/src/otel.rs
    crates/adapteros-server/src/logging.rs
    crates/adapteros-server-api/src/middleware/trace_context.rs
    crates/adapteros-server-api/src/inference_core/core.rs
    crates/adapteros-server-api/tests/observability_trace_propagation.rs
  </files>
  <action>
    Validate and patch trace propagation only where continuity breaks:

    1. Keep OTel initialization path and W3C trace middleware as single source.
    2. Ensure inference entry span and worker/kernel downstream spans share the same trace lineage.
    3. Add a focused integration test that sends one inference request and asserts one connected trace chain across required components.

    Do not add a parallel tracing mechanism.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api --test observability_trace_propagation -- --nocapture</automated>
    <manual>Inspect exported trace spans and confirm parent/child continuity through control plane -> worker -> kernel</manual>
  </verify>
  <done>
    - One request yields one connected distributed trace across all required boundaries
    - Trace headers/context remain standards-compliant (`traceparent`)
  </done>
</task>

<task type="auto">
  <name>Task 3: Validate graceful shutdown under active streaming SIGTERM (OBS-03)</name>
  <files>
    crates/adapteros-server/src/boot/server.rs
    crates/adapteros-server/src/shutdown.rs
    crates/adapteros-server-api/src/middleware_security.rs
    crates/adapteros-server-api/tests/streaming_infer.rs
  </files>
  <action>
    Add targeted verification for active-stream shutdown behavior:

    1. Start streaming inference.
    2. Send SIGTERM.
    3. Assert new requests are rejected during drain.
    4. Assert in-flight streaming requests complete (or terminate with the expected graceful semantics) before force timeout.

    Keep existing graduated drain logic and in-flight tracking model.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api --test streaming_infer -- --nocapture</automated>
    <manual>Run one scripted SIGTERM streaming scenario and record drain timeline with in-flight counts</manual>
  </verify>
  <done>
    - SIGTERM drains active streaming work within configured timeout window
    - No new work accepted once draining starts
    - Drain/force transitions remain observable in logs/metrics
  </done>
</task>

<task type="auto">
  <name>Task 4: Integrate mimalloc allocator and startup/runtime evidence (OBS-04)</name>
  <files>
    Cargo.toml
    crates/adapteros-server/src/main.rs
    crates/adapteros-server/src/logging.rs
    crates/adapteros-memory/src/allocator_stats.rs
  </files>
  <action>
    Implement process allocator hardening:

    1. Add mimalloc dependency and configure as global allocator for server process.
    2. Emit explicit startup evidence that allocator integration is active.
    3. Ensure allocator-level stats remain accessible for observability surfaces.

    Keep change narrowly scoped to allocator integration and verification outputs.
  </action>
  <verify>
    <automated>cargo check -p adapteros-server</automated>
    <manual>Confirm runtime startup output/metric indicates mimalloc is active</manual>
  </verify>
  <done>
    - Global allocator is mimalloc for runtime process
    - Operator-visible confirmation exists at startup and/or metrics surface
  </done>
</task>

<task type="auto">
  <name>Task 5: Enforce and expose memory budget thresholds with backpressure (OBS-05)</name>
  <files>
    crates/adapteros-memory/src/unified_tracker.rs
    crates/adapteros-memory/src/unified_memory.rs
    crates/adapteros-server/src/boot/metrics.rs
    crates/adapteros-telemetry/src/metrics/critical_components.rs
  </files>
  <action>
    Consolidate memory enforcement and observability:

    1. Ensure MLX unified memory and heap signals feed threshold evaluation.
    2. Enforce backpressure/alerts when threshold bands are crossed.
    3. Export threshold state and enforcement actions in metrics/logs.

    Reuse existing pressure-level and budget structures; do not create a new memory policy engine.
  </action>
  <verify>
    <automated>cargo test -p adapteros-memory</automated>
    <manual>Drive a controlled memory-pressure scenario and verify enforcement + metric emission</manual>
  </verify>
  <done>
    - Memory thresholds are enforced at runtime
    - Unified memory + heap signals are both represented in decisioning and telemetry
    - Backpressure/alerts are observable and testable
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Human acceptance of observability/runtime hardening evidence</name>
  <files></files>
  <action>
    Present consolidated evidence for `OBS-01..OBS-05`:
    1. Metrics scrape excerpt showing required metric families.
    2. Trace continuity evidence for one request.
    3. SIGTERM streaming drain evidence.
    4. mimalloc activation evidence.
    5. Memory threshold enforcement evidence.

    **Resume signal:** Type "approved" to close Phase 5 plan, or list required corrections.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification for OBS-01..OBS-05"</automated>
    <manual>User reviews required evidence and approves or requests fixes</manual>
  </verify>
  <done>
    - User confirms all five observability/runtime requirements are satisfied
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p adapteros-server-api --test metrics_contract -- --nocapture`
2. `cargo test -p adapteros-server-api --test observability_trace_propagation -- --nocapture`
3. `cargo test -p adapteros-server-api --test streaming_infer -- --nocapture`
4. `cargo check -p adapteros-server`
5. `cargo test -p adapteros-memory`
6. Manual SIGTERM-on-streaming run with drain timeline capture
7. Manual `/metrics` scrape and trace-chain inspection
</verification>

<success_criteria>
- Required Prometheus observability contract is exported and verifiable
- One inference request provides connected distributed trace across required components
- SIGTERM drain behavior is safe for active streaming workloads
- mimalloc is active and operator-verifiable
- Memory budget enforcement and pressure signaling are observable and effective
</success_criteria>

<output>
After completion, create `.planning/phases/05-observability-and-runtime-hardening/05-01-SUMMARY.md`
</output>
