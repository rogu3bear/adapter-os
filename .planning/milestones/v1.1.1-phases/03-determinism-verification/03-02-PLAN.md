---
phase: 03-determinism-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - crates/adapteros-lora-mlx-ffi/src/lib.rs
  - crates/adapteros-server/src/main.rs
  - crates/adapteros-server/src/boot/invariants.rs
  - crates/adapteros-server-api/src/boot_state.rs
  - .github/workflows/determinism.yml
  - .github/workflows/ci.yml
  - scripts/check_fast_math_flags.sh
  - tests/determinism_replay_harness.rs
  - crates/adapteros-server-api/tests/replay_determinism_tests.rs
autonomous: false
requirements:
  - DET-03
  - DET-04
  - DET-05

must_haves:
  truths:
    - "Boot fails fast when runtime MLX version differs from compiled-against version in determinism-enforcing mode"
    - "Failure output includes build/runtime version details and operator remediation context"
    - "Replay determinism suite passes 100% with canonical command set"
    - "Fast-math guard runs in CI and reports no forbidden flags"
  artifacts:
    - path: "crates/adapteros-lora-mlx-ffi/src/lib.rs"
      provides: "runtime/build MLX mismatch handling upgraded from warning to enforceable error"
      contains: "runtime version"
    - path: ".github/workflows/determinism.yml"
      provides: "determinism workflow gate includes fast-math scanner"
      contains: "check_fast_math_flags.sh"
  key_links:
    - from: "crates/adapteros-lora-mlx-ffi/src/lib.rs"
      to: "crates/adapteros-server/src/boot/invariants.rs"
      via: "runtime version mismatch propagates to boot-fatal invariant failure"
      pattern: "version mismatch|boot"
    - from: "scripts/check_fast_math_flags.sh"
      to: ".github/workflows/ci.yml"
      via: "CI deterministic build hygiene gate"
      pattern: "fast-math"
    - from: "tests/determinism_replay_harness.rs"
      to: "crates/adapteros-server-api/tests/replay_determinism_tests.rs"
      via: "phase replay determinism pass signal"
      pattern: "replay determinism"
---

<objective>
Complete runtime determinism enforcement and verification gates by making MLX version mismatch boot-fatal (`DET-03`), proving replay suite determinism (`DET-04`), and keeping CI fast-math policy green (`DET-05`).

Purpose: Converts remaining determinism gaps from warning-level risk to enforceable platform guarantees.
Output: Boot-fatal runtime version enforcement, replay suite pass evidence, and confirmed fast-math CI compliance.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-determinism-verification/03-CONTEXT.md
@.planning/phases/03-determinism-verification/03-RESEARCH.md
@.planning/phases/03-determinism-verification/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce MLX runtime/build version mismatch as boot-fatal (DET-03)</name>
  <files>
    crates/adapteros-lora-mlx-ffi/src/lib.rs
    crates/adapteros-server/src/main.rs
    crates/adapteros-server/src/boot/invariants.rs
    crates/adapteros-server-api/src/boot_state.rs
  </files>
  <action>
    Replace warning-only mismatch handling with an error path that participates in startup invariants.

    Implementation requirements:
    1. Expose an explicit version-validation result from MLX runtime init (match/mismatch/unknown handling).
    2. Convert mismatch to boot-fatal error in determinism-enforcing mode.
    3. Keep non-strict/development behavior aligned with existing config semantics if roadmap constraints permit graded strictness.
    4. Emit actionable failure detail: build version, runtime version, and remediation hint.

    Scope guard:
    - No broader boot refactor.
    - No fallback logic that masks true mismatches under strict determinism.
  </action>
  <verify>
    <automated>cargo test -p adapteros-lora-mlx-ffi --tests && cargo test -p adapteros-server --tests boot -- --test-threads=1</automated>
    <manual>Trigger a controlled mismatch scenario and confirm boot exits with clear deterministic-failure reason</manual>
  </verify>
  <done>
    - mismatch is boot-fatal in determinism-enforcing mode
    - diagnostics are operator-actionable
    - startup path remains unchanged when versions match
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate replay determinism gate and fast-math CI guard (DET-04, DET-05)</name>
  <files>
    tests/determinism_replay_harness.rs
    crates/adapteros-server-api/tests/replay_determinism_tests.rs
    scripts/check_fast_math_flags.sh
    .github/workflows/ci.yml
    .github/workflows/determinism.yml
  </files>
  <action>
    Establish a canonical Phase 3 verification command set and ensure CI parity:

    Replay gate:
    - Run replay harness and server API replay determinism tests with deterministic thread settings.
    - Resolve any nondeterministic flakes with minimal deterministic controls (seed/time/order), not broad retries.

    Fast-math gate:
    - Confirm scanner still blocks forbidden flags and is wired into both CI and determinism workflow.
    - Keep script as source of truth; only tune includes/excludes if proven necessary.
  </action>
  <verify>
    <automated>cargo test -p adapter-os --no-default-features --test determinism_replay_harness -- --test-threads=1 && cargo test -p adapteros-server-api --test replay_determinism_tests -- --test-threads=1 && bash scripts/check_fast_math_flags.sh</automated>
    <manual>Review CI workflow jobs to confirm fast-math guard is mandatory in determinism-related pipeline paths</manual>
  </verify>
  <done>
    - replay determinism suite passes and is reproducible
    - fast-math scanner passes locally and remains CI-wired
    - DET-04 and DET-05 acceptance evidence is captured
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Determinism enforcement approval checkpoint</name>
  <files></files>
  <action>
    Present the runtime version enforcement behavior and replay/fast-math verification evidence for approval.

    What to review:
    1. Boot-fatal mismatch behavior in determinism-enforcing mode.
    2. Replay suite pass output and determinism assertions.
    3. Fast-math guard status and CI workflow coverage.

    Resume signal: `approved` to finalize Phase 03 closure evidence.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification"</automated>
    <manual>Human approval of strict mismatch policy and verification evidence</manual>
  </verify>
  <done>
    - Human reviewer approves determinism enforcement behavior
    - Human reviewer approves replay + fast-math evidence
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p adapter-os --no-default-features --test determinism_replay_harness -- --test-threads=1`
2. `cargo test -p adapteros-server-api --test replay_determinism_tests -- --test-threads=1`
3. `bash scripts/check_fast_math_flags.sh`
4. Controlled MLX version mismatch startup validation (manual)
5. CI workflow check for fast-math gate presence
</verification>

<success_criteria>
- DET-03 satisfied with fail-fast runtime MLX version mismatch enforcement
- DET-04 satisfied with replay determinism suite 100% pass evidence
- DET-05 satisfied with mandatory fast-math guard and clean scan results
</success_criteria>

<output>
After completion, create `.planning/phases/03-determinism-verification/03-02-SUMMARY.md`
</output>
