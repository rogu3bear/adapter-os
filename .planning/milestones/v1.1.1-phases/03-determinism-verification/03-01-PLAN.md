---
phase: 03-determinism-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/adapteros-core/src/receipt_digest.rs
  - crates/adapteros-server-api/src/handlers/adapteros_receipts.rs
  - crates/adapteros-server-api/src/handlers/run_evidence.rs
  - crates/adapteros-server-api/src/inference_core/determinism.rs
  - docs/DETERMINISM.md
  - docs/runbooks/DETERMINISM_VIOLATION.md
  - tests/determinism/canonical_hashing.rs
  - tests/record_replay_receipt_harness.rs
autonomous: true
requirements:
  - DET-01
  - DET-02

must_haves:
  truths:
    - "Every receipt-hash input path has an explicit canonicalization rule (Q15/fixed representation/byte canonicalization)"
    - "No raw floating-point value enters receipt hash computation without deterministic conversion"
    - "Unquantized inference layers are enumerated with determinism status and mitigation strategy"
    - "Determinism envelope documentation is source-linked and auditable"
  artifacts:
    - path: "docs/DETERMINISM.md"
      provides: "Unquantized layer map and determinism envelope"
      contains: "determinism envelope"
    - path: "tests/determinism/canonical_hashing.rs"
      provides: "Receipt canonicalization coverage tests"
      contains: "canonical"
  key_links:
    - from: "crates/adapteros-core/src/receipt_digest.rs"
      to: "tests/determinism/canonical_hashing.rs"
      via: "receipt-hash input canonicalization invariants"
      pattern: "q15|canonical|digest"
    - from: "docs/DETERMINISM.md"
      to: "crates/adapteros-server-api/src/inference_core/determinism.rs"
      via: "documented unquantized layer behavior maps to runtime path"
      pattern: "unquantized|mitigation"
---

<objective>
Close determinism evidence gaps by enforcing and proving complete canonicalization coverage for receipt-hash inputs (`DET-01`) and producing a concrete unquantized-layer determinism envelope (`DET-02`).

Purpose: Determinism claims are only defensible when the hash input boundary is fully canonicalized and documented against real runtime code paths.
Output: Canonicalization guard/test coverage and a source-linked determinism envelope document update.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-determinism-verification/03-CONTEXT.md
@.planning/phases/03-determinism-verification/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and close receipt canonicalization coverage (DET-01)</name>
  <files>
    crates/adapteros-core/src/receipt_digest.rs
    crates/adapteros-server-api/src/handlers/adapteros_receipts.rs
    crates/adapteros-server-api/src/handlers/run_evidence.rs
    crates/adapteros-server-api/src/inference_core/determinism.rs
    tests/determinism/canonical_hashing.rs
    tests/record_replay_receipt_harness.rs
  </files>
  <action>
    Build an explicit inventory of all values that feed receipt hash computation paths and classify each as:
    - already canonicalized (Q15/fixed representation/canonical bytes)
    - canonicalized but untested
    - non-canonicalized (blocking)

    For every non-canonicalized or untested path:
    1. Add/extend canonicalization at the nearest receipt-input boundary using existing invariants/utilities.
    2. Add focused tests that fail if raw floating-point input reaches hash assembly without deterministic conversion.
    3. Keep field naming and conversion style aligned with existing receipt/replay structures; no new parallel digest formats.

    Scope guard:
    - Do not alter receipt schema semantics unless strictly required for deterministic representation.
    - Do not broaden beyond receipt hash input paths.
  </action>
  <verify>
    <automated>cargo test --test determinism_core_suite canonical_hashing -- --test-threads=1 && cargo test --test record_replay_receipt_harness -- --test-threads=1</automated>
    <manual>Confirm inventory shows no uncategorized/uncanonicalized receipt-hash inputs</manual>
  </verify>
  <done>
    - Canonicalization inventory exists and covers all receipt-hash input fields
    - Blocking non-canonicalized paths are removed
    - Focused canonicalization tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Publish unquantized-layer determinism envelope (DET-02)</name>
  <files>
    docs/DETERMINISM.md
    docs/runbooks/DETERMINISM_VIOLATION.md
  </files>
  <action>
    Extend determinism documentation with an explicit unquantized-layer matrix that includes:
    - layer/path owner
    - source file(s)
    - determinism risk profile
    - mitigation strategy
    - whether the path is in receipt-attested scope
    - verification signal (test or invariant)

    Ensure each documented entry links back to concrete source paths and existing tests/invariants.
    Do not add speculative mitigation text that is not represented in code.
  </action>
  <verify>
    <automated>rg -n "unquantized|determinism envelope|receipt-attested" docs/DETERMINISM.md docs/runbooks/DETERMINISM_VIOLATION.md</automated>
    <manual>Spot-check at least 3 matrix rows against actual code paths</manual>
  </verify>
  <done>
    - docs include complete unquantized-layer map and mitigation matrix
    - each row references concrete source paths
    - envelope is sufficient to satisfy DET-02 auditability requirements
  </done>
</task>

</tasks>

<verification>
1. `cargo test --test determinism_core_suite canonical_hashing -- --test-threads=1`
2. `cargo test --test record_replay_receipt_harness -- --test-threads=1`
3. `rg -n "unquantized|determinism envelope|receipt-attested" docs/DETERMINISM.md`
4. Manual cross-check: documented rows map to real source files
</verification>

<success_criteria>
- DET-01 satisfied with explicit and tested receipt-hash canonicalization coverage
- DET-02 satisfied with source-linked unquantized-layer determinism envelope
- No parallel hash/canonicalization pathways introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-determinism-verification/03-01-SUMMARY.md`
</output>
