---
phase: 10-operations-release-sign-off
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/release/mvp_control_room.sh
  - scripts/service-manager.sh
  - scripts/verify-deployment.sh
  - .planning/phases/10-operations-release-sign-off/10-01-SUMMARY.md
autonomous: true
requirements:
  - OPS-06
user_setup:
  - Provide a reachable rehearsal URL via BASE_URL (for example, http://localhost:8080 or a staging host).

must_haves:
    truths:
      - "Control-room rehearsal completes against a reachable server with no failed steps"
      - "Preflight doctor and readiness probes pass in the same rehearsal run"
      - "Evidence log and summary are generated under var/release-control-room/<timestamp>/"
    artifacts:
      - path: "var/release-control-room/20260224T103206Z/evidence.log"
        provides: "Step-by-step rehearsal evidence with pass/fail status"
        contains: "Preflight: aosctl doctor"
      - path: "var/release-control-room/20260224T103206Z/summary.txt"
        provides: "Run-level success summary"
        contains: "MVP control-room run completed successfully"
    key_links:
      - from: "scripts/release/mvp_control_room.sh"
        to: "var/release-control-room/<timestamp>/evidence.log"
        via: "run_step writes timestamped artifacts for each control-room step"
        pattern: "RESULT: PASS"
      - from: "scripts/release/mvp_control_room.sh"
        to: "./aosctl"
        via: "doctor preflight uses BASE_URL through AOS_SERVER_URL"
        pattern: "Preflight: aosctl doctor"
---

<objective>
Execute a successful control-room rehearsal against a reachable live server and capture complete evidence for OPS-06.

Purpose: remove the Phase 06 blocker where doctor failed due unreachable server and produce current rehearsal proof.
Output: a passing control-room evidence set and any minimal script-level fixes needed to eliminate false-negative failures.
</objective>

<execution_context>
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/workflows/execute-plan.md
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-operations-release-sign-off/10-CONTEXT.md
@.planning/phases/10-operations-release-sign-off/10-RESEARCH.md
@.planning/phases/06-production-operations/06-01-SUMMARY.md
@scripts/release/mvp_control_room.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate live server reachability and rehearsal inputs</name>
  <files>
    scripts/release/mvp_control_room.sh
  </files>
  <action>
    Require an explicit reachable `BASE_URL` before running control-room.

    1. Export and validate `BASE_URL`.
    2. Probe readiness endpoints with strict curl checks.
    3. Fail early if reachability is not proven.

    Keep checks script-driven and avoid ad-hoc manual assertions.
  </action>
  <verify>
    <automated>BASE_URL="${BASE_URL:?set BASE_URL}" && curl -fsS "$BASE_URL/healthz" && curl -fsS "$BASE_URL/readyz"</automated>
    <manual>Confirm BASE_URL points to the intended rehearsal target and not an accidental stale localhost process.</manual>
  </verify>
  <done>
    - BASE_URL is explicit and reachable
    - healthz and readyz probes pass prior to control-room execution
  </done>
</task>

<task type="auto">
  <name>Task 2: Run control-room rehearsal and capture evidence</name>
  <files>
    scripts/release/mvp_control_room.sh
    var/release-control-room/
  </files>
  <action>
    Run the canonical control-room command with rehearsal-safe deploy skip:

    `bash scripts/release/mvp_control_room.sh --skip-deploy --base-url "$BASE_URL"`

    Then capture the latest run directory and validate the evidence log has no failed steps and includes the key gates (`aosctl doctor`, `Healthz probe`, `Readyz probe`).
  </action>
  <verify>
    <automated>BASE_URL="${BASE_URL:?set BASE_URL}" && bash scripts/release/mvp_control_room.sh --skip-deploy --base-url "$BASE_URL" && RUN_DIR="$(ls -1dt var/release-control-room/* | head -n 1)" && test -f "$RUN_DIR/evidence.log" && test -f "$RUN_DIR/summary.txt" && ! rg -n "RESULT: FAIL" "$RUN_DIR/evidence.log" && rg -n "Preflight: aosctl doctor|Healthz probe|Readyz probe" "$RUN_DIR/evidence.log"</automated>
    <manual>Inspect the latest step logs in RUN_DIR to confirm doctor and readiness checks succeeded against the intended host.</manual>
  </verify>
  <done>
    - Rehearsal run completes successfully
    - Evidence log and summary are present and traceable
    - No FAIL step remains in latest evidence log
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply minimal blocker fixes only if rehearsal fails</name>
  <files>
    scripts/release/mvp_control_room.sh
    scripts/service-manager.sh
    scripts/verify-deployment.sh
  </files>
  <action>
    If Task 2 fails, patch only the smallest script-level defect causing false-negative operational failure (for example, argument propagation, strict-shell ordering, or health probe mismatch), then rerun Task 2 until a clean evidence log is produced.

    Do not add new release paths or bypass failing gates.
  </action>
  <verify>
    <automated>BASE_URL="${BASE_URL:?set BASE_URL}" && bash scripts/release/mvp_control_room.sh --skip-deploy --base-url "$BASE_URL"</automated>
    <manual>Confirm any script change directly addresses the observed failure mode and does not weaken gate strictness.</manual>
  </verify>
  <done>
    - Any applied fix is minimal and blocker-specific
    - Rehearsal passes end-to-end after fix
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [x] `BASE_URL="${BASE_URL:?set BASE_URL}" && curl -fsS "$BASE_URL/healthz" && curl -fsS "$BASE_URL/readyz"`
- [x] `BASE_URL="${BASE_URL:?set BASE_URL}" && bash scripts/release/mvp_control_room.sh --skip-deploy --base-url "$BASE_URL"`
- [x] `RUN_DIR="$(ls -1dt var/release-control-room/* | head -n 1)" && test -f "$RUN_DIR/evidence.log" && ! rg -n "RESULT: FAIL" "$RUN_DIR/evidence.log"`
</verification>

<success_criteria>
- OPS-06 evidence shows full control-room rehearsal success against a reachable server.
- The prior doctor connection-refused failure mode is eliminated in current evidence.
- Latest run artifacts are complete and reusable by downstream sign-off packaging.
</success_criteria>

<output>
After completion, create `.planning/phases/10-operations-release-sign-off/10-01-SUMMARY.md`
</output>
