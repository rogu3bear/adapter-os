---
phase: 07-ui-and-developer-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/adapteros-ui/
  - crates/adapteros-tui/
  - crates/adapteros-cli/
  - scripts/foundation-run.sh
  - scripts/foundation-smoke.sh
  - scripts/ci/check_ui_assets.sh
  - crates/adapteros-ui/Cargo.toml
  - Cargo.lock
  - .planning/phases/07-ui-and-developer-experience/
autonomous: false
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04

must_haves:
  truths:
    - "`cargo check -p adapteros-ui --target wasm32-unknown-unknown` passes"
    - "`scripts/foundation-run.sh` serves healthy backend and UI path smoke passes"
    - "TUI shows live latency/TPS/memory and adapter status against live API"
    - "CLI verification covers the 60+ command surface with formatting assertions"
    - "Leptos 0.8 decision is explicitly documented (upgrade now or defer)"
  artifacts:
    - path: ".planning/phases/07-ui-and-developer-experience/07-01-SUMMARY.md"
      provides: "Execution summary with UX-01..UX-04 evidence"
      contains: "Leptos 0.8 decision"
    - path: "crates/adapteros-ui/Cargo.toml"
      provides: "Leptos version evidence"
      contains: "leptos = { version ="
    - path: "crates/adapteros-tui/src/ui/dashboard.rs"
      provides: "Dashboard metrics and adapter rendering"
      contains: "TPS"
    - path: "crates/adapteros-cli/src/app.rs"
      provides: "CLI command surface definition"
      contains: "pub enum Commands"
  key_links:
    - from: "scripts/foundation-run.sh"
      to: "scripts/ci/check_ui_assets.sh"
      via: "foundation-run enforces UI asset validity before boot"
      pattern: "check_ui_assets"
    - from: "crates/adapteros-tui/src/app.rs"
      to: "crates/adapteros-tui/src/app/api.rs"
      via: "TUI update loop consumes metrics/adapters API fetches"
      pattern: "get_metrics|get_adapters"
    - from: "crates/adapteros-cli/tests/command_parsing_tests.rs"
      to: "crates/adapteros-cli/src/app.rs"
      via: "command parsing verifies clap command contract"
      pattern: "try_parse_from|pub enum Commands"
---

<objective>
Validate and harden all user-facing surfaces against the production-hardened backend: web UI serving path, TUI live operations view, and CLI command behavior/output. Deliver a clear Leptos 0.8 decision with evidence.

Purpose: close the final integration gap between backend readiness and operator/developer usability.
Output: verified UI/TUI/CLI compatibility plus documented Leptos framework decision.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-ui-and-developer-experience/07-CONTEXT.md
@.planning/phases/07-ui-and-developer-experience/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate and close web UI integration gaps (UX-01)</name>
  <files>
    crates/adapteros-ui/
    scripts/build-ui.sh
    scripts/ci/check_ui_assets.sh
    scripts/foundation-run.sh
    scripts/foundation-smoke.sh
  </files>
  <action>
    Run the canonical web UI acceptance flow:

    1. `cargo check -p adapteros-ui --target wasm32-unknown-unknown`
    2. `bash scripts/ci/check_ui_assets.sh` (rebuild via `bash scripts/build-ui.sh` only if needed)
    3. `bash scripts/foundation-run.sh` (or `--headless` when UI root smoke is intentionally skipped)

    If failures occur, make the smallest targeted fixes in existing UI build/serve paths. Do not introduce alternative serving mechanisms.
  </action>
  <verify>
    <automated>cargo check -p adapteros-ui --target wasm32-unknown-unknown && bash scripts/ci/check_ui_assets.sh && bash scripts/foundation-run.sh --headless</automated>
    <manual>Confirm backend boot, readiness endpoints, and UI asset validity are all green.</manual>
  </verify>
  <done>
    - WASM check passes
    - UI assets validate successfully
    - foundation-run completes smoke path against hardened backend
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify TUI live metrics and adapter status fidelity (UX-02)</name>
  <files>
    crates/adapteros-tui/src/app.rs
    crates/adapteros-tui/src/app/api.rs
    crates/adapteros-tui/src/ui/dashboard.rs
    crates/adapteros-tui/src/ui/status_bar.rs
  </files>
  <action>
    Verify TUI dataflow and rendering against the live API contract:

    1. Confirm polling/update behavior for health, metrics, adapters, and service status.
    2. Validate rendered fields for latency/TPS/memory/adapter-loaded state.
    3. Apply minimal fixes only where field mapping or rendering semantics diverge from API reality.
  </action>
  <verify>
    <automated>cargo check -p adapteros-tui && cargo test -p adapteros-tui --lib</automated>
    <manual>Run TUI against a live backend and confirm values update in near real time with expected semantics.</manual>
  </verify>
  <done>
    - Dashboard and status bar reflect live metrics and adapter state
    - No stale/default-only data path remains for healthy backend responses
  </done>
</task>

<task type="auto">
  <name>Task 3: Execute CLI command-surface verification matrix (UX-03)</name>
  <files>
    crates/adapteros-cli/src/app.rs
    crates/adapteros-cli/tests/
    scripts/
  </files>
  <action>
    Use layered verification for the full command surface:

    1. Parsing/help contract tests (`command_parsing_tests`, help golden/tests).
    2. Representative API-backed smoke checks for grouped command families.
    3. Output-format checks (human-readable and JSON mode where supported).

    Extend tests/scripts only when gaps are found; preserve existing command names and aliases.
  </action>
  <verify>
    <automated>cargo test -p adapteros-cli --test command_parsing_tests && cargo test -p adapteros-cli --test cli_help</automated>
    <manual>Run a documented matrix of representative commands against a hardened backend endpoint and validate output formatting.</manual>
  </verify>
  <done>
    - Command-surface verification evidence covers 60+ commands (including grouped namespaces)
    - Formatting behavior is validated for key command families
  </done>
</task>

<task type="auto">
  <name>Task 4: Evaluate Leptos 0.8 and record decision (UX-04)</name>
  <files>
    crates/adapteros-ui/Cargo.toml
    Cargo.lock
    .planning/phases/07-ui-and-developer-experience/07-01-SUMMARY.md
  </files>
  <action>
    Perform evidence-based evaluation of Leptos 0.8:

    1. Assess migration complexity from current Leptos/Router 0.7 usage.
    2. Run targeted compile/test checks for chosen path.
    3. Document explicit decision: upgrade now or defer.

    If deferred, include blockers and conditions for safe re-entry.
  </action>
  <verify>
    <automated>cargo check -p adapteros-ui --target wasm32-unknown-unknown</automated>
    <manual>Confirm decision rationale is grounded in concrete compile/runtime evidence, not assumptions.</manual>
  </verify>
  <done>
    - Leptos 0.8 decision documented with clear rationale
    - If upgraded, build remains green; if deferred, blockers and next trigger are explicit
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Review and approve Phase 7 acceptance evidence</name>
  <files></files>
  <action>
    Present UX-01..UX-04 evidence summary and residual risk list for review.

    **Resume signal:** Type "approved" to close Phase 7 plan execution, or provide requested deltas.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification for Phase 7 acceptance"</automated>
    <manual>User validates evidence quality and approves closure.</manual>
  </verify>
  <done>
    - Human approval recorded
    - Phase 7 can be marked complete in roadmap tracking
  </done>
</task>

</tasks>

<verification>
1. `cargo check -p adapteros-ui --target wasm32-unknown-unknown`
2. `bash scripts/ci/check_ui_assets.sh`
3. `bash scripts/foundation-run.sh` (headless/non-headless as needed)
4. `cargo check -p adapteros-tui && cargo test -p adapteros-tui --lib`
5. `cargo test -p adapteros-cli --test command_parsing_tests`
6. `cargo test -p adapteros-cli --test cli_help`
7. Phase summary includes explicit Leptos 0.8 decision evidence
</verification>

<success_criteria>
- Web UI is verifiably compiled and served through canonical foundation flow
- TUI shows live metrics and adapter status accurately against hardened API
- CLI command surface is verified with output-format confidence
- Leptos 0.8 decision is documented and defensible
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-and-developer-experience/07-01-SUMMARY.md`
</output>
