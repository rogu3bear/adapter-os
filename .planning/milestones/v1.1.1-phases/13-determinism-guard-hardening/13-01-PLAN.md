---
phase: 13-determinism-guard-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/adapteros-cli/src/commands/diag.rs
  - crates/adapteros-server-api/src/handlers/diagnostics.rs
  - crates/adapteros-api-types/src/diagnostics.rs
  - crates/adapteros-server-api/tests/diag_status_freshness_tests.rs
  - docs/runbooks/DETERMINISM_VIOLATION.md
  - .planning/phases/13-determinism-guard-hardening/13-01-SUMMARY.md
autonomous: true
requirements:
  - DET-07
must_haves:
    truths:
      - "Determinism diagnostics status includes explicit fresh/stale interpretation"
      - "Stale or missing determinism check state is surfaced with machine-readable reason"
      - "Automated diagnostics flow continuously refreshes determinism check records"
    artifacts:
      - path: "crates/adapteros-server-api/src/handlers/diagnostics.rs"
        provides: "Freshness evaluation and determinism status response contract"
        contains: "determinism-status"
      - path: "crates/adapteros-server-api/tests/diag_status_freshness_tests.rs"
        provides: "Targeted regression coverage for fresh/stale status behavior"
        contains: "freshness"
      - path: ".planning/phases/13-determinism-guard-hardening/13-01-SUMMARY.md"
        provides: "DET-07 execution evidence and residual risk notes"
        contains: "DET-07"
    key_links:
      - from: "crates/adapteros-cli/src/commands/diag.rs"
        to: "crates/adapteros-server-api/src/handlers/diagnostics.rs"
        via: "CLI determinism check persistence feeds diagnostics freshness API"
        pattern: "determinism_checks"
      - from: "migrations/20260217090000_determinism_checks.sql"
        to: "crates/adapteros-server-api/src/handlers/diagnostics.rs"
        via: "Storage schema fields are consumed by freshness logic"
        pattern: "last_run|result|divergences"
---

<objective>
Harden automated diagnostics freshness so determinism status is explicit, machine-checkable, and actionable for operators and automation.

Purpose: close `DET-07` with deterministic status freshness semantics instead of best-effort recency assumptions.
Output: explicit fresh/stale status behavior in diagnostics APIs plus targeted regression tests.
</objective>

<execution_context>
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/workflows/execute-plan.md
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-determinism-guard-hardening/13-CONTEXT.md
@.planning/phases/13-determinism-guard-hardening/13-RESEARCH.md
@crates/adapteros-cli/src/commands/diag.rs
@crates/adapteros-server-api/src/handlers/diagnostics.rs
@crates/adapteros-api-types/src/diagnostics.rs
@migrations/20260217090000_determinism_checks.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline determinism freshness contract from existing writer/reader paths</name>
  <files>
    crates/adapteros-cli/src/commands/diag.rs
    crates/adapteros-server-api/src/handlers/diagnostics.rs
    migrations/20260217090000_determinism_checks.sql
  </files>
  <action>
    Trace the end-to-end freshness contract from insert to API response:

    1. Confirm writer fields persisted in `determinism_checks`.
    2. Confirm diagnostics endpoint read behavior and fallback behavior when no row exists.
    3. Define canonical stale threshold semantics to implement (fresh vs stale vs unknown).
  </action>
  <verify>
    <automated>rg -n "INSERT INTO determinism_checks|determinism-status|last_run|result|divergences" crates/adapteros-cli/src/commands/diag.rs crates/adapteros-server-api/src/handlers/diagnostics.rs migrations/20260217090000_determinism_checks.sql</automated>
    <manual>Freshness threshold and stale semantics are explicitly documented before code changes.</manual>
  </verify>
  <done>
    - Baseline writer/reader contract is explicit.
    - Freshness behavior target is clear and testable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement fresh/stale determinism status behavior and targeted tests</name>
  <files>
    crates/adapteros-server-api/src/handlers/diagnostics.rs
    crates/adapteros-api-types/src/diagnostics.rs
    crates/adapteros-server-api/tests/diag_status_freshness_tests.rs
  </files>
  <action>
    Add explicit freshness evaluation to the determinism status API response:

    - expose freshness age and threshold metadata,
    - classify stale/missing check state explicitly,
    - preserve current endpoint and tenant/security behavior.

    Add focused integration tests for:
    - fresh status,
    - stale status,
    - missing-status fallback behavior.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api --test diag_status_freshness_tests -- --test-threads=1 --nocapture</automated>
    <manual>Inspect sample JSON payloads to confirm fresh/stale classification is machine-readable and unambiguous.</manual>
  </verify>
  <done>
    - API returns explicit freshness interpretation.
    - Targeted freshness tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Keep diagnostics producer path and operator runbook aligned</name>
  <files>
    crates/adapteros-cli/src/commands/diag.rs
    docs/runbooks/DETERMINISM_VIOLATION.md
    crates/adapteros-server-api/src/handlers/diagnostics.rs
  </files>
  <action>
    Ensure automated diagnostics execution and operator guidance are synchronized:

    - verify `aosctl diag` persistence path remains active for determinism checks,
    - align runbook endpoint references to `/v1/diagnostics/determinism-status`,
    - document stale-status remediation guidance consistent with new freshness semantics.
  </action>
  <verify>
    <automated>rg -n "/v1/diagnostics/determinism-status|determinism_checks" docs/runbooks/DETERMINISM_VIOLATION.md crates/adapteros-cli/src/commands/diag.rs crates/adapteros-server-api/src/handlers/diagnostics.rs</automated>
    <manual>Runbook and API behavior describe the same stale/fresh contract.</manual>
  </verify>
  <done>
    - Diagnostics producer and operator docs are consistent.
    - DET-07 freshness behavior is operationally usable.
  </done>
</task>

</tasks>

<verification>
1. `rg -n "INSERT INTO determinism_checks|determinism-status|last_run|result|divergences" crates/adapteros-cli/src/commands/diag.rs crates/adapteros-server-api/src/handlers/diagnostics.rs migrations/20260217090000_determinism_checks.sql`
2. `cargo test -p adapteros-server-api --test diag_status_freshness_tests -- --test-threads=1 --nocapture`
3. `rg -n "/v1/diagnostics/determinism-status|determinism_checks" docs/runbooks/DETERMINISM_VIOLATION.md crates/adapteros-cli/src/commands/diag.rs crates/adapteros-server-api/src/handlers/diagnostics.rs`
</verification>

<success_criteria>
- `DET-07` is satisfied with explicit determinism diagnostics freshness semantics.
- Fresh/stale behavior is tested and no longer implied.
- Operator runbook and API contract are aligned on one freshness truth.
</success_criteria>

<output>
After completion, create `.planning/phases/13-determinism-guard-hardening/13-01-SUMMARY.md`
</output>
