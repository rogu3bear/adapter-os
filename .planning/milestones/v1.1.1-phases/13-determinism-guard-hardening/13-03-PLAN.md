---
phase: 13-determinism-guard-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified:
  - crates/adapteros-server-api/src/handlers/streams/mod.rs
  - crates/adapteros-server-api/src/routes/mod.rs
  - crates/adapteros-server-api/src/handlers/monitoring/mod.rs
  - crates/adapteros-server-api/src/handlers/error_alerts.rs
  - crates/adapteros-server-api/src/sse/types.rs
  - crates/adapteros-server-api/tests/sse_stream_route_tests.rs
  - crates/adapteros-server-api/tests/diagnostics_events_test.rs
  - docs/api/ROUTE_MAP.md
  - .planning/phases/13-determinism-guard-hardening/13-03-SUMMARY.md
autonomous: true
requirements:
  - DET-09
must_haves:
    truths:
      - "Determinism-relevant alerts/telemetry are surfaced through protected stream/API surfaces"
      - "Alert and telemetry stream replay/gap semantics remain intact after route exposure"
      - "Tenant isolation and permission checks are preserved on newly surfaced streams"
    artifacts:
      - path: "crates/adapteros-server-api/src/routes/mod.rs"
        provides: "Protected route exposure for determinism-related stream surfaces"
        contains: "/v1/stream/alerts"
      - path: "crates/adapteros-server-api/src/handlers/streams/mod.rs"
        provides: "Alert/telemetry stream payload and replay behavior"
        contains: "alerts_stream"
      - path: ".planning/phases/13-determinism-guard-hardening/13-03-SUMMARY.md"
        provides: "DET-09 execution evidence and surfacing validation notes"
        contains: "DET-09"
    key_links:
      - from: "crates/adapteros-server-api/src/sse/types.rs"
        to: "crates/adapteros-server-api/src/handlers/streams/mod.rs"
        via: "Alerts stream type and replay semantics are consumed by handlers"
        pattern: "SseStreamType::Alerts"
      - from: "crates/adapteros-server-api/src/handlers/workers.rs"
        to: "crates/adapteros-server-api/src/handlers/streams/mod.rs"
        via: "Worker lifecycle emits alert events that must be operator-visible in streams"
        pattern: "emit_lifecycle"
      - from: "crates/adapteros-server-api/src/routes/mod.rs"
        to: "docs/api/ROUTE_MAP.md"
        via: "Stream route exposure and documentation must stay in sync"
        pattern: "/v1/stream/"
---

<objective>
Surface determinism guard telemetry and alerts so operators can observe freshness/replay failures through protected stream/API channels with replay-safe semantics.

Purpose: close `DET-09` by making determinism guard state visible and actionable in existing observability paths.
Output: routed stream surfaces, tenant-safe stream payloads, and verification coverage for alert/telemetry surfacing.
</objective>

<execution_context>
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/workflows/execute-plan.md
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-determinism-guard-hardening/13-CONTEXT.md
@.planning/phases/13-determinism-guard-hardening/13-RESEARCH.md
@crates/adapteros-server-api/src/handlers/streams/mod.rs
@crates/adapteros-server-api/src/routes/mod.rs
@crates/adapteros-server-api/src/sse/types.rs
@crates/adapteros-server-api/src/handlers/monitoring/mod.rs
@crates/adapteros-server-api/src/handlers/error_alerts.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose determinism-relevant alert stream routes with proper protection</name>
  <files>
    crates/adapteros-server-api/src/routes/mod.rs
    crates/adapteros-server-api/src/handlers/streams/mod.rs
    crates/adapteros-server-api/src/sse/types.rs
  </files>
  <action>
    Route existing alert/anomaly/dashboard stream handlers through protected routes:

    - expose stream paths (starting with `/v1/stream/alerts`),
    - enforce permission and tenant-isolation checks aligned with existing stream patterns,
    - preserve existing SSE replay and gap warning behavior via `SseEventManager`.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api --test sse_stream_route_tests -- --test-threads=1 --nocapture && rg -n "/v1/stream/alerts|/v1/stream/anomalies|/v1/stream/dashboard" crates/adapteros-server-api/src/routes/mod.rs</automated>
    <manual>Reconnect with `Last-Event-ID` and confirm replay events are delivered in order for surfaced streams.</manual>
  </verify>
  <done>
    - Alert/anomaly/dashboard stream routes are exposed and protected.
    - Replay and gap behavior remains functional.
  </done>
</task>

<task type="auto">
  <name>Task 2: Surface determinism freshness/replay guard state in telemetry and alert payloads</name>
  <files>
    crates/adapteros-server-api/src/handlers/streams/mod.rs
    crates/adapteros-server-api/src/handlers/monitoring/mod.rs
    crates/adapteros-server-api/src/handlers/error_alerts.rs
    crates/adapteros-server-api/tests/diagnostics_events_test.rs
  </files>
  <action>
    Extend existing stream/API payloads to include determinism guard context from `13-01` and `13-02`:

    - freshness status (fresh/stale),
    - replay guard outcome (pass/fail/unknown),
    - timestamp/age metadata for operator triage.

    Keep payloads additive and contract-stable; do not fork new event families.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api --test diagnostics_events_test -- --test-threads=1 --nocapture</automated>
    <manual>Inspect stream payload samples to confirm determinism guard fields are present and tenant-safe.</manual>
  </verify>
  <done>
    - Determinism guard state is visible in telemetry/alert surfaces.
    - Existing payload consumers remain compatible.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile stream route documentation and closure evidence</name>
  <files>
    docs/api/ROUTE_MAP.md
    crates/adapteros-server-api/src/routes/mod.rs
    .planning/phases/13-determinism-guard-hardening/13-03-SUMMARY.md
  </files>
  <action>
    Update route/evidence artifacts to match shipped stream surfaces and include validation transcript:

    - document newly exposed stream routes,
    - record curl/test evidence for stream availability and payload shape,
    - capture residual risks (for example, downstream client rollout dependencies).
  </action>
  <verify>
    <automated>rg -n "/v1/stream/alerts|/v1/stream/anomalies|/v1/stream/dashboard" docs/api/ROUTE_MAP.md crates/adapteros-server-api/src/routes/mod.rs .planning/phases/13-determinism-guard-hardening/13-03-SUMMARY.md</automated>
    <manual>Confirm summary contains explicit DET-09 closure statement tied to concrete stream evidence.</manual>
  </verify>
  <done>
    - Route docs and router definitions are synchronized.
    - DET-09 evidence package is auditable and complete.
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p adapteros-server-api --test sse_stream_route_tests -- --test-threads=1 --nocapture`
2. `cargo test -p adapteros-server-api --test diagnostics_events_test -- --test-threads=1 --nocapture`
3. `rg -n "/v1/stream/alerts|/v1/stream/anomalies|/v1/stream/dashboard" docs/api/ROUTE_MAP.md crates/adapteros-server-api/src/routes/mod.rs`
</verification>

<success_criteria>
- `DET-09` is satisfied with protected, replay-safe telemetry/alert surfacing for determinism guard state.
- Stream route exposure and documentation remain consistent.
- Tenant isolation and permission constraints are preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/13-determinism-guard-hardening/13-03-SUMMARY.md`
</output>
