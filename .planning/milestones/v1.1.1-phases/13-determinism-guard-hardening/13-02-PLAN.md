---
phase: 13-determinism-guard-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - crates/adapteros-server-api/src/handlers/health.rs
  - crates/adapteros-server-api/tests/health_readyz_timeout_tests.rs
  - crates/adapteros-server-api/tests/replay_guard_health_tests.rs
  - tests/determinism_replay_harness.rs
  - crates/adapteros-server-api/tests/replay_determinism_tests.rs
  - scripts/check_fast_math_flags.sh
  - .planning/phases/13-determinism-guard-hardening/13-02-SUMMARY.md
autonomous: true
requirements:
  - DET-08
must_haves:
    truths:
      - "Replay determinism guardrails remain merge-gating in CI with deterministic execution constraints"
      - "Runtime readiness/health surfaces expose replay guard status explicitly"
      - "Stale or failing replay guard state cannot be silently treated as healthy"
    artifacts:
      - path: ".github/workflows/ci.yml"
        provides: "Replay determinism merge-gate guardrail definitions"
        contains: "replay-harness"
      - path: "crates/adapteros-server-api/src/handlers/health.rs"
        provides: "Runtime readiness exposure for replay guard status"
        contains: "readyz"
      - path: ".planning/phases/13-determinism-guard-hardening/13-02-SUMMARY.md"
        provides: "DET-08 execution transcript and residual-risk capture"
        contains: "DET-08"
    key_links:
      - from: ".github/workflows/ci.yml"
        to: "tests/determinism_replay_harness.rs"
        via: "CI replay harness enforces deterministic replay contract in merge path"
        pattern: "determinism_replay_harness"
      - from: ".github/workflows/ci.yml"
        to: "crates/adapteros-server-api/tests/replay_determinism_tests.rs"
        via: "Server replay determinism coverage must remain guardrailed"
        pattern: "replay|determinism"
      - from: "crates/adapteros-server-api/src/handlers/health.rs"
        to: "crates/adapteros-server-api/tests/health_readyz_timeout_tests.rs"
        via: "Readiness semantics are verified by targeted health tests"
        pattern: "readyz"
---

<objective>
Harden deterministic replay guardrails so replay determinism is enforced both at merge time (CI) and at runtime health/readiness boundaries.

Purpose: close `DET-08` by making replay guard status explicit, testable, and operationally visible.
Output: CI guardrail hardening plus runtime readiness exposure for replay status.
</objective>

<execution_context>
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/workflows/execute-plan.md
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-determinism-guard-hardening/13-CONTEXT.md
@.planning/phases/13-determinism-guard-hardening/13-RESEARCH.md
@.github/workflows/ci.yml
@crates/adapteros-server-api/src/handlers/health.rs
@crates/adapteros-server-api/tests/health_readyz_timeout_tests.rs
@tests/determinism_replay_harness.rs
@crates/adapteros-server-api/tests/replay_determinism_tests.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline current replay guardrails across CI and readiness surfaces</name>
  <files>
    .github/workflows/ci.yml
    crates/adapteros-server-api/src/handlers/health.rs
    crates/adapteros-server-api/tests/health_readyz_timeout_tests.rs
  </files>
  <action>
    Establish current replay guard behavior before edits:

    1. enumerate replay-related CI lanes and deterministic execution flags,
    2. identify current readiness fields and check semantics,
    3. define fail-closed behavior target for stale/failing replay guard state.
  </action>
  <verify>
    <automated>rg -n "replay-harness|determinism-gate|determinism_replay_harness|replay_determinism_tests|readyz|healthz" .github/workflows/ci.yml crates/adapteros-server-api/src/handlers/health.rs crates/adapteros-server-api/tests/health_readyz_timeout_tests.rs</automated>
    <manual>Replay guard contract is explicitly documented before code changes.</manual>
  </verify>
  <done>
    - Baseline CI and readiness semantics are explicit.
    - Replay guard target behavior is unambiguous.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tighten replay determinism merge guardrails in CI</name>
  <files>
    .github/workflows/ci.yml
    tests/determinism_replay_harness.rs
    crates/adapteros-server-api/tests/replay_determinism_tests.rs
    scripts/check_fast_math_flags.sh
  </files>
  <action>
    Strengthen existing CI determinism lanes without creating new parallel workflows:

    - keep replay-sensitive suites serial where required,
    - ensure replay harness and replay API tests remain merge-gating signals,
    - keep fast-math guard in the determinism lane.
  </action>
  <verify>
    <automated>cargo test -p adapter-os --no-default-features --test determinism_replay_harness -- --test-threads=1 --nocapture && cargo test -p adapteros-server-api --test replay_determinism_tests -- --test-threads=1 && bash scripts/check_fast_math_flags.sh</automated>
    <manual>CI workflow changes preserve existing lane names/ownership and avoid duplicate determinism workflows.</manual>
  </verify>
  <done>
    - Replay CI lanes are explicit and deterministic.
    - Core replay guard commands pass locally in targeted runs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Surface replay guard status in readiness/health with targeted tests</name>
  <files>
    crates/adapteros-server-api/src/handlers/health.rs
    crates/adapteros-server-api/tests/health_readyz_timeout_tests.rs
    crates/adapteros-server-api/tests/replay_guard_health_tests.rs
  </files>
  <action>
    Extend health/readiness output to include replay guard state:

    - expose replay freshness/outcome signal in readiness payload,
    - enforce fail-closed behavior for stale/failing replay guard under strict mode,
    - cover fresh/stale/fail behavior in targeted readiness tests.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api --test health_readyz_timeout_tests -- --test-threads=1 && cargo test -p adapteros-server-api --test replay_guard_health_tests -- --test-threads=1 --nocapture</automated>
    <manual>Inspect `/readyz` JSON to confirm replay guard status is explicit and actionable.</manual>
  </verify>
  <done>
    - Runtime readiness clearly reports replay guard state.
    - Strict-mode replay guard failure/staleness is no longer silent.
  </done>
</task>

</tasks>

<verification>
1. `rg -n "replay-harness|determinism-gate|determinism_replay_harness|replay_determinism_tests|readyz|healthz" .github/workflows/ci.yml crates/adapteros-server-api/src/handlers/health.rs crates/adapteros-server-api/tests/health_readyz_timeout_tests.rs`
2. `cargo test -p adapter-os --no-default-features --test determinism_replay_harness -- --test-threads=1 --nocapture && cargo test -p adapteros-server-api --test replay_determinism_tests -- --test-threads=1 && bash scripts/check_fast_math_flags.sh`
3. `cargo test -p adapteros-server-api --test health_readyz_timeout_tests -- --test-threads=1 && cargo test -p adapteros-server-api --test replay_guard_health_tests -- --test-threads=1 --nocapture`
</verification>

<success_criteria>
- `DET-08` is satisfied with replay guardrails enforced in CI and visible in runtime health/readiness.
- Replay guard stale/fail states are explicit and fail-closed where required.
- No parallel determinism workflow/health paths are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/13-determinism-guard-hardening/13-02-SUMMARY.md`
</output>
