---
phase: 12-governance-debt-retirement
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - scripts/ci/check_governance_preflight.sh
  - .github/workflows/ci.yml
  - .planning/phases/12-governance-debt-retirement/12-02-SUMMARY.md
autonomous: true
requirements:
  - GOV-07
must_haves:
    truths:
      - "Governance preflight is automated with deterministic output and exit behavior"
      - "Preflight remains read-only and does not mutate branch-protection policy"
      - "Preflight evaluates canonical repo/branch/context used by governance retirement"
    artifacts:
      - path: ".planning/phases/12-governance-debt-retirement/12-02-SUMMARY.md"
        provides: "automation transcript and capability status evidence"
        contains: "GOV-07"
    key_links:
      - from: ".github/workflows/ci.yml"
        to: ".planning/phases/12-governance-debt-retirement/12-02-SUMMARY.md"
        via: "summary anchors governance preflight to canonical ASAN merge-gate context already present in CI"
        pattern: "ffi-asan|FFI AddressSanitizer \\(push\\)"
---

<objective>
Implement and wire governance preflight automation that deterministically reports branch-protection capability and required-check status for governance debt retirement.

Purpose: eliminate manual governance preflight drift and make retirement readiness machine-checkable.
Output: CI-ready preflight script, workflow invocation, and summary evidence.
</objective>

<execution_context>
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/workflows/execute-plan.md
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-governance-debt-retirement/12-CONTEXT.md
@.planning/phases/12-governance-debt-retirement/12-RESEARCH.md
@.planning/phases/12-governance-debt-retirement/12-01-PLAN.md
@docs/governance/README.md
@MVP_PROD_CHECKLIST.md
@scripts/ci/
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic governance preflight script</name>
  <files>
    scripts/ci/check_governance_preflight.sh
  </files>
  <action>
    Add a new CI check script that:
    - accepts explicit `--repo`, `--branch`, and `--required-context` inputs,
    - performs read-only branch-protection required-check probe via `gh api`,
    - emits a clear normalized status (`capable`, `blocked_external`, `misconfigured`, `error`),
    - exits with deterministic codes suitable for CI gating/policy decisions.

    Keep output concise and grep-friendly for downstream evidence capture.
  </action>
  <verify>
    <automated>bash scripts/ci/check_governance_preflight.sh --help</automated>
    <automated>bash scripts/ci/check_governance_preflight.sh --repo rogu3bear/adapter-os --branch main --required-context 'FFI AddressSanitizer (push)'</automated>
  </verify>
  <done>
    - Script interface and status model are stable and documented by behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire governance preflight into CI workflow</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <action>
    Add a CI invocation path for `check_governance_preflight.sh` that runs with canonical repo/branch/context inputs and captures status output as part of governance evidence.

    Keep integration minimal and aligned to current CI job structure.
  </action>
  <verify>
    <automated>rg -n "check_governance_preflight\\.sh|FFI AddressSanitizer \\(push\\)|required_status_checks" .github/workflows/ci.yml -S</automated>
  </verify>
  <done>
    - CI contains a native invocation path for governance preflight.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run preflight and capture evidence transcript</name>
  <files>
    .planning/phases/12-governance-debt-retirement/12-02-SUMMARY.md
    var/evidence/phase12/
  </files>
  <action>
    Execute governance preflight with canonical inputs, capture output to evidence log, and record resulting status class with next-action guidance in summary.

    Expected behavior in this environment may remain `blocked_external`; record this explicitly if observed.
  </action>
  <verify>
    <automated>bash scripts/ci/check_governance_preflight.sh --repo rogu3bear/adapter-os --branch main --required-context 'FFI AddressSanitizer (push)' | tee var/evidence/phase12/12-02-governance-preflight.log</automated>
    <automated>rg -n "capable|blocked_external|misconfigured|error" var/evidence/phase12/12-02-governance-preflight.log -S</automated>
  </verify>
  <done>
    - Preflight status is evidenced and explicitly tied to follow-on action.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/ci/check_governance_preflight.sh --help`
- [ ] `bash scripts/ci/check_governance_preflight.sh --repo rogu3bear/adapter-os --branch main --required-context 'FFI AddressSanitizer (push)'`
- [ ] `rg -n "check_governance_preflight\\.sh|FFI AddressSanitizer \\(push\\)|required_status_checks" .github/workflows/ci.yml -S`
- [ ] `rg -n "capable|blocked_external|misconfigured|error" var/evidence/phase12/12-02-governance-preflight.log -S`
</verification>

<success_criteria>
- Governance preflight is automated, reproducible, and CI-consumable.
- Retirement readiness is determined by deterministic status classes, not manual interpretation.
</success_criteria>

<output>
After completion, create `.planning/phases/12-governance-debt-retirement/12-02-SUMMARY.md`
</output>
