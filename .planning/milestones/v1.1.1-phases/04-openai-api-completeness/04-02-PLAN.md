---
phase: 04-openai-api-completeness
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - crates/adapteros-server-api/src/handlers/openai_compat.rs
  - crates/adapteros-server-api/src/routes/mod.rs
  - crates/adapteros-server-api/src/bin/export-openapi.rs
  - crates/adapteros-server-api/tests/openai_models_list_test.rs
  - crates/adapteros-server-api/tests/openai_error_format_tests.rs
  - target/codegen/openapi.json
autonomous: true
requirements:
  - API-05
  - API-06

must_haves:
  truths:
    - "Generated OpenAPI spec reflects implemented OpenAI endpoint request/response fields with no contract drift"
    - "OpenAI endpoint failures consistently return `{ error: { message, type, code?, param? } }` envelope"
    - "OpenAI error `type` values are not hardcoded to a single class for all failures"
    - "Compatibility regressions verify both schema parity and error envelope behavior"
  artifacts:
    - path: "crates/adapteros-server-api/src/routes/mod.rs"
      provides: "OpenAPI path registration parity for OpenAI endpoints"
      contains: "handlers::openai_compat::chat_completions"
    - path: "crates/adapteros-server-api/src/handlers/openai_compat.rs"
      provides: "OpenAI error classification and envelope consistency"
      contains: "OpenAiErrorResponse"
    - path: "target/codegen/openapi.json"
      provides: "Deterministic generated OpenAPI output matching runtime contracts"
    - path: "crates/adapteros-server-api/tests/openai_error_format_tests.rs"
      provides: "Error envelope regression coverage"
  key_links:
    - from: "crates/adapteros-server-api/src/routes/mod.rs"
      to: "crates/adapteros-server-api/src/bin/export-openapi.rs"
      via: "utoipa ApiDoc generation path"
      pattern: "ApiDoc::openapi"
    - from: "crates/adapteros-server-api/src/handlers/openai_compat.rs"
      to: "target/codegen/openapi.json"
      via: "schema annotations must match runtime behavior"
      pattern: "utoipa::path"
---

<objective>
Close Phase 4 contract and error-format gaps: ensure OpenAPI parity with implemented OpenAI behavior and standardize OpenAI error envelopes/types across compatibility endpoints.

Purpose: Eliminates client-side contract drift and parsing ambiguity for OpenAI ecosystem tooling.
Output: Regenerated OpenAPI artifact plus targeted contract/error regression coverage for `API-05` and `API-06`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-openai-api-completeness/04-RESEARCH.md
@.planning/phases/04-openai-api-completeness/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove OpenAPI contract drift for OpenAI endpoints</name>
  <files>
    crates/adapteros-server-api/src/routes/mod.rs
    crates/adapteros-server-api/src/handlers/openai_compat.rs
    crates/adapteros-server-api/src/bin/export-openapi.rs
    target/codegen/openapi.json
  </files>
  <action>
    Ensure all OpenAI compatibility request/response fields introduced in 04-01 are represented in utoipa schemas and generated paths.

    Then regenerate OpenAPI via the existing exporter path and validate deterministic output.

    Use the existing route and ApiDoc ownership only; do not create alternate OpenAPI generation flows.
  </action>
  <verify>
    <automated>cargo run -p adapteros-server-api --bin export-openapi -- target/codegen/openapi.json && rg -n '"/v1/chat/completions"|"/v1/completions"|"/v1/embeddings"|"/v1/models"' target/codegen/openapi.json</automated>
    <manual>Inspect generated schemas for `response_format`, `tools`, and new OpenAI fields</manual>
  </verify>
  <done>
    - Generated OpenAPI contains updated OpenAI request/response contracts
    - No route/spec drift remains for OpenAI endpoints
  </done>
</task>

<task type="auto">
  <name>Task 2: Standardize OpenAI error envelope and typing across failure classes</name>
  <files>
    crates/adapteros-server-api/src/handlers/openai_compat.rs
    crates/adapteros-server-api/tests/openai_error_format_tests.rs
  </files>
  <action>
    Normalize OpenAI compatibility endpoint error responses so they always match envelope shape and include meaningful `error.type` classification.

    At minimum, validate separate classes for:
    - invalid request/validation failures
    - auth/permission failures
    - rate/backpressure failures
    - server/internal failures

    Keep adapterOS internal error data in `code`/`message` without leaking non-contractual structures.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api openai_error_format -- --nocapture</automated>
    <manual>Sample each failure class and confirm envelope and type are client-parseable</manual>
  </verify>
  <done>
    - OpenAI errors are consistently shaped
    - Error type is classification-aware
    - Regression tests lock envelope behavior
  </done>
</task>

<task type="auto">
  <name>Task 3: Add contract-level compatibility regression checks</name>
  <files>
    crates/adapteros-server-api/tests/openai_models_list_test.rs
    crates/adapteros-server-api/tests/openai_chat_completions_compat.rs
  </files>
  <action>
    Add or extend targeted tests that assert:
    - request fields present in code are represented in generated schema
    - response/error examples match OpenAI-compatible shapes
    - endpoint-level compatibility remains stable after schema regeneration

    Keep tests focused and deterministic; avoid broad system test expansion.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api openai_ -- --nocapture</automated>
    <manual>Confirm API-05 and API-06 each have explicit assertion coverage</manual>
  </verify>
  <done>
    - Schema and runtime compatibility checks are covered by focused tests
    - Phase 4 contract/error regressions are guarded
  </done>
</task>

</tasks>

<verification>
1. `cargo run -p adapteros-server-api --bin export-openapi -- target/codegen/openapi.json`
2. `cargo test -p adapteros-server-api openai_ -- --nocapture`
3. Targeted manual checks for OpenAI error envelopes by failure class
</verification>

<success_criteria>
- `API-05` contract drift is resolved and guarded
- `API-06` error shape/type compatibility is resolved and guarded
- OpenAI ecosystem-facing contracts are stable and documented through generated spec
</success_criteria>

<output>
After completion, create `.planning/phases/04-openai-api-completeness/04-02-SUMMARY.md`
</output>
