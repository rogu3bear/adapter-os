---
phase: 04-openai-api-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/adapteros-server-api/src/handlers/openai_compat.rs
  - crates/adapteros-server-api/src/types/request.rs
  - crates/adapteros-server-api/src/types/conversion.rs
  - crates/adapteros-server-api/tests/openai_chat_completions_streaming.rs
  - crates/adapteros-server-api/tests/openai_embeddings_tests.rs
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04

must_haves:
  truths:
    - "`/v1/chat/completions` accepts `response_format` requests and enforces schema constraints for supported structured-output mode"
    - "`/v1/chat/completions` accepts `tools` and `tool_choice` and emits OpenAI-compatible tool-call response payloads"
    - "Completion responses expose coherent `usage.prompt_tokens`, `usage.completion_tokens`, `usage.total_tokens` on successful non-streaming requests"
    - "`seed`, `stop`, `frequency_penalty`, `presence_penalty`, and `logprobs` are accepted on OpenAI completion requests and have defined behavior (applied or explicit compatibility error)"
  artifacts:
    - path: "crates/adapteros-server-api/src/handlers/openai_compat.rs"
      provides: "Expanded OpenAI request/response surface and boundary mapping"
      contains: "response_format"
    - path: "crates/adapteros-server-api/src/handlers/openai_compat.rs"
      provides: "Tool calling request/response support"
      contains: "tool_choice"
    - path: "crates/adapteros-server-api/src/handlers/openai_compat.rs"
      provides: "Missing parameter handling"
      contains: "frequency_penalty"
    - path: "crates/adapteros-server-api/tests/openai_chat_completions_streaming.rs"
      provides: "Compatibility regression tests for request/response behavior"
  key_links:
    - from: "crates/adapteros-server-api/src/handlers/openai_compat.rs"
      to: "crates/adapteros-server-api/src/types/conversion.rs"
      via: "OpenAI request fields mapped to inference request semantics"
      pattern: "seed|stop|frequency_penalty|presence_penalty|logprobs"
    - from: "crates/adapteros-server-api/src/handlers/openai_compat.rs"
      to: "crates/adapteros-server-api/src/routes/mod.rs"
      via: "Existing OpenAI route ownership remains unchanged"
      pattern: "openai_compat::chat_completions"
---

<objective>
Implement Phase 4 behavior completeness on top of the existing OpenAI shim: structured output support, tool/function calling compatibility, complete usage accounting consistency, and acceptance/mapping of missing OpenAI parameters.

Purpose: Closes the runtime behavior gap for ecosystem clients while keeping ownership centralized in `openai_compat.rs`.
Output: OpenAI endpoints with behavior coverage for `API-01..API-04` and targeted compatibility tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-openai-api-completeness/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand OpenAI request/response types without parallel handler paths</name>
  <files>
    crates/adapteros-server-api/src/handlers/openai_compat.rs
  </files>
  <action>
    Extend `OpenAiChatCompletionsRequest` and related response structs to include the missing OpenAI compatibility fields required by roadmap scope:

    - `response_format` with structured JSON schema mode
    - `tools`
    - `tool_choice`
    - `seed`
    - `stop`
    - `frequency_penalty`
    - `presence_penalty`
    - `logprobs`

    Keep all changes in the existing `openai_compat.rs` ownership boundary.

    For unsupported value combinations, return OpenAI-style errors via the existing envelope, including `param` where applicable.
  </action>
  <verify>
    <automated>rg -n "response_format|json_schema|tools|tool_choice|frequency_penalty|presence_penalty|logprobs|seed|stop" crates/adapteros-server-api/src/handlers/openai_compat.rs</automated>
    <manual>Confirm no new parallel OpenAI handler files were introduced</manual>
  </verify>
  <done>
    - Request structs expose required compatibility fields
    - Response structs can represent tool-call and structured-output-related payloads as needed
    - Validation failures emit OpenAI-style errors with parameter targeting
  </done>
</task>

<task type="auto">
  <name>Task 2: Map new fields to inference behavior and enforce structured output semantics</name>
  <files>
    crates/adapteros-server-api/src/handlers/openai_compat.rs
    crates/adapteros-server-api/src/types/request.rs
    crates/adapteros-server-api/src/types/conversion.rs
  </files>
  <action>
    Implement boundary mapping from OpenAI fields into existing inference request flow:

    - Apply `seed` mapping to deterministic sampling inputs.
    - Apply `stop` mapping to stop policy behavior.
    - Wire penalties and logprob controls where supported by inference path; otherwise return explicit compatibility errors.
    - Implement structured output validation/enforcement for `response_format` JSON schema mode.
    - Implement tool-call output shaping for `tools` + `tool_choice` requests.

    Ensure `usage` remains coherent on all successful non-streaming responses, including structured/tool-call responses.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api openai_chat_completions -- --nocapture</automated>
    <manual>Run one concrete chat request per feature path: structured output, tool call, seeded request, stop-sequence request</manual>
  </verify>
  <done>
    - Each required field has defined runtime behavior (applied or explicit compatibility rejection)
    - Structured output path validates against supplied schema for supported mode
    - Tool-calling response envelope is parseable by OpenAI-style clients
    - Usage totals remain internally consistent
  </done>
</task>

<task type="auto">
  <name>Task 3: Add targeted OpenAI compatibility regressions for API-01..API-04</name>
  <files>
    crates/adapteros-server-api/tests/openai_chat_completions_streaming.rs
    crates/adapteros-server-api/tests/openai_embeddings_tests.rs
    crates/adapteros-server-api/tests/openai_chat_completions_compat.rs
  </files>
  <action>
    Extend existing OpenAI compatibility tests and add one focused chat compatibility test file if needed.

    Required assertions:
    - `response_format.json_schema` success + failure cases
    - `tools/tool_choice` request and response shape validity
    - usage field coherence (`prompt + completion == total`)
    - missing parameter acceptance and expected behavior/compatibility error path

    Keep tests narrow and route-level; avoid broad workspace suites.
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api openai_ -- --nocapture</automated>
    <manual>Review test names and assertions to ensure each of API-01..API-04 is directly covered</manual>
  </verify>
  <done>
    - Focused OpenAI tests exist for each requirement in this plan
    - No duplicate or parallel test harness introduced
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p adapteros-server-api openai_ -- --nocapture`
2. `rg -n "response_format|json_schema|tools|tool_choice|seed|stop|frequency_penalty|presence_penalty|logprobs" crates/adapteros-server-api/src/handlers/openai_compat.rs`
3. Concrete manual request checks for structured output and tool-calling response shape
</verification>

<success_criteria>
- `API-01`, `API-02`, `API-03`, and `API-04` are implemented in the existing OpenAI shim
- OpenAI request/response compatibility is behaviorally validated with targeted tests
- No duplicate routing or handler structures were created
</success_criteria>

<output>
After completion, create `.planning/phases/04-openai-api-completeness/04-01-SUMMARY.md`
</output>
