---
phase: 09-determinism-and-compatibility-revalidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/determinism_replay_harness.rs
  - tests/record_replay_receipt_harness.rs
  - tests/determinism/canonical_hashing.rs
  - crates/adapteros-server-api/tests/replay_determinism_tests.rs
  - scripts/check_fast_math_flags.sh
  - docs/DETERMINISM.md
  - docs/runbooks/DETERMINISM_VIOLATION.md
  - .planning/phases/09-determinism-and-compatibility-revalidation/
autonomous: true
requirements:
  - DET-06
must_haves:
    truths:
      - "Deferred replay determinism suites from Phase 03 are re-run on current workspace state"
      - "Replay determinism and receipt replay commands pass with deterministic thread settings"
      - "Fast-math guard remains green after rerun"
      - "Any regression fix stays within existing determinism paths (no parallel harness)"
    artifacts:
      - path: ".planning/phases/09-determinism-and-compatibility-revalidation/09-01-SUMMARY.md"
        provides: "DET-06 command transcript, outcomes, and residual risk notes"
        contains: "DET-06"
      - path: "tests/determinism_replay_harness.rs"
        provides: "Primary replay determinism harness ownership"
        contains: "cargo test -p adapter-os --no-default-features --test determinism_replay_harness"
      - path: "crates/adapteros-server-api/tests/replay_determinism_tests.rs"
        provides: "Server API replay determinism coverage"
        contains: "replay"
    key_links:
      - from: "tests/determinism_replay_harness.rs"
        to: "crates/adapteros-server-api/tests/replay_determinism_tests.rs"
        via: "Replay determinism is proven at harness and API layers"
        pattern: "replay|determinism"
      - from: "scripts/check_fast_math_flags.sh"
        to: ".github/workflows/ci.yml"
        via: "Fast-math guard remains part of deterministic verification baseline"
        pattern: "check_fast_math_flags.sh"
---

<objective>
Re-run and close the deferred replay determinism verification set from Phase 03 summaries to satisfy `DET-06` on current workspace state.

Purpose: determinism claims for v1.1 need fresh replay-level evidence, not compile-only or subset confidence.
Output: passing deferred determinism command set, plus minimal regression fixes only if failures appear.
</objective>

<execution_context>
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/workflows/execute-plan.md
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-determinism-and-compatibility-revalidation/09-CONTEXT.md
@.planning/phases/09-determinism-and-compatibility-revalidation/09-RESEARCH.md
@.planning/phases/03-determinism-verification/03-01-SUMMARY.md
@.planning/phases/03-determinism-verification/03-02-SUMMARY.md
@tests/determinism_replay_harness.rs
@tests/record_replay_receipt_harness.rs
@crates/adapteros-server-api/tests/replay_determinism_tests.rs
@scripts/check_fast_math_flags.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run deferred determinism command matrix (DET-06)</name>
  <files>
    tests/determinism/canonical_hashing.rs
    tests/record_replay_receipt_harness.rs
    tests/determinism_replay_harness.rs
    crates/adapteros-server-api/tests/replay_determinism_tests.rs
    scripts/check_fast_math_flags.sh
  </files>
  <action>
    Re-run the exact deferred command set captured by Phase 03 closeouts on the current workspace:

    1. `cargo test --test determinism_core_suite canonical_hashing -- --test-threads=1`
    2. `cargo test --test record_replay_receipt_harness -- --test-threads=1`
    3. `cargo test -p adapter-os --no-default-features --test determinism_replay_harness -- --test-threads=1 --nocapture`
    4. `cargo test -p adapteros-server-api --test replay_determinism_tests -- --test-threads=1`
    5. `bash scripts/check_fast_math_flags.sh`

    Keep command order stable to preserve evidence comparability with prior summaries.
  </action>
  <verify>
    <automated>cargo test --test determinism_core_suite canonical_hashing -- --test-threads=1 && cargo test --test record_replay_receipt_harness -- --test-threads=1 && cargo test -p adapter-os --no-default-features --test determinism_replay_harness -- --test-threads=1 --nocapture && cargo test -p adapteros-server-api --test replay_determinism_tests -- --test-threads=1 && bash scripts/check_fast_math_flags.sh</automated>
    <manual>Confirm all previously deferred suites are represented in logs with explicit pass/fail outcomes.</manual>
  </verify>
  <done>
    - All deferred determinism commands have fresh outcomes.
    - Replay-level determinism confidence is current, not inherited.
    - Fast-math scan remains clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply smallest deterministic regression fixes only when reruns fail</name>
  <files>
    tests/determinism_replay_harness.rs
    tests/record_replay_receipt_harness.rs
    crates/adapteros-server-api/tests/replay_determinism_tests.rs
    docs/DETERMINISM.md
    docs/runbooks/DETERMINISM_VIOLATION.md
  </files>
  <action>
    If any deferred suite fails, fix only the failing deterministic path using existing abstractions.

    Rules:
    - No new replay harness files.
    - No broad determinism refactor.
    - Keep fixes source-linked to failing assertions and update docs only if behavior/guard semantics changed.
  </action>
  <verify>
    <automated>cargo test -p adapter-os --no-default-features --test determinism_replay_harness -- --test-threads=1 --nocapture && cargo test -p adapteros-server-api --test replay_determinism_tests -- --test-threads=1</automated>
    <manual>Review diff to ensure no parallel determinism path or duplicate test harness was introduced.</manual>
  </verify>
  <done>
    - Failing deterministic behavior is corrected with minimal localized changes.
    - Command matrix returns to passing state.
  </done>
</task>

<task type="auto">
  <name>Task 3: Capture DET-06 closure evidence and residual risk</name>
  <files>
    .planning/phases/09-determinism-and-compatibility-revalidation/09-01-SUMMARY.md
  </files>
  <action>
    Record exact commands, outcomes, and any environment limitations in `09-01-SUMMARY.md`.

    Include:
    - deferred command matrix
    - pass/fail outcomes with key lines
    - behavior changed (if any)
    - residual risk and unverified items
  </action>
  <verify>
    <automated>rg -n "DET-06|determinism_replay_harness|replay_determinism_tests|Residual Risk" .planning/phases/09-determinism-and-compatibility-revalidation/09-01-SUMMARY.md</automated>
    <manual>Confirm summary explicitly states whether all deferred suites passed on current workspace.</manual>
  </verify>
  <done>
    - Summary provides auditable DET-06 closure evidence.
    - Residual risk is explicit if any command was skipped or environment-blocked.
  </done>
</task>

</tasks>

<verification>
1. `cargo test --test determinism_core_suite canonical_hashing -- --test-threads=1`
2. `cargo test --test record_replay_receipt_harness -- --test-threads=1`
3. `cargo test -p adapter-os --no-default-features --test determinism_replay_harness -- --test-threads=1 --nocapture`
4. `cargo test -p adapteros-server-api --test replay_determinism_tests -- --test-threads=1`
5. `bash scripts/check_fast_math_flags.sh`
</verification>

<success_criteria>
- `DET-06` is satisfied with fresh replay determinism evidence on current workspace state.
- Deferred Phase 03 suites are no longer outstanding.
- No duplicate determinism verification tracks were introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/09-determinism-and-compatibility-revalidation/09-01-SUMMARY.md`
</output>
