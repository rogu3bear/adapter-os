---
phase: 09-determinism-and-compatibility-revalidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/adapteros-server-api/src/handlers/openai_compat.rs
  - crates/adapteros-server-api/src/routes/mod.rs
  - crates/adapteros-server-api/tests/openai_chat_completions_compat.rs
  - crates/adapteros-server-api/tests/openai_chat_completions_streaming.rs
  - crates/adapteros-server-api/tests/openai_embeddings_tests.rs
  - crates/adapteros-server-api/tests/openai_models_list_test.rs
  - crates/adapteros-server-api/tests/openai_error_format_tests.rs
  - crates/adapteros-server-api/tests/streaming_infer.rs
  - crates/adapteros-server-api/tests/streaming_adapter_integration.rs
  - scripts/ci/check_openapi_drift.sh
  - docs/api/openapi.json
  - .planning/phases/09-determinism-and-compatibility-revalidation/
autonomous: true
requirements:
  - API-07
must_haves:
    truths:
      - "Full OpenAI compatibility suites are rerun, not only targeted subsets"
      - "OpenAPI drift gate runs against current handlers and stays aligned"
      - "OpenAI compatibility regressions are fixed in existing shim/tests only"
      - "Phase 04 deferred OpenAI and OpenAPI checks are explicitly closed"
    artifacts:
      - path: ".planning/phases/09-determinism-and-compatibility-revalidation/09-02-SUMMARY.md"
        provides: "API-07 command transcript and closure evidence"
        contains: "API-07"
      - path: "docs/api/openapi.json"
        provides: "Committed OpenAPI contract artifact"
        contains: "\"paths\""
      - path: "scripts/ci/check_openapi_drift.sh"
        provides: "Canonical OpenAPI drift guard"
        contains: "export-openapi"
    key_links:
      - from: "scripts/ci/check_openapi_drift.sh"
        to: "docs/api/openapi.json"
        via: "Drift check compares generated spec against committed OpenAPI artifact"
        pattern: "docs/api/openapi.json"
      - from: "crates/adapteros-server-api/src/routes/mod.rs"
        to: "crates/adapteros-server-api/src/handlers/openai_compat.rs"
        via: "OpenAI routes remain owned by existing compatibility handler"
        pattern: "openai_compat::"
---

<objective>
Re-run full OpenAI compatibility suites and OpenAPI regeneration/drift checks deferred in Phase 04, then close `API-07` with current workspace evidence.

Purpose: OpenAI compatibility claims are valid only when the complete suite and contract drift checks are fresh and passing.
Output: full OpenAI suite pass evidence, OpenAPI parity confirmation, and minimal compatibility fixes if regressions surface.
</objective>

<execution_context>
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/workflows/execute-plan.md
@~/.codex/skills/gsd-codex-runtime/runtime/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-determinism-and-compatibility-revalidation/09-CONTEXT.md
@.planning/phases/09-determinism-and-compatibility-revalidation/09-RESEARCH.md
@.planning/phases/04-openai-api-completeness/04-01-SUMMARY.md
@.planning/phases/04-openai-api-completeness/04-02-SUMMARY.md
@crates/adapteros-server-api/src/handlers/openai_compat.rs
@crates/adapteros-server-api/src/routes/mod.rs
@scripts/ci/check_openapi_drift.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute full OpenAI compatibility test matrix (API-07)</name>
  <files>
    crates/adapteros-server-api/tests/openai_chat_completions_compat.rs
    crates/adapteros-server-api/tests/openai_chat_completions_streaming.rs
    crates/adapteros-server-api/tests/openai_embeddings_tests.rs
    crates/adapteros-server-api/tests/openai_models_list_test.rs
    crates/adapteros-server-api/tests/openai_error_format_tests.rs
    crates/adapteros-server-api/tests/streaming_infer.rs
    crates/adapteros-server-api/tests/streaming_adapter_integration.rs
  </files>
  <action>
    Re-run the complete OpenAI compatibility suite set, including route-level and stream-level checks, instead of only Phase 04 targeted subsets:

    1. `cargo test -p adapteros-server-api --test openai_chat_completions_compat -- --test-threads=1 --nocapture`
    2. `cargo test -p adapteros-server-api --test openai_chat_completions_streaming -- --test-threads=1 --nocapture`
    3. `cargo test -p adapteros-server-api --test openai_embeddings_tests -- --test-threads=1 --nocapture`
    4. `cargo test -p adapteros-server-api --test openai_models_list_test -- --test-threads=1 --nocapture`
    5. `cargo test -p adapteros-server-api --test openai_error_format_tests -- --test-threads=1 --nocapture`
    6. `cargo test -p adapteros-server-api --test streaming_infer test_openai_compatible_format -- --test-threads=1 --nocapture`
    7. `cargo test -p adapteros-server-api --test streaming_adapter_integration test_openai_spec_compliance -- --test-threads=1 --nocapture`

    Keep all fixes in existing OpenAI compatibility ownership (`openai_compat.rs` and current tests).
  </action>
  <verify>
    <automated>cargo test -p adapteros-server-api --test openai_chat_completions_compat -- --test-threads=1 --nocapture && cargo test -p adapteros-server-api --test openai_chat_completions_streaming -- --test-threads=1 --nocapture && cargo test -p adapteros-server-api --test openai_embeddings_tests -- --test-threads=1 --nocapture && cargo test -p adapteros-server-api --test openai_models_list_test -- --test-threads=1 --nocapture && cargo test -p adapteros-server-api --test openai_error_format_tests -- --test-threads=1 --nocapture && cargo test -p adapteros-server-api --test streaming_infer test_openai_compatible_format -- --test-threads=1 --nocapture && cargo test -p adapteros-server-api --test streaming_adapter_integration test_openai_spec_compliance -- --test-threads=1 --nocapture</automated>
    <manual>Confirm the command set covers all previously deferred OpenAI areas (chat, streaming, embeddings, models, error format, spec compliance).</manual>
  </verify>
  <done>
    - Full OpenAI compatibility suite matrix has fresh pass/fail outcomes.
    - Deferred Phase 04 subset-only risk is removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run OpenAPI regeneration and drift checks</name>
  <files>
    scripts/ci/check_openapi_drift.sh
    crates/adapteros-server-api/src/bin/export-openapi.rs
    docs/api/openapi.json
  </files>
  <action>
    Re-run canonical OpenAPI parity checks:

    1. `bash scripts/ci/check_openapi_drift.sh`
    2. `cargo run --locked -p adapteros-server-api --bin export-openapi -- target/codegen/openapi.json`

    If drift exists, use `bash scripts/ci/check_openapi_drift.sh --fix` to update `docs/api/openapi.json` through the existing toolchain. Do not add alternate export scripts.
  </action>
  <verify>
    <automated>bash scripts/ci/check_openapi_drift.sh && cargo run --locked -p adapteros-server-api --bin export-openapi -- target/codegen/openapi.json</automated>
    <manual>Confirm generated and committed OpenAPI artifacts remain aligned with current OpenAI handlers/routes.</manual>
  </verify>
  <done>
    - OpenAPI drift gate passes.
    - Export command produces current spec artifact.
    - Contract parity for OpenAI paths is explicit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Capture API-07 closure evidence and residual risk</name>
  <files>
    .planning/phases/09-determinism-and-compatibility-revalidation/09-02-SUMMARY.md
  </files>
  <action>
    Record exact suite commands, outcomes, any drift fixes, and residual risk in `09-02-SUMMARY.md`.

    Include explicit references to:
    - full OpenAI rerun scope
    - OpenAPI drift/regeneration status
    - any skipped command and reason
  </action>
  <verify>
    <automated>rg -n "API-07|openai_|check_openapi_drift|export-openapi|Residual Risk" .planning/phases/09-determinism-and-compatibility-revalidation/09-02-SUMMARY.md</automated>
    <manual>Confirm summary is explicit about full-suite coverage versus prior targeted subset coverage.</manual>
  </verify>
  <done>
    - Summary provides auditable API-07 closure evidence.
    - Remaining uncertainty is explicit and scoped.
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p adapteros-server-api --test openai_chat_completions_compat -- --test-threads=1 --nocapture`
2. `cargo test -p adapteros-server-api --test openai_chat_completions_streaming -- --test-threads=1 --nocapture`
3. `cargo test -p adapteros-server-api --test openai_embeddings_tests -- --test-threads=1 --nocapture`
4. `cargo test -p adapteros-server-api --test openai_models_list_test -- --test-threads=1 --nocapture`
5. `cargo test -p adapteros-server-api --test openai_error_format_tests -- --test-threads=1 --nocapture`
6. `cargo test -p adapteros-server-api --test streaming_infer test_openai_compatible_format -- --test-threads=1 --nocapture`
7. `cargo test -p adapteros-server-api --test streaming_adapter_integration test_openai_spec_compliance -- --test-threads=1 --nocapture`
8. `bash scripts/ci/check_openapi_drift.sh`
9. `cargo run --locked -p adapteros-server-api --bin export-openapi -- target/codegen/openapi.json`
</verification>

<success_criteria>
- `API-07` is satisfied with fresh full-suite OpenAI compatibility evidence.
- Deferred Phase 04 full-suite and OpenAPI regeneration checks are closed.
- No duplicate compatibility routing or parallel contract-generation path is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/09-determinism-and-compatibility-revalidation/09-02-SUMMARY.md`
</output>
