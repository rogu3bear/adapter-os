---
phase: 06-production-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MVP_PROD_CHECKLIST.md
  - .github/workflows/security-regression-tests.yml
  - scripts/backup/backup.sh
  - scripts/backup/verify-backups.sh
  - scripts/backup/test-restore.sh
  - scripts/backup/ci-smoke.sh
  - scripts/release/sbom.sh
  - scripts/release/mvp_control_room.sh
  - scripts/verify-deployment.sh
  - docs/runbooks/README.md
autonomous: false
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04
  - OPS-05

must_haves:
  truths:
    - "Security regression gate runs clean with no blocking failures"
    - "Backup can be created, verified, and restored to clean target paths"
    - "Release artifacts include SBOM and provenance bundle outputs"
    - "All REQUIRED Phase 6-aligned items in MVP production checklist have evidence"
    - "Deployment runbook path executes in staging-equivalent flow without manual intervention failures"
  artifacts:
    - path: "var/release-control-room/<timestamp>/evidence.log"
      provides: "Timestamped operational run evidence for go/no-go review"
    - path: "target/release-bundle/sbom.json"
      provides: "SBOM artifact bound to release binaries"
      contains: "\"schema\": \"adapteros-bundle-sbom-v1\""
    - path: "target/release-bundle/build_provenance.json"
      provides: "Build provenance metadata for release traceability"
      contains: "\"git_sha\""
  key_links:
    - from: "MVP_PROD_CHECKLIST.md"
      to: ".github/workflows/security-regression-tests.yml"
      via: "Required security gate alignment"
      pattern: "security-regression-tests.yml"
    - from: "MVP_PROD_CHECKLIST.md"
      to: "scripts/backup/ci-smoke.sh"
      via: "Backup/restore required command set"
      pattern: "backup/ci-smoke.sh"
    - from: "MVP_PROD_CHECKLIST.md"
      to: "scripts/release/sbom.sh"
      via: "Required SBOM/provenance generation"
      pattern: "release/sbom.sh"
    - from: "MVP_PROD_CONTROL_ROOM.md"
      to: "scripts/release/mvp_control_room.sh"
      via: "Operator fast-path execution"
      pattern: "mvp_control_room.sh"
---

<objective>
Close Phase 6 by validating production operations gates end-to-end: security regression readiness, backup/restore recovery confidence, release provenance artifact generation, checklist evidence completion, and deployment runbook rehearsal.

Purpose: converts existing operations assets into verifiable launch readiness evidence without introducing parallel operational paths.
Output: checklist-backed evidence proving OPS-01..OPS-05 are satisfied or explicitly blocked with owner/action.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-production-operations/06-RESEARCH.md
@MVP_PROD_CHECKLIST.md
@MVP_PROD_CONTROL_ROOM.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate security regression gate (OPS-01)</name>
  <files>
    .github/workflows/security-regression-tests.yml
    tests/security_regression_suite.rs
    tests/security_regression_tests.rs
  </files>
  <action>
    Execute and validate the security gate command set aligned with CI:

    ```bash
    cargo test --test security_regression_suite -- --nocapture --test-threads=1
    cargo test -p adapteros-crypto --test crypto_operations_tests -- --nocapture
    cargo clippy --workspace -- -D unsafe_code -W clippy::all
    cargo clippy -p adapteros-crypto -- -D clippy::panic -W clippy::all
    ```

    If failures occur, apply minimal fixes only where required to restore gate integrity. Do not broaden scope into non-security refactors.
  </action>
  <verify>
    <automated>cargo test --test security_regression_suite -- --test-threads=1 && cargo test -p adapteros-crypto --test crypto_operations_tests</automated>
    <manual>Confirm security gate commands match workflow intent and produce pass/fail evidence</manual>
  </verify>
  <done>
    - Security regression suite passes or blockers are explicitly documented with owner and expiry
    - Crypto operation tests pass
    - Security clippy checks complete without blocking violations
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove backup and restore recovery path (OPS-02)</name>
  <files>
    scripts/backup/backup.sh
    scripts/backup/verify-backups.sh
    scripts/backup/test-restore.sh
    scripts/backup/ci-smoke.sh
    deploy/cron/adapteros-backup
    scripts/backup/cron.example
  </files>
  <action>
    Run the canonical backup pipeline and recovery validation:

    ```bash
    bash scripts/backup/backup.sh
    bash scripts/backup/verify-backups.sh
    bash scripts/backup/test-restore.sh
    bash scripts/backup/ci-smoke.sh
    ```

    Validate that backup retention/configuration assets remain aligned with operational expectations (`deploy/cron/adapteros-backup`, `scripts/backup/cron.example`).
  </action>
  <verify>
    <automated>bash scripts/backup/ci-smoke.sh</automated>
    <manual>Confirm restored target passes embedded health/data checks and evidence is retained</manual>
  </verify>
  <done>
    - Backup artifacts are created and verified
    - Restore succeeds into clean target path
    - CI smoke for backup pipeline passes
    - Retention/cron assets are present and reviewable
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate release SBOM and provenance bundle (OPS-03)</name>
  <files>
    scripts/release/sbom.sh
    target/release-bundle/
  </files>
  <action>
    Produce release artifacts and provenance bundle using the canonical script:

    ```bash
    cargo build --release
    bash scripts/release/sbom.sh
    ```

    Verify `target/release-bundle/sbom.json` and `target/release-bundle/build_provenance.json` exist and include expected metadata fields. If signing key is configured, verify signature artifacts are generated as well.
  </action>
  <verify>
    <automated>cargo build --release && bash scripts/release/sbom.sh && test -f target/release-bundle/sbom.json && test -f target/release-bundle/build_provenance.json</automated>
    <manual>Confirm artifacts are attached/recorded as release evidence for checklist closure</manual>
  </verify>
  <done>
    - Release build succeeds
    - SBOM and provenance artifacts are generated
    - Artifact metadata is traceable to release commit/build
  </done>
</task>

<task type="auto">
  <name>Task 4: Close checklist gates and validate deployment runbook (OPS-04, OPS-05)</name>
  <files>
    MVP_PROD_CHECKLIST.md
    MVP_PROD_CONTROL_ROOM.md
    scripts/release/mvp_control_room.sh
    scripts/verify-deployment.sh
    docs/runbooks/README.md
  </files>
  <action>
    Execute staged runbook rehearsal and evidence logging:

    ```bash
    bash scripts/release/mvp_control_room.sh --skip-deploy --base-url http://localhost:8080
    bash scripts/verify-deployment.sh
    ```

    Then reconcile `MVP_PROD_CHECKLIST.md` REQUIRED items for Phase 6 scope and attach evidence paths (logs/artifacts/command output). For unresolved REQUIRED items, record explicit blockers with owner + next action.
  </action>
  <verify>
    <automated>bash scripts/release/mvp_control_room.sh --skip-deploy --base-url http://localhost:8080</automated>
    <manual>Confirm control-room run produces timestamped evidence log and checklist status is unambiguous</manual>
  </verify>
  <done>
    - Deployment runbook rehearsal completes without manual intervention failures in rehearsed path
    - Evidence log and summary are generated under `var/release-control-room/`
    - Phase 6 REQUIRED checklist gates are marked complete or blocked with explicit ownership
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Go/No-Go review on production operations evidence</name>
  <files></files>
  <action>
    Present consolidated evidence for OPS-01..OPS-05:
    - Security gate outcomes
    - Backup/restore outcomes
    - SBOM/provenance artifact outputs
    - Checklist completion state
    - Runbook rehearsal logs

    Request explicit `GO` or `NO-GO` decision with any required follow-up actions.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human go/no-go on Phase 6 evidence"</automated>
    <manual>Human reviewer validates evidence sufficiency and decides GO/NO-GO</manual>
  </verify>
  <done>
    - Human review decision recorded
    - Follow-up actions (if any) are assigned and tracked
  </done>
</task>

</tasks>

<verification>
1. Security regression command set passes or has explicit, approved exceptions.
2. `bash scripts/backup/ci-smoke.sh` passes.
3. `cargo build --release` and `bash scripts/release/sbom.sh` produce expected artifacts.
4. Control-room rehearsal generates `var/release-control-room/<timestamp>/evidence.log`.
5. `MVP_PROD_CHECKLIST.md` REQUIRED items for Phase 6 are complete or explicitly blocked with owners.
</verification>

<success_criteria>
- OPS-01 through OPS-05 are demonstrably satisfied through script-backed evidence
- No parallel operational process was introduced
- Remaining blockers (if any) are explicit, owned, and actionable
</success_criteria>

<output>
After completion, create `.planning/phases/06-production-operations/06-01-SUMMARY.md`
</output>
