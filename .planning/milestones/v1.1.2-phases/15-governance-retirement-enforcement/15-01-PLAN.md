---
phase: 15-governance-retirement-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [GOV-09, GOV-11, AUTO-01]
files_modified:
  - var/evidence/governance-retirement-<UTCSTAMP>/preflight-before.log
  - var/evidence/governance-retirement-<UTCSTAMP>/pre-read.json
  - var/evidence/governance-retirement-<UTCSTAMP>/existing-contexts.txt
  - var/evidence/governance-retirement-<UTCSTAMP>/checklist-contexts.txt
  - var/evidence/governance-retirement-<UTCSTAMP>/gate-state.txt
autonomous: true
user_setup:
  - GitHub CLI auth for repository-admin capable identity is already configured.
must_haves:
  truths:
    - Capability is evaluated before any policy write.
    - Baseline required-check state is captured for preserve+add verification.
  artifacts:
    - path: var/evidence/governance-retirement-<UTCSTAMP>/preflight-before.log
      provides: Baseline capability status and exit code.
      contains: status=
    - path: var/evidence/governance-retirement-<UTCSTAMP>/pre-read.json
      provides: Baseline required_status_checks payload.
      contains: contexts
    - path: var/evidence/governance-retirement-<UTCSTAMP>/existing-contexts.txt
      provides: Sorted baseline contexts.
      contains: FFI AddressSanitizer
  key_links:
    - from: scripts/ci/check_governance_preflight.sh
      to: var/evidence/governance-retirement-<UTCSTAMP>/preflight-before.log
      via: preflight command output redirect
---

# Phase 15 Plan 01: Capability Gate and Immutable Baseline Evidence

## Objective
- **What:** Revalidate governance capability and capture immutable baseline artifacts.
- **Why:** Any branch-protection write without capability proof or baseline snapshot risks false closure and unsafe drift.
- **Output:** Timestamped evidence bundle with preflight result, baseline required-check state, and normalized context inputs.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-governance-retirement-enforcement/15-CONTEXT.md
@.planning/phases/15-governance-retirement-enforcement/15-RESEARCH.md

## Tasks

<task type="code">
  <name>Create immutable evidence workspace and canonical checklist inputs</name>
  <files>var/evidence/governance-retirement-<UTCSTAMP>/*</files>
  <action>Create a new UTC-stamped evidence directory, write checklist contexts (`CI`, `Stability Gate`, `Integration Tests`, `Cross-Hardware Determinism`, `Migration Testing`, `Security Regression Tests`, `Check Merge Conflicts`, `FFI AddressSanitizer (push)`), and record execution metadata.</action>
  <verify>Ensure directory is new (no overwrite) and checklist-contexts.txt has exactly eight unique lines.</verify>
  <done>Evidence directory exists and checklist input file is deterministic.</done>
</task>

<task type="code">
  <name>Run preflight hard gate before any policy write</name>
  <files>scripts/ci/check_governance_preflight.sh, var/evidence/governance-retirement-<UTCSTAMP>/preflight-before.log</files>
  <action>Run `check_governance_preflight.sh` for `rogu3bear/adapter-os:main` with required context `FFI AddressSanitizer (push)`. Capture stdout/stderr and exit code into evidence files, then derive gate-state (`blocked_external` or `capable`).</action>
  <verify>Log contains explicit `status=` line and gate-state is consistent with process exit code.</verify>
  <done>Hard-gate result is persisted and available for downstream branching.</done>
</task>

<task type="code">
  <name>Capture baseline required-status-checks (capable path only)</name>
  <files>var/evidence/governance-retirement-<UTCSTAMP>/pre-read.json, var/evidence/governance-retirement-<UTCSTAMP>/existing-contexts.txt</files>
  <action>If gate-state is `capable`, call `gh api repos/rogu3bear/adapter-os/branches/main/protection/required_status_checks` and store payload as `pre-read.json`; extract and sort contexts into `existing-contexts.txt`. If gate-state is `blocked_external`, skip API write preparation and mark phase path as evidence-only.</action>
  <verify>`pre-read.json` and `existing-contexts.txt` exist for capable path; blocked path has explicit skip marker and no PATCH attempt files.</verify>
  <done>Baseline state is captured or safely deferred according to gate outcome.</done>
</task>

## Verification
- Confirm gate files exist and reflect one of two legal outcomes: `capable` or `blocked_external`.
- Confirm capable path includes baseline context extraction artifacts.
- Confirm blocked path includes no write-attempt evidence.

## Success Criteria
- [ ] Preflight run is captured with deterministic artifacts before any policy mutation.
- [ ] Baseline required-check state is captured for capable path and safely skipped for blocked path.
- [ ] Inputs needed by Plan 02 (`pre-read.json`, `existing-contexts.txt`, `checklist-contexts.txt`) are available when gate is open.
