---
phase: 15-governance-retirement-enforcement
plan: 02
type: execute
wave: 2
depends_on: [15-01-PLAN.md]
requirements: [GOV-10, GOV-11, GOV-12]
files_modified:
  - var/evidence/governance-retirement-<UTCSTAMP>/final-contexts.txt
  - var/evidence/governance-retirement-<UTCSTAMP>/write.json
  - var/evidence/governance-retirement-<UTCSTAMP>/post-read.json
  - var/evidence/governance-retirement-<UTCSTAMP>/preflight-after.log
  - var/evidence/governance-retirement-<UTCSTAMP>/verification.txt
  - var/evidence/governance-retirement-<UTCSTAMP>/rollback.json
autonomous: true
user_setup:
  - Plan 01 capable path artifacts are present in the same evidence directory.
must_haves:
  truths:
    - Required checks are applied with strict=true using preserve+add union semantics.
    - Post-write validation confirms inclusion of all canonical contexts and retention of prior contexts.
    - Rollback path exists and is executable from baseline artifacts.
  artifacts:
    - path: var/evidence/governance-retirement-<UTCSTAMP>/write.json
      provides: PATCH response for required_status_checks update.
      contains: strict
    - path: var/evidence/governance-retirement-<UTCSTAMP>/post-read.json
      provides: Authoritative post-write required-check state.
      contains: contexts
    - path: var/evidence/governance-retirement-<UTCSTAMP>/verification.txt
      provides: Pass fail matrix for context inclusion and preserve checks.
      contains: FFI AddressSanitizer
  key_links:
    - from: var/evidence/governance-retirement-<UTCSTAMP>/pre-read.json
      to: var/evidence/governance-retirement-<UTCSTAMP>/post-read.json
      via: preserve+add diff check
    - from: var/evidence/governance-retirement-<UTCSTAMP>/post-read.json
      to: var/evidence/governance-retirement-<UTCSTAMP>/verification.txt
      via: context verification table generation
---

# Phase 15 Plan 02: Enforce Required Checks and Verify Rollback Safety

## Objective
- **What:** Apply branch-protection required status checks with additive context union and strict mode, then verify enforcement and safety.
- **Why:** Governance debt is only retired when write-path enforcement is proven with preserved pre-existing controls.
- **Output:** Read/write/read proof package, postflight capability evidence, and rollback receipt when needed.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-governance-retirement-enforcement/15-CONTEXT.md
@.planning/phases/15-governance-retirement-enforcement/15-RESEARCH.md
@.planning/phases/15-governance-retirement-enforcement/15-01-PLAN.md

## Tasks

<task type="code">
  <name>Build final required-context union set</name>
  <files>var/evidence/governance-retirement-<UTCSTAMP>/existing-contexts.txt, var/evidence/governance-retirement-<UTCSTAMP>/checklist-contexts.txt, var/evidence/governance-retirement-<UTCSTAMP>/final-contexts.txt</files>
  <action>From Plan 01 capable artifacts, compute `final-contexts.txt = union(existing-contexts, checklist-contexts)` with sort-unique determinism.</action>
  <verify>final-contexts.txt contains all eight checklist contexts and all pre-existing contexts.</verify>
  <done>Union context set is deterministic and complete.</done>
</task>

<task type="code">
  <name>PATCH required_status_checks with strict=true and final contexts</name>
  <files>var/evidence/governance-retirement-<UTCSTAMP>/write.json, var/evidence/governance-retirement-<UTCSTAMP>/post-read.json</files>
  <action>Apply `gh api` PATCH to `repos/rogu3bear/adapter-os/branches/main/protection/required_status_checks` with `strict=true` and full `contexts[]` from final-contexts. Save write response and immediate readback.</action>
  <verify>write.json indicates accepted update; post-read.json shows strict=true and required contexts array populated.</verify>
  <done>Policy write and immediate post-read are archived.</done>
</task>

<task type="code">
  <name>Run postflight and generate verification matrix</name>
  <files>scripts/ci/check_governance_preflight.sh, var/evidence/governance-retirement-<UTCSTAMP>/preflight-after.log, var/evidence/governance-retirement-<UTCSTAMP>/verification.txt</files>
  <action>Run governance preflight again, parse post-read and pre-read into a pass/fail matrix: `status=capable`, canonical contexts present, and baseline contexts preserved. Store in verification.txt.</action>
  <verify>verification.txt has explicit pass/fail rows for all eight canonical contexts plus preservation checks.</verify>
  <done>Enforcement proof is explicit and machine-checkable.</done>
</task>

<task type="code">
  <name>Apply rollback guard if verification fails</name>
  <files>var/evidence/governance-retirement-<UTCSTAMP>/rollback.json</files>
  <action>If any verification assertion fails, PATCH branch protection back to exact pre-read strict/contexts values and store rollback response.</action>
  <verify>On rollback path, rollback.json exists and post-rollback read confirms baseline restoration.</verify>
  <done>Misconfiguration has deterministic recovery evidence.</done>
</task>

## Verification
- `preflight-after.log` must include `status=capable` for success path.
- `post-read.json` must include full canonical context set and strict mode.
- Pre vs post context diff must show no baseline context removals.
- Rollback branch must be fully evidenced if failure path is triggered.

## Success Criteria
- [ ] Write path proves strict enforcement with preserve+add semantics.
- [ ] Canonical eight contexts are required on `main`.
- [ ] Verification report clearly states pass/fail per context and preservation checks.
- [ ] Rollback path is recorded and executable when needed.
