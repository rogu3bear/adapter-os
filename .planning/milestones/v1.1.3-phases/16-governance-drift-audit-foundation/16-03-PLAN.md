---
phase: 16-governance-drift-audit-foundation
plan: 03
type: execute
wave: 3
depends_on: [16-02-PLAN.md]
requirements: [GOV-13, AUTO-01]
files_modified:
  - .github/workflows/governance-drift-audit.yml
  - docs/governance/README.md
  - MVP_PROD_CHECKLIST.md
  - var/evidence/governance-drift-<UTCSTAMP>/ci-check.log
autonomous: true
user_setup:
  - Plan 02 drift audit runner is available and produces deterministic report outputs.
must_haves:
  truths:
    - Governance drift audit is runnable as check-only CI.
    - Operator response guidance is explicit for each outcome class.
    - No documentation claims parity closure before Phase 17 evidence.
  artifacts:
    - path: var/evidence/governance-drift-<UTCSTAMP>/ci-check.log
      provides: CI-equivalent check-only execution transcript.
      contains: governance-drift
    - path: docs/governance/README.md
      provides: Outcome-to-response runbook and command usage.
      contains: approved_exception
  key_links:
    - from: .github/workflows/governance-drift-audit.yml
      to: scripts/ci/audit_governance_drift.sh
      via: check-only workflow step invocation
---

# Phase 16 Plan 03: CI Wiring and Operator Runbook

## Objective
- **What:** Wire governance drift audit into check-only execution paths and document operator response procedures.
- **Why:** Drift detection only reduces risk when it is scheduled/observable and actionable by operators.
- **Output:** CI workflow integration, runbook updates, and dry-run evidence.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-governance-drift-audit-foundation/16-CONTEXT.md
@.planning/phases/16-governance-drift-audit-foundation/16-RESEARCH.md
@.planning/phases/16-governance-drift-audit-foundation/16-02-PLAN.md

## Tasks

<task type="code">
  <name>Add check-only governance drift CI workflow</name>
  <files>.github/workflows/governance-drift-audit.yml</files>
  <action>Introduce CI workflow/job that runs manifest validation and drift audit in read-only mode, persisting report artifacts without write/remediation actions.</action>
  <verify>Workflow can be executed in CI/lint context and fails on drift without mutating branch-protection state.</verify>
  <done>Scheduled and on-demand check-only audit path exists.</done>
</task>

<task type="code">
  <name>Document operator response matrix and escalation path</name>
  <files>docs/governance/README.md, MVP_PROD_CHECKLIST.md</files>
  <action>Document required responses for `compliant`, `drifted`, `blocked_external`, and `approved_exception` outcomes, including escalation/ownership expectations.</action>
  <verify>Runbook references exact command/report artifacts and explicitly preserves external-blocker truth semantics.</verify>
  <done>Operators can triage drift outcomes consistently.</done>
</task>

<task type="code">
  <name>Capture CI-equivalent dry-run evidence</name>
  <files>var/evidence/governance-drift-<UTCSTAMP>/ci-check.log</files>
  <action>Run workflow-equivalent command sequence locally and capture transcript showing manifest validation + drift audit reports.</action>
  <verify>Log includes command sequence, outcome classes, and generated report paths.</verify>
  <done>Phase 16 readiness evidence exists for verification/UAT.</done>
</task>

## Verification
- Run CI-equivalent command sequence locally with read-only assertions.
- Confirm docs include deterministic operator actions per outcome class.
- Confirm no parity closure wording is introduced before Phase 17.

## Success Criteria
- [ ] Check-only CI execution path is wired and reproducible.
- [ ] Operator runbook clearly maps each outcome class to response actions.
- [ ] Dry-run transcript exists and references generated report artifacts.
