---
phase: 16-governance-drift-audit-foundation
plan: 02
type: execute
wave: 2
depends_on: [16-01-PLAN.md]
requirements: [GOV-13]
files_modified:
  - scripts/ci/audit_governance_drift.sh
  - var/evidence/governance-drift-<UTCSTAMP>/audit.log
  - var/evidence/governance-drift-<UTCSTAMP>/report.json
  - var/evidence/governance-drift-<UTCSTAMP>/report.txt
autonomous: true
user_setup:
  - Plan 01 manifest and validator outputs are available.
must_haves:
  truths:
    - Drift audit is strictly read-only across all targets.
    - Output classification is deterministic per target state.
    - Reports are emitted in machine and human forms.
  artifacts:
    - path: var/evidence/governance-drift-<UTCSTAMP>/report.json
      provides: Structured per-target drift outcomes.
      contains: compliant
    - path: var/evidence/governance-drift-<UTCSTAMP>/report.txt
      provides: Human-readable audit summary and action hints.
      contains: blocked_external
  key_links:
    - from: docs/governance/target-manifest.json
      to: var/evidence/governance-drift-<UTCSTAMP>/report.json
      via: manifest-driven target evaluation
---

# Phase 16 Plan 02: Read-Only Drift Audit Runner and Reports

## Objective
- **What:** Implement deterministic read-only governance drift audit execution and reporting.
- **Why:** Operator trust depends on repeatable detection output that cannot mutate policy state.
- **Output:** Audit command plus reproducible JSON and text reports per run.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-governance-drift-audit-foundation/16-CONTEXT.md
@.planning/phases/16-governance-drift-audit-foundation/16-RESEARCH.md
@.planning/phases/16-governance-drift-audit-foundation/16-01-PLAN.md

## Tasks

<task type="code">
  <name>Implement read-only governance drift audit command</name>
  <files>scripts/ci/audit_governance_drift.sh</files>
  <action>Build a manifest-driven audit script that performs only read operations (`gh api` GET + preflight checks) per target and captures explicit status/error classes.</action>
  <verify>Script contains no write/PATCH surfaces and exits deterministically for compliant, drifted, and blocked targets.</verify>
  <done>Drift audit command is safe for CI check-only execution.</done>
</task>

<task type="code">
  <name>Emit deterministic JSON + human summary reports</name>
  <files>var/evidence/governance-drift-<UTCSTAMP>/report.json, var/evidence/governance-drift-<UTCSTAMP>/report.txt</files>
  <action>Generate report outputs with stable ordering and outcome classes (`compliant`, `drifted`, `blocked_external`, `approved_exception`) plus expected/observed context sets.</action>
  <verify>Report schema validates and repeated runs on unchanged input produce semantically identical results.</verify>
  <done>Outputs are machine-checkable and operator-readable.</done>
</task>

<task type="code">
  <name>Capture complete audit transcript</name>
  <files>var/evidence/governance-drift-<UTCSTAMP>/audit.log</files>
  <action>Capture full command transcript, manifest reference, and environment metadata for reproducibility and post-run review.</action>
  <verify>Audit log links manifest path, report paths, and final status summary.</verify>
  <done>Evidence bundle is complete for downstream CI/docs integration.</done>
</task>

## Verification
- Confirm script never issues write requests.
- Validate report JSON schema and deterministic target ordering.
- Re-run against same manifest snapshot and compare outcome equivalence.

## Success Criteria
- [ ] Drift audit command is read-only and deterministic.
- [ ] Report outputs clearly classify each target outcome.
- [ ] Evidence bundle includes audit log + JSON + human report for one run.
