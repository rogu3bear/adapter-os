---
phase: 17-multi-repo-parity-proof
plan: 02
type: execute
wave: 2
depends_on: [17-01-PLAN.md]
requirements: [OPS-09]
files_modified:
  - var/evidence/governance-parity-<UTCSTAMP>/audit.log
  - var/evidence/governance-parity-<UTCSTAMP>/report.json
  - var/evidence/governance-parity-<UTCSTAMP>/report.txt
  - var/evidence/governance-parity-<UTCSTAMP>/parity-matrix.txt
  - var/evidence/governance-parity-<UTCSTAMP>/approved-exceptions.txt
autonomous: true
user_setup:
  - Expanded manifest target set is present and validator has passed.
must_haves:
  truths:
    - Multi-repo parity outcomes are explicitly evidenced per target.
    - Approved exceptions and blocker classes are not implicit.
  artifacts:
    - path: var/evidence/governance-parity-<UTCSTAMP>/report.json
      provides: Structured parity outcome report for approved targets.
      contains: results
    - path: var/evidence/governance-parity-<UTCSTAMP>/parity-matrix.txt
      provides: Human parity matrix with outcome counts and per-target statuses.
      contains: outcome
  key_links:
    - from: docs/governance/target-manifest.json
      to: var/evidence/governance-parity-<UTCSTAMP>/report.json
      via: audit_governance_drift.sh manifest-driven parity run
---

# Phase 17 Plan 02: Run Parity Verification and Exception Handling

## Objective
- **What:** Execute parity verification across approved targets and generate explicit exception evidence.
- **Why:** OPS-09 requires parity proof that distinguishes compliant targets from approved exception/blocker states.
- **Output:** Multi-target parity report package with explicit approved-exception receipts.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-multi-repo-parity-proof/17-CONTEXT.md
@.planning/phases/17-multi-repo-parity-proof/17-RESEARCH.md
@.planning/phases/17-multi-repo-parity-proof/17-01-PLAN.md

## Tasks

<task type="code">
  <name>Execute read-only parity audit on approved target set</name>
  <files>scripts/ci/audit_governance_drift.sh, var/evidence/governance-parity-<UTCSTAMP>/report.json, var/evidence/governance-parity-<UTCSTAMP>/report.txt</files>
  <action>Run the drift auditor against expanded manifest target set with fail-on drifted behavior, writing all outputs to parity evidence directory.</action>
  <verify>Audit exits 0 with no unapproved drifted outcomes and emits report artifacts.</verify>
  <done>Parity report package exists and is reproducible.</done>
</task>

<task type="code">
  <name>Generate parity matrix summary</name>
  <files>var/evidence/governance-parity-<UTCSTAMP>/parity-matrix.txt</files>
  <action>Derive a deterministic matrix from report JSON listing each target outcome, raw outcome, endpoint status, and missing-context count.</action>
  <verify>Matrix row count equals manifest target count and ordering is deterministic by target id.</verify>
  <done>Operator-readable parity matrix is available.</done>
</task>

<task type="code">
  <name>Capture approved-exceptions receipt</name>
  <files>var/evidence/governance-parity-<UTCSTAMP>/approved-exceptions.txt</files>
  <action>Extract approved_exception targets and rationale into a dedicated receipt for closure and audit review.</action>
  <verify>Each approved exception in report JSON appears with target id and reason in receipt.</verify>
  <done>Exception handling is explicit and traceable.</done>
</task>

## Verification
- Audit returns success for fail-on drifted policy.
- Matrix and exception receipts match report JSON deterministically.
- Evidence package includes all report artifacts.

## Success Criteria
- [ ] Multi-repo parity outcomes are captured for approved targets.
- [ ] Approved exceptions are explicitly documented with rationale.
- [ ] No hidden drift outcomes remain in parity report.
