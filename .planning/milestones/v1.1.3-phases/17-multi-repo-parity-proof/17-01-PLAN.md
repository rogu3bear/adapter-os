---
phase: 17-multi-repo-parity-proof
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [OPS-09]
files_modified:
  - docs/governance/target-manifest.json
  - var/evidence/governance-parity-<UTCSTAMP>/target-set.txt
  - var/evidence/governance-parity-<UTCSTAMP>/manifest-validation.txt
autonomous: true
user_setup:
  - GitHub CLI auth is available for repository metadata and branch-protection read probes.
must_haves:
  truths:
    - Approved parity target set is explicit and reproducible.
    - Expanded target set passes deterministic manifest validation.
  artifacts:
    - path: docs/governance/target-manifest.json
      provides: Approved multi-repo parity target set.
      contains: targets
    - path: var/evidence/governance-parity-<UTCSTAMP>/target-set.txt
      provides: Immutable captured target list for this parity run.
      contains: rogu3bear/
  key_links:
    - from: docs/governance/target-manifest.json
      to: var/evidence/governance-parity-<UTCSTAMP>/manifest-validation.txt
      via: validate_governance_target_manifest.sh
---

# Phase 17 Plan 01: Approve Multi-Repo Target Set and Baseline Capture

## Objective
- **What:** Expand and approve the parity target set in manifest and capture baseline inventory evidence.
- **Why:** Multi-repo parity proof requires explicit, immutable target scope.
- **Output:** Approved manifest target set with validation and target-set receipt artifacts.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-multi-repo-parity-proof/17-CONTEXT.md
@.planning/phases/17-multi-repo-parity-proof/17-RESEARCH.md

## Tasks

<task type="code">
  <name>Approve and persist multi-repo parity target set</name>
  <files>docs/governance/target-manifest.json</files>
  <action>Expand canonical manifest from single-target to approved multi-repo target inventory with explicit per-target exception policy metadata.</action>
  <verify>Manifest contains at least three repo targets with unique id and repo/branch pairs.</verify>
  <done>Approved parity target set is versioned and explicit.</done>
</task>

<task type="code">
  <name>Validate expanded target manifest deterministically</name>
  <files>scripts/ci/validate_governance_target_manifest.sh, var/evidence/governance-parity-<UTCSTAMP>/manifest-validation.txt</files>
  <action>Run manifest validator against expanded target set and persist deterministic status output under parity evidence directory.</action>
  <verify>Validator exits 0 and includes target_count + digest in output transcript.</verify>
  <done>Parity target set is schema-valid and reproducible.</done>
</task>

<task type="code">
  <name>Capture immutable target set receipt</name>
  <files>var/evidence/governance-parity-<UTCSTAMP>/target-set.txt</files>
  <action>Write sorted repo/branch target inventory receipt from manifest into parity evidence directory for audit traceability.</action>
  <verify>Receipt lines match manifest targets exactly and deterministically sorted by id.</verify>
  <done>Baseline target-set evidence is locked for downstream parity verification.</done>
</task>

## Verification
- Manifest validates successfully with deterministic output.
- Target-set receipt exists and matches manifest contents.
- Evidence path is UTC-stamped and immutable.

## Success Criteria
- [ ] Approved parity target set is explicit in manifest.
- [ ] Validation transcript is captured under parity evidence.
- [ ] Target-set receipt exists for this phase run.
