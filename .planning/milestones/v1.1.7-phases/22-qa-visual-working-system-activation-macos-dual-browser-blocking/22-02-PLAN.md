---
phase: 22-qa-visual-working-system-activation-macos-dual-browser-blocking
plan: 02
type: execute
wave: 1
depends_on: [22-01]
requirements: [QA-22-03]
files_modified:
  - tests/playwright/ui/visual.spec.ts
  - tests/playwright/ui/visual.spec.ts-snapshots/*
  - tests/playwright/scripts/check-visual-snapshot-contract.mjs
  - tests/playwright/package.json
autonomous: true
user_setup:
  - Baseline regeneration (if needed) must run on macOS to preserve canonical `darwin` snapshots.
must_haves:
  truths:
    - Every active visual assertion has both Chromium and WebKit canonical baseline files.
    - Any unreferenced snapshot file fails contract validation.
  artifacts:
    - path: tests/playwright/scripts/check-visual-snapshot-contract.mjs
      provides: Deterministic missing/orphan baseline guard.
      contains: "Orphan visual baselines"
    - path: tests/playwright/ui/visual.spec.ts-snapshots/
      provides: Canonical macOS baseline images.
      contains: "*-darwin.png"
  key_links:
    - from: tests/playwright/scripts/check-visual-snapshot-contract.mjs
      to: tests/playwright/package.json
      via: quality gate precheck hook
---

# Phase 22 Plan 02: Rectify Snapshot/Baseline Drift

## Objective
- **What:** Remove snapshot drift/orphans and enforce deterministic baseline contract checks.
- **Why:** Visual gates are only truthful when baseline set and assertions match exactly.
- **Output:** Canonical `darwin` baseline set with automated missing/orphan enforcement in the quality lane.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-qa-visual-working-system-activation-macos-dual-browser-blocking/22-CONTEXT.md
@tests/playwright/ui/visual.spec.ts
@tests/playwright/ui/visual.spec.ts-snapshots
@tests/playwright/scripts/check-visual-snapshot-contract.mjs
@tests/playwright/package.json

## Tasks

<task type="code">
  <name>Validate visual reference-to-baseline contract and identify drift</name>
  <files>tests/playwright/ui/visual.spec.ts, tests/playwright/ui/visual.spec.ts-snapshots/*</files>
  <action>Run the snapshot contract script to detect missing active baselines and orphaned files before any visual execution.</action>
  <verify>
    <automated>cd tests/playwright && node scripts/check-visual-snapshot-contract.mjs</automated>
    <manual>Inspect script output counts and ensure canonical suffix is `-darwin.png`.</manual>
    <sampling_rate>run before and after any snapshot edits</sampling_rate>
  </verify>
  <done>Contract report returns OK with zero missing and zero orphan baselines.</done>
</task>

<task type="code">
  <name>Rectify orphaned or stale baselines</name>
  <files>tests/playwright/ui/visual.spec.ts-snapshots/*</files>
  <action>Remove files with no matching `toHaveScreenshot(...)` references; if active baselines are stale/missing, regenerate canonical baselines on macOS for Chromium+WebKit using update-snapshots mode.</action>
  <verify>
    <automated>cd tests/playwright && npm run test:gate:quality -- --project=chromium --update-snapshots && npm run test:gate:quality -- --project=webkit --update-snapshots</automated>
    <manual>Only accept regenerated `*-darwin.png` files that map to active assertions.</manual>
    <sampling_rate>only when drift is detected</sampling_rate>
  </verify>
  <done>Snapshot directory contains only canonical referenced baselines.</done>
</task>

<task type="code">
  <name>Ensure contract check is wired before visual execution in quality gate</name>
  <files>tests/playwright/package.json</files>
  <action>Ensure `test:gate:quality` runs `check-visual-snapshot-contract.mjs` before invoking bundled specs.</action>
  <verify>
    <automated>rg -n 'test:gate:quality.*check-visual-snapshot-contract' tests/playwright/package.json</automated>
    <manual>Confirm gate exits non-zero on contract failure prior to spec execution.</manual>
    <sampling_rate>once after script wiring validation</sampling_rate>
  </verify>
  <done>Quality lane is contract-guarded before visual assertions run.</done>
</task>

## Verification
- Snapshot contract script must return success.
- Snapshot directory must contain no orphans.
- Gate script must run contract check before bundled specs.

## Success Criteria
- [ ] Missing active baselines are zero.
- [ ] Orphan baseline files are zero.
- [ ] Contract checker is enforced by `test:gate:quality` prior to visual suite execution.
