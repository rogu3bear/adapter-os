---
phase: 22-qa-visual-working-system-activation-macos-dual-browser-blocking
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [QA-22-01, QA-22-02, AUTO-04]
files_modified:
  - .github/workflows/ci.yml
  - tests/playwright/package.json
  - tests/playwright/README.md
autonomous: true
user_setup:
  - macOS runner policy remains `macos-14` for visual blocking jobs.
must_haves:
  truths:
    - Tier-2 UI quality gate is explicitly dual-browser blocking on macOS.
    - CI and local commands use the same bundled quality lane selector contract.
  artifacts:
    - path: .github/workflows/ci.yml
      provides: Tier-2 dual-browser blocking job definitions.
      contains: "npm run test:gate:quality -- --project=chromium"
    - path: .github/workflows/ci.yml
      provides: Tier-2 dual-browser blocking job definitions.
      contains: "npm run test:gate:quality -- --project=webkit"
    - path: tests/playwright/package.json
      provides: Canonical local+CI command selectors.
      contains: "test:gate:quality"
  key_links:
    - from: tests/playwright/package.json
      to: .github/workflows/ci.yml
      via: shared bundled quality gate command
---

# Phase 22 Plan 01: Anchor CI/Test Selection Contract

## Objective
- **What:** Lock Tier-2 UI gate and Playwright selectors to explicit bundled quality commands.
- **Why:** Remove stale/implicit selector behavior and guarantee deterministic test selection in CI and local runs.
- **Output:** CI + package contract surfaces verified/aligned with dual-browser blocking policy.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-qa-visual-working-system-activation-macos-dual-browser-blocking/22-CONTEXT.md
@.planning/phases/22-qa-visual-working-system-activation-macos-dual-browser-blocking/22-RESEARCH.md
@.github/workflows/ci.yml
@tests/playwright/package.json

## Tasks

<task type="code">
  <name>Verify Tier-2 dual-browser macOS blocking jobs use explicit bundled lane command</name>
  <files>.github/workflows/ci.yml</files>
  <action>Ensure Chromium and WebKit Tier-2 jobs run on `macos-14` and each invokes `npm run test:gate:quality -- --project=<browser>` with no grep-based selector path.</action>
  <verify>
    <automated>rg -n 'playwright-ui-quality-gate-(chromium|webkit)|runs-on: macos-14|npm run test:gate:quality -- --project=' .github/workflows/ci.yml</automated>
    <manual>Confirm these jobs are blocking (no `continue-on-error`) while concurrency smoke remains advisory.</manual>
    <sampling_rate>once after inspection/edit</sampling_rate>
  </verify>
  <done>Tier-2 dual-browser blocking contract is explicit and deterministic.</done>
</task>

<task type="code">
  <name>Anchor Playwright command contract in package scripts</name>
  <files>tests/playwright/package.json</files>
  <action>Ensure `test:audit` and `test:gate:quality` scripts are present and `test:gate:quality` bundles `console.regression`, `routes.best_practices.audit`, `visual`, `runs`, and `repositories` specs.</action>
  <verify>
    <automated>rg -n '"test:audit"|"test:gate:quality"|console\.regression|routes\.best_practices\.audit|ui/visual\.spec\.ts|ui/runs\.spec\.ts|ui/repositories\.spec\.ts' tests/playwright/package.json</automated>
    <manual>Confirm command contract is compatible with `--project=chromium|webkit` overrides from CI.</manual>
    <sampling_rate>once after inspection/edit</sampling_rate>
  </verify>
  <done>Local and CI use the same command contract for quality gating.</done>
</task>

<task type="code">
  <name>Document canonical blocking lane usage in Playwright README</name>
  <files>tests/playwright/README.md</files>
  <action>Ensure README includes the canonical dual-browser gate commands and clearly calls out bundled lane contents and blocking usage.</action>
  <verify>
    <automated>rg -n 'Blocking UI Quality Gate|test:gate:quality -- --project=chromium|test:gate:quality -- --project=webkit' tests/playwright/README.md</automated>
    <manual>Confirm wording matches CI contract to avoid local-vs-CI divergence.</manual>
    <sampling_rate>once after inspection/edit</sampling_rate>
  </verify>
  <done>Command usage is operator-clear and contract-accurate.</done>
</task>

## Verification
- CI and package contract surfaces are grep-verifiable.
- Dual-browser lane selectors are explicit and deterministic.
- No stale style-audit grep gate path remains in Tier-2 quality jobs.

## Success Criteria
- [ ] `macos-14` Chromium and WebKit blocking jobs both execute `test:gate:quality`.
- [ ] `test:audit` and bundled `test:gate:quality` command contracts are present and explicit.
- [ ] Selector truth checks pass without empty-selection risk.
