---
phase: 26-qa-settings-user-route-audit-experiment-matrix-and-root-cause-isolation
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [QA-23-01, QA-23-02, DOC-23-01]
files_modified:
  - tests/playwright/ui/routes.best_practices.audit.spec.ts
  - .planning/phases/26-qa-settings-user-route-audit-experiment-matrix-and-root-cause-isolation/26-01-SUMMARY.md
  - .planning/phases/26-qa-settings-user-route-audit-experiment-matrix-and-root-cause-isolation/26-VERIFICATION.md
  - .planning/phases/26-qa-settings-user-route-audit-experiment-matrix-and-root-cause-isolation/26-UAT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
must_haves:
  truths:
    - `/settings` and `/user` route-audit behavior must be tested under explicit unskip mode with minimal branch deltas.
    - Setup health must be separated from route execution failure using global-setup evidence.
    - Default scoped debt behavior remains untouched unless both browsers pass retirement path.
  artifacts:
    - path: var/playwright/runs/phase26-exp1-chromium/
      provides: Chromium EXP1 evidence
      contains: report/index.html
    - path: var/playwright/runs/phase26-exp1-webkit/
      provides: WebKit EXP1 evidence
      contains: report/index.html
    - path: var/playwright/runs/phase26-exp2-chromium/
      provides: Chromium EXP2 evidence
      contains: report/index.html
    - path: var/playwright/runs/phase26-exp2-webkit/
      provides: WebKit EXP2 evidence
      contains: report/index.html
---

# Phase 26 Plan 01: Experiment Matrix for Settings/User Route-Audit Deadlock

## Objective
- **What:** Run two minimal route-local experiment variants against `/settings` + `/user` under unskip mode.
- **Why:** Determine whether debt retirement is blocked by branch strategy or deeper runtime/auth transition behavior.
- **Output:** Evidence-backed conclusion (`retired` or `gaps_found`) with reconciled planning state.

## Tasks

<task type="code">
  <name>Execute EXP1 with standard auth flow path</name>
  <files>tests/playwright/ui/routes.best_practices.audit.spec.ts</files>
  <action>Route `/settings` + `/user` through standard `gotoAndBootstrap(..., ui-only)` path by disabling soft-route special branch and run targeted two-test matrix.</action>
  <verify>
    <automated>cd tests/playwright && PW_UNSKIP_SETTINGS_USER_AUDIT=1 PW_RUN_ID=phase26-exp1-chromium npm run test:ui -- --project=chromium ui/routes.best_practices.audit.spec.ts --grep "best-practices audit: /(settings|user)"</automated>
    <automated>cd tests/playwright && PW_UNSKIP_SETTINGS_USER_AUDIT=1 PW_RUN_ID=phase26-exp1-webkit npm run test:ui -- --project=webkit ui/routes.best_practices.audit.spec.ts --grep "best-practices audit: /(settings|user)"</automated>
  </verify>
  <done>Targeted matrix outputs deterministic pass/fail behavior for both browsers under EXP1.</done>
</task>

<task type="code">
  <name>Execute EXP2 with soft-route branch + auth bypass</name>
  <files>tests/playwright/ui/routes.best_practices.audit.spec.ts</files>
  <action>Re-enable soft-route set and test `mode: none` path for `/settings` + `/user` using same targeted matrix, then restore stable baseline behavior.</action>
  <verify>
    <automated>cd tests/playwright && PW_UNSKIP_SETTINGS_USER_AUDIT=1 PW_RUN_ID=phase26-exp2-chromium npm run test:ui -- --project=chromium ui/routes.best_practices.audit.spec.ts --grep "best-practices audit: /(settings|user)"</automated>
    <automated>cd tests/playwright && PW_UNSKIP_SETTINGS_USER_AUDIT=1 PW_RUN_ID=phase26-exp2-webkit npm run test:ui -- --project=webkit ui/routes.best_practices.audit.spec.ts --grep "best-practices audit: /(settings|user)"</automated>
  </verify>
  <done>Targeted matrix outputs deterministic pass/fail behavior for both browsers under EXP2 and baseline branch is restored.</done>
</task>

## Success Criteria
- [ ] At least one experiment path retires `/settings` + `/user` debt in both browsers.
- [ ] If no experiment retires debt, blocker evidence is complete and planning reconciles to `gaps_found`.
