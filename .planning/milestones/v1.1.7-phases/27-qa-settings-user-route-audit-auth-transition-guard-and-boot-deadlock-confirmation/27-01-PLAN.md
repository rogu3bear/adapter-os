---
phase: 27-qa-settings-user-route-audit-auth-transition-guard-and-boot-deadlock-confirmation
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [QA-23-01, QA-23-02, DOC-23-01]
files_modified:
  - tests/playwright/ui/utils.ts
  - .planning/phases/27-qa-settings-user-route-audit-auth-transition-guard-and-boot-deadlock-confirmation/27-01-SUMMARY.md
  - .planning/phases/27-qa-settings-user-route-audit-auth-transition-guard-and-boot-deadlock-confirmation/27-VERIFICATION.md
  - .planning/phases/27-qa-settings-user-route-audit-auth-transition-guard-and-boot-deadlock-confirmation/27-UAT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
must_haves:
  truths:
    - Keep selector/CI contracts unchanged while testing `/settings` and `/user` retirement path.
    - Prove whether auth-transition guard removes login-surface churn mode without claiming full retirement success.
    - Record blocker evidence under `var/playwright/runs/phase27-*/...`.
  artifacts:
    - path: var/playwright/runs/phase27-fingerprint-chromium/
      provides: Chromium fingerprint repro for `/settings`
      contains: report/index.html
    - path: var/playwright/runs/phase27-fix1-chromium/
      provides: Chromium targeted pair after auth-transition guard
      contains: report/index.html
    - path: var/playwright/runs/phase27-fix1-webkit/
      provides: WebKit targeted pair after auth-transition guard
      contains: report/index.html
---

# Phase 27 Plan 01: Auth Transition Guard + Targeted Dual-Browser Recheck

## Objective
- **What:** Harden `attemptUiLogin` against login-form detachment during automatic redirect, then re-run `/settings` + `/user` unskip matrix.
- **Why:** Remove known auth-surface false-negative churn and determine remaining blocker class.
- **Output:** Evidence-backed phase closure (`retired` or `gaps_found`) with reconciled planning state.

## Tasks

<task type="code">
  <name>Add redirect-aware recovery in auth bootstrap</name>
  <files>tests/playwright/ui/utils.ts</files>
  <action>Update `attemptUiLogin` to recover when login fields detach because auth state navigates to non-auth surfaces (e.g., `/chat`).</action>
  <verify>
    <automated>cd tests/playwright && PW_UNSKIP_SETTINGS_USER_AUDIT=1 PW_RUN_ID=phase27-fingerprint-chromium npm run test:ui -- --project=chromium ui/routes.best_practices.audit.spec.ts --grep "best-practices audit: /settings"</automated>
  </verify>
  <done>Trace evidence shows redirect churn is recognized and flow advances to route navigation.</done>
</task>

<task type="code">
  <name>Execute targeted unskip matrix after guard</name>
  <files>tests/playwright/ui/utils.ts</files>
  <action>Run `/settings` + `/user` in Chromium and WebKit under unskip mode using updated helper logic.</action>
  <verify>
    <automated>cd tests/playwright && PW_UNSKIP_SETTINGS_USER_AUDIT=1 PW_RUN_ID=phase27-fix1-chromium npm run test:ui -- --project=chromium ui/routes.best_practices.audit.spec.ts --grep "best-practices audit: /(settings|user)"</automated>
    <automated>cd tests/playwright && PW_UNSKIP_SETTINGS_USER_AUDIT=1 PW_RUN_ID=phase27-fix1-webkit npm run test:ui -- --project=webkit ui/routes.best_practices.audit.spec.ts --grep "best-practices audit: /(settings|user)"</automated>
  </verify>
  <done>Both browsers produce deterministic pass/fail evidence for the two-route pair.</done>
</task>

## Success Criteria
- [ ] `/settings` + `/user` pass in both browsers under unskip mode.
- [ ] If failure remains, evidence isolates blocker class after auth transition guard.
