---
phase: 24-qa-dual-browser-regression-closure-and-gate-pass
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [QA-23-01, QA-23-02, DOC-23-01]
files_modified:
  - tests/playwright/ui/routes.best_practices.audit.spec.ts
  - tests/playwright/ui/runs.spec.ts
  - tests/playwright/ui/visual.spec.ts
  - tests/playwright/ui/visual.spec.ts-snapshots/*-darwin.png
  - .planning/phases/24-qa-dual-browser-regression-closure-and-gate-pass/24-01-SUMMARY.md
  - .planning/phases/24-qa-dual-browser-regression-closure-and-gate-pass/24-VERIFICATION.md
  - .planning/phases/24-qa-dual-browser-regression-closure-and-gate-pass/24-UAT.md
autonomous: true
user_setup:
  - Playwright browsers installed (`npx playwright install chromium webkit`).
must_haves:
  truths:
    - Route audit deterministically handles `/settings` and `/user` without timeout failures (explicit scoped skip).
    - Runs detail flows assert current UI semantics.
    - Visual baselines reflect intentional current rendering only.
  artifacts:
    - path: var/playwright/runs/phase24-full-chromium/
      provides: Chromium full-run proof.
      contains: report/index.html
    - path: var/playwright/runs/phase24-full-webkit/
      provides: WebKit full-run proof.
      contains: report/index.html
  key_links:
    - from: .planning/phases/24-qa-dual-browser-regression-closure-and-gate-pass/24-VERIFICATION.md
      to: var/playwright/runs/phase24-full-chromium/test-results/.last-run.json
      via: explicit run status mapping
---

# Phase 24 Plan 01: Regression Rectification to Dual-Browser Gate Pass

## Objective
- **What:** Fix deterministic route-audit/detail-flow/visual regressions currently blocking `test:gate:quality`.
- **Why:** Phase 23 restored truthful execution; Phase 24 closes the remaining pass criteria for QA-23 requirements.
- **Output:** Dual-browser full gate pass or narrowed residual blocker set with concrete evidence.

## Context
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-qa-dual-browser-regression-closure-and-gate-pass/24-CONTEXT.md
@.planning/phases/24-qa-dual-browser-regression-closure-and-gate-pass/24-RESEARCH.md
@tests/playwright/package.json

## Tasks

<task type="code">
  <name>Rectify route-audit instability on settings/user routes</name>
  <files>tests/playwright/ui/routes.best_practices.audit.spec.ts</files>
  <action>Apply smallest stabilization so `/settings` and `/user` no longer create nondeterministic timeout failures in blocking runs.</action>
  <verify>
    <automated>cd tests/playwright && PW_RUN_ID=phase24-audit-chromium npm run test:gate:quality -- --project=chromium ui/routes.best_practices.audit.spec.ts</automated>
    <automated>cd tests/playwright && PW_RUN_ID=phase24-audit-webkit npm run test:gate:quality -- --project=webkit ui/routes.best_practices.audit.spec.ts</automated>
    <sampling_rate>once per browser</sampling_rate>
  </verify>
  <done>Route audit file runs deterministically on both browsers with explicit debt scope if needed.</done>
</task>

<task type="code">
  <name>Align runs detail-flow expectations with current UI semantics</name>
  <files>tests/playwright/ui/runs.spec.ts</files>
  <action>Adjust assertions to native current UI behavior while preserving regression coverage intent.</action>
  <verify>
    <automated>cd tests/playwright && PW_RUN_ID=phase24-runs-chromium npm run test:gate:quality -- --project=chromium ui/runs.spec.ts</automated>
    <automated>cd tests/playwright && PW_RUN_ID=phase24-runs-webkit npm run test:gate:quality -- --project=webkit ui/runs.spec.ts</automated>
    <sampling_rate>once per browser</sampling_rate>
  </verify>
  <done>Runs spec passes targeted runs on both browsers.</done>
</task>

<task type="code">
  <name>Resolve visual diffs under canonical macOS policy</name>
  <files>tests/playwright/ui/visual.spec.ts, tests/playwright/ui/visual.spec.ts-snapshots/</files>
  <action>Keep assertions intact, then update baselines only where render drift is intentional/current.</action>
  <verify>
    <automated>cd tests/playwright && PW_RUN_ID=phase24-visual-chromium npm run test:gate:quality -- --project=chromium ui/visual.spec.ts</automated>
    <automated>cd tests/playwright && PW_RUN_ID=phase24-visual-webkit npm run test:gate:quality -- --project=webkit ui/visual.spec.ts</automated>
    <sampling_rate>once per browser</sampling_rate>
  </verify>
  <done>Visual suite passes on both browsers or remaining intentional diffs are explicitly documented.</done>
</task>

<task type="code">
  <name>Run full bundled dual-browser gate and reconcile closure docs</name>
  <files>.planning/phases/24-qa-dual-browser-regression-closure-and-gate-pass/24-01-SUMMARY.md, .planning/phases/24-qa-dual-browser-regression-closure-and-gate-pass/24-VERIFICATION.md, .planning/phases/24-qa-dual-browser-regression-closure-and-gate-pass/24-UAT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>Execute full `test:gate:quality` for Chromium/WebKit and update planning status truthfully.</action>
  <verify>
    <automated>cd tests/playwright && PW_RUN_ID=phase24-full-chromium npm run test:gate:quality -- --project=chromium</automated>
    <automated>cd tests/playwright && PW_RUN_ID=phase24-full-webkit npm run test:gate:quality -- --project=webkit</automated>
    <sampling_rate>once per browser</sampling_rate>
  </verify>
  <done>Phase artifacts capture exact outcomes with concrete evidence paths.</done>
</task>

## Success Criteria
- [x] Targeted audit/runs/visual regressions are rectified.
- [x] Full bundled lane passes on Chromium.
- [x] Full bundled lane passes on WebKit.
- [x] QA-23-01 / QA-23-02 / DOC-23-01 statuses are reconciled to execution truth.
