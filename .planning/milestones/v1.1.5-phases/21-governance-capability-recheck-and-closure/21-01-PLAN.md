---
phase: 21-governance-capability-recheck-and-closure
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [GOV-16, AUTO-03]
files_modified:
  - var/evidence/governance-capability-rerun-<UTCSTAMP>/capability-loop.log
  - var/evidence/governance-capability-rerun-<UTCSTAMP>/gate-state.txt
  - var/evidence/governance-capability-rerun-<UTCSTAMP>/loop-summary.txt
  - var/evidence/governance-capability-rerun-<UTCSTAMP>/preflight-confirmation.log
  - var/evidence/governance-capability-rerun-<UTCSTAMP>/branch-decision.txt
autonomous: true
user_setup:
  - GitHub CLI auth remains valid for canonical repository reads.
must_haves:
  truths:
    - Canonical capability state is freshly measured before any write attempt.
    - Branch decision (`capable` vs `blocked_external`) is artifact-backed and deterministic.
  artifacts:
    - path: var/evidence/governance-capability-rerun-<UTCSTAMP>/loop-summary.txt
      provides: Deterministic polling summary for this execution window.
      contains: final_status=
    - path: var/evidence/governance-capability-rerun-<UTCSTAMP>/branch-decision.txt
      provides: Explicit downstream branch contract for Plan 21-02.
      contains: branch=
  key_links:
    - from: var/evidence/governance-capability-rerun-<UTCSTAMP>/gate-state.txt
      to: var/evidence/governance-capability-rerun-<UTCSTAMP>/branch-decision.txt
      via: deterministic gate-state classification
---

# Phase 21 Plan 01: Fresh Capability Gate and Branch Contract

## Objective
- **What:** Capture a fresh deterministic capability evidence bundle and lock branch decision for execution.
- **Why:** `GOV-16` cannot close without a current, artifact-backed gate truth before running canonical enforcement.
- **Output:** Immutable capability rerun receipts with explicit `branch=` decision.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-governance-capability-recheck-and-closure/21-CONTEXT.md
@.planning/phases/21-governance-capability-recheck-and-closure/21-RESEARCH.md
@scripts/ci/run_governance_capability_loop.sh

## Tasks

<task type="code">
  <name>Run deterministic canonical capability loop</name>
  <files>var/evidence/governance-capability-rerun-&lt;UTCSTAMP&gt;/capability-loop.log, var/evidence/governance-capability-rerun-&lt;UTCSTAMP&gt;/gate-state.txt, var/evidence/governance-capability-rerun-&lt;UTCSTAMP&gt;/loop-summary.txt</files>
  <action>Run `scripts/ci/run_governance_capability_loop.sh` for canonical target with deterministic defaults (`attempts=4`, `sleep-seconds=2`) and capture all receipts in a new UTC-stamped evidence directory.</action>
  <verify>
    <automated>bash scripts/ci/run_governance_capability_loop.sh --repo rogu3bear/adapter-os --branch main --required-context 'FFI AddressSanitizer (push)' --output-dir var/evidence/governance-capability-rerun-&lt;UTCSTAMP&gt; --attempts 4 --sleep-seconds 2</automated>
    <manual>Confirm `gate-state.txt` matches the final loop summary status token.</manual>
    <sampling_rate>run once after command completion</sampling_rate>
  </verify>
  <done>Fresh gate-state evidence is complete and immutable.</done>
</task>

<task type="code">
  <name>Capture single preflight confirmation probe</name>
  <files>var/evidence/governance-capability-rerun-&lt;UTCSTAMP&gt;/preflight-confirmation.log, var/evidence/governance-capability-rerun-&lt;UTCSTAMP&gt;/preflight-confirmation.exit</files>
  <action>Run `check_governance_preflight.sh` once after the loop and store stdout/stderr plus exit code to anchor point-in-time gate truth.</action>
  <verify>
    <automated>bash scripts/ci/check_governance_preflight.sh --repo rogu3bear/adapter-os --branch main --required-context 'FFI AddressSanitizer (push)' &gt; var/evidence/governance-capability-rerun-&lt;UTCSTAMP&gt;/preflight-confirmation.log 2&gt;&amp;1; echo $? &gt; var/evidence/governance-capability-rerun-&lt;UTCSTAMP&gt;/preflight-confirmation.exit</automated>
    <manual>Verify confirmation status is consistent with `gate-state.txt` unless explicitly documented as transient drift.</manual>
    <sampling_rate>run immediately after loop</sampling_rate>
  </verify>
  <done>Capability truth is backed by both loop and point probe receipts.</done>
</task>

<task type="code">
  <name>Emit branch decision contract for Plan 21-02</name>
  <files>var/evidence/governance-capability-rerun-&lt;UTCSTAMP&gt;/branch-decision.txt</files>
  <action>Generate an explicit branch contract from gate-state (`branch=capable` or `branch=blocked_external`) and map it to the required next action (`run_plan_21_02_capable_proof` or `retain_blocker_branch`).</action>
  <verify>
    <automated>test -f var/evidence/governance-capability-rerun-&lt;UTCSTAMP&gt;/gate-state.txt &amp;&amp; rg -n '^(capable|blocked_external)$' var/evidence/governance-capability-rerun-&lt;UTCSTAMP&gt;/gate-state.txt</automated>
    <manual>Ensure branch receipt references only artifacts from this same UTCSTAMP directory.</manual>
    <sampling_rate>run after branch decision receipt creation</sampling_rate>
  </verify>
  <done>Plan 21-02 has a machine-checkable execution branch contract.</done>
</task>

## Verification
- Confirm capability rerun artifacts exist and are internally consistent.
- Confirm branch decision is explicit and restart-safe.
- Confirm no write path is attempted in Plan 21-01.

## Success Criteria
- [ ] Fresh deterministic gate-state evidence is captured.
- [ ] Branch contract is explicit and immutable.
- [ ] Plan 21-02 can execute without reinterpreting gate truth.
