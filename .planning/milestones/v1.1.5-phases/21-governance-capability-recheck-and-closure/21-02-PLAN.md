---
phase: 21-governance-capability-recheck-and-closure
plan: 02
type: execute
wave: 2
depends_on: [21-01]
requirements: [GOV-16, AUTO-03]
files_modified:
  - var/evidence/governance-enforcement-rerun-<UTCSTAMP>/gate-state.txt
  - var/evidence/governance-enforcement-rerun-<UTCSTAMP>/verification.txt
  - var/evidence/governance-enforcement-rerun-<UTCSTAMP>/blocked-write-attempts.txt
  - var/evidence/governance-enforcement-rerun-<UTCSTAMP>/post-read.json
  - var/evidence/governance-enforcement-rerun-<UTCSTAMP>/rollback-post-read.json
  - var/evidence/governance-enforcement-rerun-<UTCSTAMP>/execution-branch.txt
autonomous: true
user_setup:
  - Canonical repo permissions must allow required-check API writes when capability becomes available.
must_haves:
  truths:
    - Canonical executor emits one authoritative branch outcome (`enforced_verified`, `blocked_external`, or guarded failure).
    - Capable-path success requires verification matrix proof; non-capable/failure branches require explicit guarded artifacts.
  artifacts:
    - path: var/evidence/governance-enforcement-rerun-<UTCSTAMP>/execution-branch.txt
      provides: Canonical branch classification and downstream action routing.
      contains: status=
    - path: var/evidence/governance-enforcement-rerun-<UTCSTAMP>/verification.txt
      provides: Capable-path verification matrix when write/readback branch executes.
      contains: verify_ok=
  key_links:
    - from: var/evidence/governance-enforcement-rerun-<UTCSTAMP>/gate-state.txt
      to: var/evidence/governance-enforcement-rerun-<UTCSTAMP>/execution-branch.txt
      via: hard-gated executor status classification
---

# Phase 21 Plan 02: Canonical Enforcement Proof Re-Run

## Objective
- **What:** Execute canonical required-check flow for this new capability window and classify the exact branch result.
- **Why:** `GOV-16` closes only through canonical `enforced_verified` proof or explicit blocked/failure evidence.
- **Output:** Canonical enforcement rerun bundle with branch classification receipt.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-governance-capability-recheck-and-closure/21-CONTEXT.md
@.planning/phases/21-governance-capability-recheck-and-closure/21-RESEARCH.md
@.planning/phases/21-governance-capability-recheck-and-closure/21-01-PLAN.md
@scripts/ci/execute_governance_required_checks.sh

## Tasks

<task type="code">
  <name>Run canonical required-check executor</name>
  <files>var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;/preflight-before.log, var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;/gate-state.txt, var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;/verification.txt</files>
  <action>Execute `scripts/ci/execute_governance_required_checks.sh` with canonical target and approved manifest into a new UTC-stamped rerun directory.</action>
  <verify>
    <automated>bash scripts/ci/execute_governance_required_checks.sh --repo rogu3bear/adapter-os --branch main --required-context 'FFI AddressSanitizer (push)' --manifest docs/governance/target-manifest.json --output-dir var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;</automated>
    <manual>Capture status and exit code for deterministic branch classification.</manual>
    <sampling_rate>run once per execution window</sampling_rate>
  </verify>
  <done>Canonical execution outcome is captured in a single evidence directory.</done>
</task>

<task type="code">
  <name>Validate branch-specific artifact contract</name>
  <files>var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;/blocked-write-attempts.txt, var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;/post-read.json, var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;/rollback-post-read.json</files>
  <action>Validate artifacts by branch: `blocked_external` requires strict no-write receipt, `enforced_verified` requires verification + post-read proof, guarded failure requires rollback receipts.</action>
  <verify>
    <automated>test -f var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;/gate-state.txt &amp;&amp; (test -f var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;/verification.txt || test -f var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;/blocked-write-attempts.txt || test -f var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;/rollback-post-read.json)</automated>
    <manual>Confirm branch artifacts are complete and contradiction-free.</manual>
    <sampling_rate>run after executor completion</sampling_rate>
  </verify>
  <done>Branch-specific contract is explicitly proven for this run.</done>
</task>

<task type="code">
  <name>Emit execution-branch classification receipt</name>
  <files>var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;/execution-branch.txt</files>
  <action>Write a deterministic status receipt with `status`, `exit_code`, `proof_state`, and `next_action` (`close_gov16`, `retain_blocker_debt`, or `investigate_guarded_failure`).</action>
  <verify>
    <automated>rg -n '^status=' var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;/execution-branch.txt &amp;&amp; rg -n '^next_action=' var/evidence/governance-enforcement-rerun-&lt;UTCSTAMP&gt;/execution-branch.txt</automated>
    <manual>Ensure next_action aligns with emitted status and available artifacts.</manual>
    <sampling_rate>run after receipt creation</sampling_rate>
  </verify>
  <done>Plan 21-03 can reconcile artifacts from a single canonical branch receipt.</done>
</task>

## Verification
- Confirm executor artifacts satisfy exactly one branch contract.
- Confirm classification receipt is deterministic and complete.
- Confirm capable closure is asserted only with immutable verification proof.

## Success Criteria
- [ ] Canonical rerun emits deterministic branch outcome and artifacts.
- [ ] Branch-specific contract validation passes.
- [ ] Execution-branch receipt is ready for reconciliation.
