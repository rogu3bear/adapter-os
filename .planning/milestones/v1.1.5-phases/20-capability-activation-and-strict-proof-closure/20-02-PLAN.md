---
phase: 20-capability-activation-and-strict-proof-closure
plan: 02
type: execute
wave: 2
depends_on: [20-01]
requirements: [GOV-16, AUTO-03]
files_modified:
  - var/evidence/governance-enforcement-exec-<UTCSTAMP>/gate-state.txt
  - var/evidence/governance-enforcement-exec-<UTCSTAMP>/verification.txt
  - var/evidence/governance-enforcement-exec-<UTCSTAMP>/blocked-write-attempts.txt
  - var/evidence/governance-enforcement-exec-<UTCSTAMP>/post-read.json
  - var/evidence/governance-enforcement-exec-<UTCSTAMP>/rollback-post-read.json
  - var/evidence/governance-enforcement-exec-<UTCSTAMP>/execution-branch.txt
autonomous: true
user_setup:
  - Canonical repo permissions must allow required-check API writes for capable-path proof (`status=capable`).
must_haves:
  truths:
    - Canonical enforcement run emits a single authoritative execution outcome (`enforced_verified`, `blocked_external`, or guarded failure) with immutable receipts.
    - Capable-path success requires strict + contexts readback verification; failure path includes rollback evidence.
  artifacts:
    - path: var/evidence/governance-enforcement-exec-<UTCSTAMP>/verification.txt
      provides: Canonical verification matrix from executor contract.
      contains: verify_ok=
    - path: var/evidence/governance-enforcement-exec-<UTCSTAMP>/execution-branch.txt
      provides: Deterministic outcome classification and downstream reconciliation directive.
      contains: status=
  key_links:
    - from: var/evidence/governance-enforcement-exec-<UTCSTAMP>/gate-state.txt
      to: var/evidence/governance-enforcement-exec-<UTCSTAMP>/execution-branch.txt
      via: executor status classification
    - from: var/evidence/governance-enforcement-exec-<UTCSTAMP>/verification.txt
      to: var/evidence/governance-enforcement-exec-<UTCSTAMP>/execution-branch.txt
      via: capable-path proof verdict
---

# Phase 20 Plan 02: Canonical Required-Check Proof Execution

## Objective
- **What:** Execute canonical enforcement proof path with hard-gated behavior and branch-safe verification receipts.
- **Why:** GOV-16 closes only when canonical write/readback proof succeeds or blocker/failure branch is explicitly captured without contradiction.
- **Output:** Canonical enforcement evidence package with outcome classification receipt.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-capability-activation-and-strict-proof-closure/20-CONTEXT.md
@.planning/phases/20-capability-activation-and-strict-proof-closure/20-RESEARCH.md
@.planning/phases/20-capability-activation-and-strict-proof-closure/20-01-PLAN.md
@scripts/ci/execute_governance_required_checks.sh

## Tasks

<task type="code">
  <name>Execute canonical required-check enforcement flow</name>
  <files>var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;/preflight-before.log, var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;/gate-state.txt, var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;/verification.txt</files>
  <action>Run `scripts/ci/execute_governance_required_checks.sh` for canonical target using target manifest and a new UTC-stamped output directory; preserve raw stdout/stderr and exit code for branch classification.</action>
  <verify>
    <automated>bash scripts/ci/execute_governance_required_checks.sh --repo rogu3bear/adapter-os --branch main --required-context 'FFI AddressSanitizer (push)' --manifest docs/governance/target-manifest.json --output-dir var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;</automated>
    <manual>Capture the emitted status token and command exit code in execution notes for branch routing.</manual>
    <sampling_rate>run once per attempt</sampling_rate>
  </verify>
  <done>Executor run completes with full artifact package for one deterministic branch.</done>
</task>

<task type="code">
  <name>Validate branch-specific contract artifacts</name>
  <files>var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;/blocked-write-attempts.txt, var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;/post-read.json, var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;/rollback-post-read.json</files>
  <action>Validate artifact presence by branch: for `blocked_external`, require no-write receipt; for `enforced_verified`, require post-read and verification proof; for verification failure, require rollback artifacts. Record any contract gap as explicit blocker evidence.</action>
  <verify>
    <automated>test -f var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;/gate-state.txt &amp;&amp; (test -f var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;/verification.txt || test -f var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;/blocked-write-attempts.txt || test -f var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;/rollback-post-read.json)</automated>
    <manual>Confirm branch-specific mandatory artifacts exist and contradiction-free.</manual>
    <sampling_rate>run after executor command</sampling_rate>
  </verify>
  <done>Canonical execution branch satisfies deterministic artifact contract.</done>
</task>

<task type="code">
  <name>Emit canonical execution-branch classification receipt</name>
  <files>var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;/execution-branch.txt</files>
  <action>Generate a stable receipt with `status`, `exit_code`, `proof_state`, and `next_action` derived from executor outputs; include one of: `reconcile_capable_success`, `retain_blocker_debt`, `investigate_guarded_failure`.</action>
  <verify>
    <automated>rg -n '^status=' var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;/execution-branch.txt &amp;&amp; rg -n '^next_action=' var/evidence/governance-enforcement-exec-&lt;UTCSTAMP&gt;/execution-branch.txt</automated>
    <manual>Ensure next_action aligns with executor status and available artifacts.</manual>
    <sampling_rate>run after classification receipt creation</sampling_rate>
  </verify>
  <done>Plan 03 can reconcile documentation and target outcomes from a single canonical status receipt.</done>
</task>

## Verification
- Confirm executor output directory includes branch-appropriate receipts and no contradictory files.
- Confirm execution-branch receipt maps status to exactly one downstream action.
- Confirm capable-path proof is declared only when verification matrix indicates success.

## Success Criteria
- [ ] Canonical executor run yields deterministic status and complete branch artifacts.
- [ ] Branch-specific contract validation is explicit and reproducible.
- [ ] Execution-branch receipt is ready for reconciliation in Plan 03.
