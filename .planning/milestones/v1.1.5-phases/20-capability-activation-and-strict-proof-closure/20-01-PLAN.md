---
phase: 20-capability-activation-and-strict-proof-closure
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [GOV-16, AUTO-03]
files_modified:
  - var/evidence/governance-capability-activation-<UTCSTAMP>/capability-loop.log
  - var/evidence/governance-capability-activation-<UTCSTAMP>/gate-state.txt
  - var/evidence/governance-capability-activation-<UTCSTAMP>/loop-summary.txt
  - var/evidence/governance-capability-activation-<UTCSTAMP>/preflight-attempt-XX.log
  - var/evidence/governance-capability-activation-<UTCSTAMP>/branch-decision.txt
autonomous: true
user_setup:
  - GitHub CLI auth remains valid for canonical repository reads.
must_haves:
  truths:
    - Canonical capability state is measured deterministically before any policy mutation path.
    - Branch decision (`capable` vs `blocked_external`) is artifact-backed and restart-safe.
  artifacts:
    - path: var/evidence/governance-capability-activation-<UTCSTAMP>/loop-summary.txt
      provides: Canonical loop summary with deterministic final status and exit code.
      contains: final_status=
    - path: var/evidence/governance-capability-activation-<UTCSTAMP>/branch-decision.txt
      provides: Explicit decision receipt that determines whether Plan 02 runs capable proof or blocker branch capture.
      contains: branch=
  key_links:
    - from: var/evidence/governance-capability-activation-<UTCSTAMP>/capability-loop.log
      to: var/evidence/governance-capability-activation-<UTCSTAMP>/branch-decision.txt
      via: deterministic preflight polling summary
---

# Phase 20 Plan 01: Capability Gate Readiness and Branch Decision

## Objective
- **What:** Capture deterministic canonical capability polling evidence and lock branch decision for Phase 20 execution.
- **Why:** GOV-16 prohibits any write path until capability is proven; all downstream work depends on this gate truth.
- **Output:** Immutable capability loop evidence package with explicit branch decision receipt.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-capability-activation-and-strict-proof-closure/20-CONTEXT.md
@.planning/phases/20-capability-activation-and-strict-proof-closure/20-RESEARCH.md
@docs/governance/README.md

## Tasks

<task type="code">
  <name>Run deterministic canonical capability loop</name>
  <files>var/evidence/governance-capability-activation-&lt;UTCSTAMP&gt;/capability-loop.log, var/evidence/governance-capability-activation-&lt;UTCSTAMP&gt;/gate-state.txt, var/evidence/governance-capability-activation-&lt;UTCSTAMP&gt;/loop-summary.txt</files>
  <action>Execute `scripts/ci/run_governance_capability_loop.sh` for the canonical target (`rogu3bear/adapter-os`, `main`, `FFI AddressSanitizer (push)`) using the discuss-locked defaults (`attempts=4`, `sleep-seconds=2`) and capture all loop receipts in a new UTC-stamped evidence directory.</action>
  <verify>
    <automated>bash scripts/ci/run_governance_capability_loop.sh --repo rogu3bear/adapter-os --branch main --required-context 'FFI AddressSanitizer (push)' --output-dir var/evidence/governance-capability-activation-&lt;UTCSTAMP&gt; --attempts 4 --sleep-seconds 2</automated>
    <manual>Confirm `gate-state.txt` exists and matches final loop status in `loop-summary.txt`.</manual>
    <sampling_rate>run once after command completion</sampling_rate>
  </verify>
  <done>Capability loop artifacts are complete, deterministic, and immutable.</done>
</task>

<task type="code">
  <name>Capture immediate preflight confirmation receipt</name>
  <files>var/evidence/governance-capability-activation-&lt;UTCSTAMP&gt;/preflight-confirmation.log, var/evidence/governance-capability-activation-&lt;UTCSTAMP&gt;/preflight-confirmation.exit</files>
  <action>Run a single `check_governance_preflight.sh` probe after the loop to anchor the latest gate truth and exit code in explicit confirmation receipts.</action>
  <verify>
    <automated>bash scripts/ci/check_governance_preflight.sh --repo rogu3bear/adapter-os --branch main --required-context 'FFI AddressSanitizer (push)' &gt; var/evidence/governance-capability-activation-&lt;UTCSTAMP&gt;/preflight-confirmation.log 2&gt;&amp;1; echo $? &gt; var/evidence/governance-capability-activation-&lt;UTCSTAMP&gt;/preflight-confirmation.exit</automated>
    <manual>Verify confirmation status token matches `gate-state.txt` unless a race condition note is explicitly recorded.</manual>
    <sampling_rate>run immediately after loop</sampling_rate>
  </verify>
  <done>Canonical gate truth has both loop-level and point-in-time confirmation receipts.</done>
</task>

<task type="code">
  <name>Emit branch decision contract for downstream plans</name>
  <files>var/evidence/governance-capability-activation-&lt;UTCSTAMP&gt;/branch-decision.txt</files>
  <action>Write a deterministic branch decision receipt (`branch=capable` or `branch=blocked_external`) derived from `gate-state.txt`, including required downstream action (`run_plan_20_02_capable_proof` vs `capture_blocker_branch_only`).</action>
  <verify>
    <automated>test -f var/evidence/governance-capability-activation-&lt;UTCSTAMP&gt;/gate-state.txt &amp;&amp; rg -n '^(capable|blocked_external)$' var/evidence/governance-capability-activation-&lt;UTCSTAMP&gt;/gate-state.txt</automated>
    <manual>Confirm branch-decision token is unambiguous and references the same evidence directory.</manual>
    <sampling_rate>run after decision receipt creation</sampling_rate>
  </verify>
  <done>Phase 20 has an explicit, machine-checkable branch contract for Plan 02.</done>
</task>

## Verification
- Confirm evidence directory contains capability loop, gate-state, confirmation, and branch-decision receipts.
- Confirm branch decision is derived only from captured gate-state artifacts.
- Confirm no policy write path is attempted in this plan.

## Success Criteria
- [ ] Canonical capability gate is measured with deterministic loop artifacts.
- [ ] Branch decision receipt is explicit and restart-safe.
- [ ] Plan 02 can execute without reinterpreting gate truth.
