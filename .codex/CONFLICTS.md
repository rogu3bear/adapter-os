# Conflicts

## Commit message format

- Conflict: `CONTRIBUTING.md` requires `type(scope): ...` with a detailed body, while mission instructions require `fix: <summary> (closes #N)` format.
- Resolution: follow mission-required format for this sweep, and include scope only if it doesn't violate the required prefix; document in PR description if needed.
