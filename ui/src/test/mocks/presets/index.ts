/**
 * Test Presets
 *
 * Factory functions and vi.mock() helpers for common testing scenarios.
 *
 * NOTE: vi.mock() must be called at module scope. These presets provide
 * factory functions that create mock objects, plus helper functions that
 * return vi.mock() factory callbacks.
 */

export * from './chat-test';
