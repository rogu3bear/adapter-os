//! Authentication and authorization related types.
//!
//! Note: Most auth types (TokenResponse, UserInfoResponse, etc.) are imported
//! from adapteros-api-types. This module contains additional types specific
//! to the server API layer.

// Re-export from api-types is done at mod.rs level via pub use adapteros_api_types::*
