[package]
name = "adapteros-profiler"
version = "0.1.0"
edition = "2021"

[dependencies]
adapteros-core = { path = "../adapteros-core" }
adapteros-telemetry = { path = "../adapteros-telemetry" }
parking_lot = "0.12"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]

