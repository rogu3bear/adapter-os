[package]
name = "adapteros-graph"
version = "0.1.0"
edition = "2021"
description = "Tensor metadata canonicalization and hash graph for AdapterOS"

[dependencies]
adapteros-core = { path = "../adapteros-core" }
serde = { workspace = true }
serde_cbor = { workspace = true }
serde_json = { workspace = true }
blake3 = { workspace = true }
hex = { workspace = true }

[dev-dependencies]
criterion = "0.5"
