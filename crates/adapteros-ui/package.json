{
  "name": "adapteros-ui-leptos",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i tailwind.css -o dist/tailwind.css --minify",
    "dev:css": "tailwindcss -i tailwind.css -o dist/tailwind.css --watch",
    "build": "trunk build --release",
    "dev": "trunk serve"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
