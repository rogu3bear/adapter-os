[package]
name = "adapteros-lora-kernel-prof"
version.workspace = true
edition = "2021"

[dependencies]
adapteros-core = { path = "../adapteros-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[target.'cfg(target_os = "macos")'.dependencies]
metal = { workspace = true }
mach2 = "0.4"

[dev-dependencies]
