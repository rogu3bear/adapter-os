[package]
name = "adapteros-numerics"
version = "0.1.0"
edition = "2021"
authors = ["AdapterOS Team"]
description = "Numerical stability tracking and quantization noise measurement for AdapterOS kernels"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
tracing = "0.1"

[dev-dependencies]
criterion = "0.5"
