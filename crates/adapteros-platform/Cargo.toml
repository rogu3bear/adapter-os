[package]
name = "adapteros-platform"
version.workspace = true
edition = "2021"
authors = ["adapterOS Team"]
description = "Cross-platform filesystem operations for adapterOS"
license = "MIT OR Apache-2.0"

[dependencies]
adapteros-storage = { path = "../adapteros-storage" }
