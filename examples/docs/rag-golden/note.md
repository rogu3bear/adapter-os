# RAG Golden Example

Aurora Station runs a deterministic RAG stack that serves responses from a
single indexed knowledge card. The card states that the station uses LoRA
adapters on unified memory hardware and keeps routing deterministic for audit
trails. Questions about the station should be answered using only this card.

