# DB Contract Rectification Audit Anchor (2026-02-20)

## Scope
- Full backend SQL contract sweep against migrated schema.
- Breaking worker detail field rename is accepted (`last_heartbeat_at` -> `last_seen_at`).
- Missing-schema behavior must fail closed.

## Mismatch Ledger

| ID | Query location | Schema evidence | Severity | Fix owner | Test that proves fix |
|---|---|---|---|---|---|
| AUD-001 | `crates/adapteros-db/src/workers.rs:504` selects `last_heartbeat_at` in `get_worker_detail` and `WorkerDetail` exposes `last_heartbeat_at` at `crates/adapteros-db/src/workers.rs:847`; API response emits it at `crates/adapteros-server-api/src/handlers/worker_detail.rs:42`/`185` | Workers column renamed to `last_seen_at` via `migrations/0122_workers_rename_last_heartbeat_column.sql:4` | P0 | DB Contract + API Contract | `cargo test -p adapteros-server-api --test worker_detail_contract` |
| AUD-002 | `crates/adapteros-server-api/src/runtime_mode.rs:189` and `:258` query `settings` table directly | Canonical table is `system_settings` (`migrations/0110_system_settings.sql:4`) and helper exists at `crates/adapteros-db/src/lib.rs:3194` | P0 | API Contract | `cargo test -p adapteros-server-api --test runtime_mode_system_settings_contract` |
| AUD-003 | `crates/adapteros-db/src/inference_trace.rs:2495` selects `adapter_ids_blob, gates_blob` in `get_provenance_chain` | Canonical columns are `selected_adapter_ids` and `gates_q15` in `migrations/0192_inference_trace_v2.sql:51-52` | P0 | DB Contract | `cargo test -p adapteros-db --test provenance_chain_contract` |
| AUD-004 | `crates/adapteros-server-api/src/citations.rs:105-108` selects nonexistent `chunk_index`, `line_start`, `line_end`, `content_preview` from `training_dataset_rows` | Canonical columns are `id`, `source_file`, `source_line`, `content_hash_b3`, `prompt`, `response` in `migrations/0246_training_dataset_rows.sql:23-43` | P0 | API Contract | `cargo test -p adapteros-server-api --test citations_contract` |
| AUD-005 | `crates/adapteros-server-api/src/cab_workflow.rs:96` queries nonexistent `plans.plan_id`, `plans.metallib_hash`, `plans.adapter_hashes` and `:143` queries `artifacts.cpid` | Canonical plan fields are `plan_id_b3`, `metallib_hash_b3`, `kernel_hashes_json` (`migrations/0001_init.sql:83,85`, `migrations/0209_plans_metallib_hash_compat.sql:6`); CAB evidence should use `bundle_signatures.cpid` (`migrations/0004_signing_keys.sql:13`) | P0 | API Contract | `cargo test -p adapteros-server-api --test cab_workflow_contract` |
| AUD-006 | `crates/adapteros-server-api/src/handlers/monitoring/mod.rs:122-127` returns empty payload on missing `process_health_metrics` table | Missing schema should be contract violation, not silent success | P1 | Invariant/Gate | `cargo test -p adapteros-server --test boot_invariants_tests` + targeted monitoring assertions |
| AUD-007 | `crates/adapteros-server-api/src/handlers/capacity.rs:162-172` silently returns `0` active requests when `request_log` missing | Missing schema should be explicit failure; `request_log` is required contract surface | P1 | Invariant/Gate | `cargo test -p adapteros-server --test boot_invariants_tests` |
| AUD-008 | `crates/adapteros-server-api/src/handlers/system_overview.rs:111-115` uses `unwrap_or(0)` for DB counters and `:304-312` treats missing telemetry table as non-fatal unknown | Missing schema should be explicit failure, not synthetic zero/unknown | P1 | Invariant/Gate | `cargo test -p adapteros-server --test boot_invariants_tests` |
| AUD-009 | `crates/adapteros-server-api/src/handlers/inference_metrics.rs:88-134` catches DB errors and returns synthetic zeros | Missing schema should raise structured contract violation | P1 | Invariant/Gate | `cargo test -p adapteros-server --test boot_invariants_tests` |
| AUD-010 | No root migration creates `telemetry_events` or `request_log` tables (search over `migrations/*.sql`) while active reads rely on both | Read queries exist in `crates/adapteros-server-api/src/handlers/inference_metrics.rs`, `crates/adapteros-server-api/src/handlers/metrics.rs`, `crates/adapteros-server-api/src/handlers/capacity.rs` and DB telemetry reads in `crates/adapteros-db/src/telemetry_bundles.rs` / `crates/adapteros-db/src/workers.rs` | P0 | DB Contract | `cargo test -p adapteros-db test_all_root_migrations_have_signatures` and targeted API/DB contract tests |
| AUD-011 | `crates/adapteros-server/src/boot/invariants.rs` has no DAT-008 required table/column invariant | Required columns must include `workers.last_seen_at`, `system_settings.value`, `inference_trace_tokens.selected_adapter_ids`, `inference_trace_tokens.gates_q15`, `training_dataset_rows.prompt`, `training_dataset_rows.response`, `training_dataset_rows.source_line`, `plans.cpid`, `plans.metallib_hash_b3`, `plans.kernel_hashes_json`, `telemetry_events.event_data`, `request_log.status_code`, `request_log.latency_ms` | P0 | Invariant/Gate | `cargo test -p adapteros-server --test boot_invariants_tests` |
| AUD-012 | API schema version remains `1.0.0` (`crates/adapteros-core/src/version.rs:322`, `crates/adapteros-api-types/src/lib.rs:17`) and OpenAPI artifacts are stale until regeneration | Breaking worker detail field rename requires schema bump to `2.0.0` and synchronized OpenAPI artifacts | P0 | API Contract + Integration Sheriff | `./scripts/ci/check_openapi_drift.sh --fix` then sync root `openapi.json` |
| AUD-013 | `/v1/metrics/inference` handler exists in `crates/adapteros-server-api/src/handlers/inference_metrics.rs:101` but route/OpenAPI registration omitted it in `crates/adapteros-server-api/src/routes/mod.rs` | Metrics routing contract requires explicit inference route and system compatibility alias | P0 | API Contract | `cargo test -p adapteros-server-api --test metrics_contract` |
| AUD-014 | Active system metrics route path used fail-open DB fallbacks (`unwrap_or(0)`/`unwrap_or(0.0)`) for `request_log` reads in `crates/adapteros-server-api/src/handlers/metrics.rs` | Missing required runtime tables/columns must surface `SCHEMA_CONTRACT_VIOLATION` (503), not synthetic zeros | P0 | API Contract + Invariant/Gate | `cargo test -p adapteros-server-api --test metrics_contract` |

## Execution Ownership
- DB Contract Agent: `crates/adapteros-db` + `migrations` (+ `migrations/postgres` mirror as needed).
- API Contract Agent: `crates/adapteros-server-api`, `crates/adapteros-api-types`, OpenAPI artifacts.
- Invariant/Gate Agent: `crates/adapteros-server/src/boot/invariants.rs`, `crates/adapteros-config/src/types.rs`, `crates/adapteros-server/tests/boot_invariants_tests.rs`, migration signing + gate checks.
- Integration Sheriff: minimal merge diffs, duplicate-path prevention, targeted test execution.

## Excluded Artifacts
- `tmp_columns.json`: generated local analysis scratch output; excluded from contract patch set because it is non-runtime, non-source, and would introduce noisy, duplicative payload into the merge.
