# UI Walkthrough

Use the canonical journey first:
- [Canonical User Workflow](./CANONICAL_USER_WORKFLOW.md)

## Primary Screens

- `/documents` for document upload and parsing status.
- `/datasets` for dataset and dataset version inspection.
- `/training` for training job creation and status tracking.
- `/adapters` for adapter and adapter version lineage/provenance.
- `/chat` for runtime interaction with selected adapters.
- `/runs` for diagnostic trace and routing detail inspection.
