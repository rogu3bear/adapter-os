# Skills (Legacy Root)

Status: legacy docs-only directory.

## Canonical Skill Runtime

- Active Codex skills are loaded from `$CODEX_HOME/skills/` (for example `/Users/<user>/.codex/skills/`).
- Repository code and tests should not depend on root `skills/` content for runtime behavior.

## Guidance

- Do not add new product/runtime paths under root `skills/`.
- Keep this directory for historical reference only until retirement.
