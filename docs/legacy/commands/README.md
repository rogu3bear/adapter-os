# Commands (Legacy Root)

Status: legacy docs-only directory.

## Canonical Command Surfaces

- Executable CLI commands live under `crates/adapteros-cli/src/commands/`.
- Shell orchestration belongs under `scripts/`.
- Workflow automation roots are under `.agents/workflows/`.

## Guidance

- Do not add new runtime/product command paths under root `commands/`.
- Keep this folder for historical notes only until retirement.
